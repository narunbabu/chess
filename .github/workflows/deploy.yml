name: Deploy to Production

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}   # <-- your private key (PEM), no passphrase
          port: 22
          script_stop: false
          script: |
            # Deployment script with robust error handling
            # Critical errors will be logged but won't fail the entire deployment

            set -e  # Exit on error for critical steps

            echo "🚀 Starting deployment..."

            # ==================== GIT SYNC ====================
            echo ""
            echo "📦 Syncing code from repository..."
            cd /opt/Chess-Web
            git fetch origin
            git reset --hard origin/master
            echo "✅ Code synced successfully"

            # ==================== BACKEND (Laravel) ====================
            echo ""
            echo "🔧 Deploying Laravel backend..."
            cd chess-backend

            # Allow composer to run as superuser (deployment context)
            export COMPOSER_ALLOW_SUPERUSER=1

            # Install dependencies with optimizations
            echo "📦 Installing Composer dependencies..."
            composer install --no-dev --optimize-autoloader --classmap-authoritative || {
              echo "⚠️  Composer install failed, continuing..."
            }

            # Run migrations (non-blocking)
            echo "🗄️  Running database migrations..."
            php artisan migrate --force || {
              echo "⚠️  Migration failed or no new migrations"
            }

            # Clear Laravel caches
            echo "🧹 Clearing Laravel caches..."
            php artisan config:clear || true
            php artisan route:clear || true
            php artisan cache:clear || true
            php artisan view:clear || true

            composer dump-autoload --optimize || true

            # Rebuild caches
            echo "⚡ Rebuilding Laravel caches..."
            php artisan config:cache || true
            php artisan route:cache || true
            php artisan view:cache || true
            php artisan optimize || true

            # Restart queue workers
            php artisan queue:restart || {
              echo "ℹ️  No queue workers running"
            }

            echo "✅ Backend deployment complete"

            # ==================== FRONTEND (React) ====================
            echo ""
            echo "⚛️  Deploying React frontend..."
            cd ../chess-frontend

            # Load NVM and use Node 20
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            nvm use 20 || nvm install 20

            # Clean npm cache if needed
            npm cache clean --force || true

            # Install dependencies
            echo "📦 Installing npm dependencies..."
            npm ci || npm install || {
              echo "⚠️  npm install encountered issues, but continuing..."
            }

            # Try to fix npm vulnerabilities (non-blocking)
            echo "🔒 Checking npm security..."
            npm audit fix --force || {
              echo "ℹ️  Some npm vulnerabilities could not be auto-fixed"
            }

            # Build with improved error handling and source map suppression
            echo "🏗️  Building React application..."
            export DISABLE_ESLINT_PLUGIN=true
            export CI=false
            export GENERATE_SOURCEMAP=false  # Suppress source map warnings
            npm run build || {
              echo "❌ Build failed - this is critical!"
              exit 1  # This is a critical failure
            }

            # Deploy frontend build to web server
            echo "📤 Deploying build to web server..."
            rsync -av --delete build/ /var/www/chess99.com/ || {
              echo "❌ Rsync failed - this is critical!"
              exit 1  # This is a critical failure
            }

            echo "✅ Frontend deployment complete"

            # ==================== PERMISSIONS ====================
            echo ""
            echo "🔐 Setting file permissions..."
            chown -R www-data:www-data /opt/Chess-Web/chess-backend/storage || true
            chown -R www-data:www-data /opt/Chess-Web/chess-backend/bootstrap/cache || true
            echo "✅ Permissions updated"

            # ==================== NGINX CONFIGURATION ====================
            echo ""
            echo "📝 Updating nginx configuration..."
            cd /opt/Chess-Web

            # Disable exit-on-error for nginx config update (non-critical)
            set +e

            if [ -f nginx-api-fix.conf ]; then
              # Backup current config
              cp /etc/nginx/sites-available/api.chess99.com /etc/nginx/sites-available/api.chess99.com.backup-$(date +%Y%m%d-%H%M%S)

              # Copy new config
              cp nginx-api-fix.conf /etc/nginx/sites-available/api.chess99.com

              # Test nginx config
              if nginx -t 2>&1 | grep -q "successful"; then
                echo "✅ Nginx configuration updated successfully"
              else
                echo "⚠️  Nginx configuration test failed, reverting..."
                LATEST_BACKUP=$(ls -t /etc/nginx/sites-available/api.chess99.com.backup-* 2>/dev/null | head -1)
                if [ -n "$LATEST_BACKUP" ]; then
                  cp "$LATEST_BACKUP" /etc/nginx/sites-available/api.chess99.com
                  echo "↩️  Reverted to previous configuration"
                fi
              fi
            else
              echo "⚠️  nginx-api-fix.conf not found, skipping nginx config update"
            fi

            # Re-enable exit-on-error
            set -e

            # ==================== SERVICE RESTART ====================
            echo ""
            echo "♻️  Restarting services..."

            # Disable exit-on-error for service restarts (may not all exist)
            set +e

            systemctl restart php8.3-fpm && echo "✅ PHP-FPM restarted" || echo "⚠️  PHP-FPM restart failed"
            systemctl reload nginx && echo "✅ Nginx reloaded" || echo "⚠️  Nginx reload failed"
            systemctl restart laravel-reverb && echo "✅ Reverb restarted" || echo "ℹ️  Reverb service not found"

            # ==================== SERVICE VERIFICATION ====================
            echo ""
            echo "🔍 Verifying service status..."

            NGINX_STATUS=$(systemctl is-active nginx 2>/dev/null || echo "inactive")
            PHP_STATUS=$(systemctl is-active php8.3-fpm 2>/dev/null || echo "inactive")
            REVERB_STATUS=$(systemctl is-active laravel-reverb 2>/dev/null || echo "inactive")

            if [ "$NGINX_STATUS" = "active" ]; then
              echo "✅ Nginx is running"
            else
              echo "❌ Nginx is not running"
            fi

            if [ "$PHP_STATUS" = "active" ]; then
              echo "✅ PHP-FPM is running"
            else
              echo "❌ PHP-FPM is not running"
            fi

            if [ "$REVERB_STATUS" = "active" ]; then
              echo "✅ Laravel Reverb is running"
            else
              echo "ℹ️  Laravel Reverb is not running (WebSockets disabled)"
            fi

            # ==================== CORS VERIFICATION ====================
            echo ""
            echo "🔍 Verifying CORS configuration..."

            CORS_CHECK=$(curl -s -I https://api.chess99.com/storage/ 2>/dev/null || echo "")

            if echo "$CORS_CHECK" | grep -q "Access-Control-Allow-Origin"; then
              echo "✅ CORS headers are configured correctly"
            else
              echo "ℹ️  CORS headers not detected (this may be normal if endpoint doesn't exist)"
            fi

            # ==================== DEPLOYMENT COMPLETE ====================
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "✅ Deployment completed successfully!"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo ""

            # Ensure script always exits with success (unless critical failure occurred)
            exit 0
