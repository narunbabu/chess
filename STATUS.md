# Chess99 (Chess-Web) — Status

**Live**: chess99.com (Laravel 12 + React, VPS 69.62.73.225). Priority P0.
**Branch**: `chore/unfreeze-working-tree-2026-09-02` pushed to origin and fast-forward merged into `master` (owner decision). Production deployed at `568e46c` 2026-09-13.

## Done recently
- 2026-09-17 10:24 (this session): **The delivered-but-uncommitted work is committed in five gated batches, every head `[skip ci]`, nothing pushed.** `2ea0d5f` backend (championship drops/elimination + migrations, publicShow 404 fix + `player_color`, bot-resume clocks, compact lifeline field, 4 new test classes), `34454a7` web (resume poll route, best-move budget, Withdrawn badge, jest e2e exclusion), `eba4933` Android (P0 viewer/Learn/Daily fixes, string extraction, SecurePreferences migration, 12 new test classes), `8fda0b4` CI/scripts (post-deploy health-check wiring, deploy runbook wrappers), then docs. The P0 batch could not be split out alone (its files carry the string-extraction rewrite and its helpers' new signatures), so batches were cut by area — each commit state equals a gated state. Gates re-run before committing: backend full suite **409 / 0 failed** (first pass over the `player_color` change), web Jest **15 suites / 128 passed**, Android offline `testDebugUnitTest --rerun` **319 / 0**, exit 0. Left untracked as junk: `build-check.log`, `merge_check.sh`, `scripts/__pycache__/`, `logs/`. Evidence: docs/updates/2026_09_17_10_24_update.md.
- 2026-09-17 09:35: **Public game viewer: board orientation, details, share and Play CTA.** `publicShow` now returns `player_color` (stored perspective → only human seat → white); Android viewer orients from it with a Flip action, adds a date/moves/end-reason/opening/time-control card, a Share action (`chess99.com/games/{id}/replay`) and a guest-safe Start-playing CTA. `PublicGameViewerTest` 8 passed; Android offline `testDebugUnitTest lintDebug` BUILD SUCCESSFUL, 319 / 0 failures. Not device-checked, uncommitted. Evidence: docs/updates/2026_09_17_09_35_update.md.
- 2026-09-17 08:45: **P0 Learn / Daily Challenges / Public Viewer gaps re-verified — already landed 09-16, no new changes needed.** Fixes confirmed still in the working tree; re-ran `PublicGameViewerTest` (6 passed) and Android `:app:testDebugUnitTest --rerun` offline (315 / 0 failures, P0 test classes freshly executed). Uncommitted, not deployed. Evidence: docs/updates/2026_09_17_08_45_update.md.
- 2026-09-17 02:00 (this session): **Android unit-test gate re-run offline and green — 315 tests / 0 failures / 0 errors / 0 skipped, actually executed this time.** AGP and every dependency resolved from the read-only cache `C:\Users\ab\.gradle\caches`, so `--offline` needed no network. The first invocation returned `:app:testDebugUnitTest UP-TO-DATE` (a cached pass from 09-16 17:30, not an execution), so the gate was re-run with `--rerun`: BUILD SUCCESSFUL exit 0 in 1m 10s, and the 36 XML class reports under `app/build/test-results/testDebugUnitTest/` are freshly written (01:58) with 315/0/0/0. Scope was the unit gate only — no lint, assemble, instrumented or release gate. No source changes, no commit, push or deploy; `--no-daemon` throughout and zero java processes left. Evidence: docs/updates/2026_09_17_02_00_update.md.
- 2026-09-16 17:40: **Release AAB re-verified blocked — and the Firebase file is now *proven* to be the only blocker, not just the first one reported.** `app/google-services.json` is still the placeholder (sha256 `098d7f85…`, mtime 2026-02-24); `:app:verifyReleaseConfiguration` fails with that one issue and no signing issue, so the four signing checks and the `D:` keystore path from 00:06 all still pass. `:app:bundleRelease` was actually run and stopped at the gate before any compile (`preBuild UP-TO-DATE` → gate FAILED, exit 1) — no AAB produced, `app/build/outputs/bundle/` still absent. Because a failing gate naming one issue does not prove the later checks would pass, a reverse mutation check was run: with a production-*shaped* `google-services.json` in place and nothing else changed, the gate is **BUILD SUCCESSFUL exit 0** — so the real Firebase file is sufficient as well as necessary. The placeholder was restored byte-identical (same sha256) and the synthetic file removed; it is gitignored and was never committed. No source changes, no commit, push or upload; all Gradle runs `--no-daemon --offline`, zero java processes left. Evidence: docs/updates/2026_09_16_17_40_update.md.
- 2026-09-16 (this session): **The parity audit's three P0 Android correctness fixes are landed, tested and gated — and a fourth, larger bug was found while testing them.** `GameController::publicShow` gated on `$game->status !== 'completed' && !== 'ended'`, but `status` reads the `game_statuses` lookup whose only finished code is `finished` (`completed` is a write-side alias, `ended` is not a code at all), so **the public replay endpoint 404'd for every game** — web `GameReplayPage.js` as much as Android; now compared with `GameStatusEnum::FINISHED`. The three P0 items themselves were already implemented but ungated and unrecorded in the working tree; this session verified them against the backend contracts and fixed three daily-solver defects: the solver auto-played entry 1 of a daily solution as the opponent's reply (the line is all the player's moves, per `DailyChallengePage.js:126`) and dropped any multi-move challenge outright; the submitted SAN was generated on the post-move board, so `Move.san()` could prepend a bogus disambiguator the server rejects; and a failed bundled-asset read took the daily challenge down with it. 22 new tests: `PublicGameViewerTest` (6, backend — its first run reproduced the 404), `DailyChallengeSolveTest` (7), `TutorialLessonCompletionTest` (4), `PublicGameViewerLoadTest` (3), `DailyChallengesViewModelTest` (3). Gates: Android `testDebugUnitTest` 315 / 0 failures (was 298) plus `lintDebug` 0 errors and `assembleDebug` BUILD SUCCESSFUL; backend `php artisan test` 407 passed / 158 skipped / 1 risky / 0 failed; frontend Jest 15 suites / 128 passed. `pnpm test:e2e` not run (needs a local stack); no device or emulator, so none of it is device-accepted. Uncommitted; not pushed, not deployed. Evidence: docs/updates/2026_09_16_17_30_update.md.
- 2026-09-16 (this session): **Owner task "run the new lookup migration on production MySQL during deployment" — verified blocked, nothing run on production.** The lookup migration is `2026_09_15_010000_add_elimination_round_type` (inserts the `elimination` row into `championship_round_types`; sibling `2026_09_15_000000` adds the dropped columns), and on the VPS it is applied by step 3 `php artisan migrate --force` of the owner runbook (`scripts/vps-deploy-steps2-7.sh`, whose header says "agents must not run it"). All verified read-only: both migration files are untracked (not even committed), origin/master and VPS HEAD are still `568e46c` (local 19 ahead), the VPS has no `2026_09_15_*` migration files and no `migrate:status` rows for them — production cannot run them until the owner commits → pushes → deploys, at which point `migrate --force` applies both automatically. No production write attempted (no-push/no-deploy rules). Re-verified on this tree instead: both migrations `Ran` in local `migrate:status` (batches 27/28) and `EliminationBracketServiceTest` + `ChampionshipParticipantDropTest` 17 passed / 57 assertions.
- 2026-09-16 (this session): **Re-verified the `ChampionshipRoundType::ELIMINATION` fix and made the drop regression test exercise the service directly.** Focused backend coverage is 16 passed / 53 assertions; full `php artisan test` is 400 passed / 158 skipped / 1 risky / 0 failed (4638 assertions). Uncommitted; no push or deploy.
- 2026-09-16 (this session): **Closed the nine Android unit-test failures left by UI-copy extraction.** The three affected fixtures now stub the extracted resource IDs for computer engine/share copy, multiplayer resume, and takeback messages, and the engine test asserts resolved text. Offline `:app:testDebugUnitTest` is green: 298 tests / 0 failures / 0 errors / 0 skipped; `:app:lintDebug` and `:app:assembleDebug` also passed. Uncommitted. Evidence: docs/updates/2026_09_16_09_43_update.md.
- 2026-09-16 (this session): **Multiplayer Best-move budget accounting implemented on both clients** (the remaining Module 4 parity follow-up, moved from `Next / TODO`). The casual/learning multiplayer Best toggle now draws from the same `undoChancesRemaining`/`bestMoveBudget` pool as takebacks, with the invariant `budget = server remaining (undo_<colour>_remaining, fallback 9) − my moves carrying a persisted `best-move` marker` (the lifeline-summary marker set), so budget and lifeline summary survive reload and takeback resync. New web `src/utils/multiplayerBestBudget.js` (+12 Jest tests) wired into `PlayMultiplayer.js` (load + takeback-resync paths); Android `bestMoveSpend()` + a charged `setCctHintLevel` in `PlayMultiplayerViewModel.kt` (+9 JVM tests in new `PlayMultiplayerBestBudgetTest`), Best control gated on the pool. Fixed during verification: the opponent-move desync fallback left a paid reveal showing after the position was resynced from the server FEN. Gates: web Jest 15 suites / 128 passed, eslint 0 errors on touched files; Android offline `:app:testDebugUnitTest` 298 completed / 9 failed — exactly the 9 documented pre-existing string-fixture failures (own TODO item) — plus `lintDebug assembleDebug` BUILD SUCCESSFUL. Unit-level only; no device test of the toggle; uncommitted. Evidence: docs/updates/2026_09_16_08_17_update.md.
- 2026-09-16 (this session): **Status reconciliation only — no source files changed.** Compared `Next / TODO` with the working tree, git log and docs; moved the finished items below into this section, reworded two stale ones and added three proposals.
- 2026-09-16 (this session): **Ran the offline Android unit gate on the working tree — 289 tests completed, 9 failed** (`:app:testDebugUnitTest`, `--no-daemon --offline`, project-local Gradle home, read-only dep cache, in-process Kotlin; single-use daemon exited). All 9 are copy mismatches from the 03:40 string-extraction rewrite resolving `context.getString` on relaxed mocks — `PlayComputerViewModelTest` ×4, `PlayMultiplayerResumeRequestTest` ×3, `PlayMultiplayerTakebackTest` ×2 (e.g. expected `Resume request sent`, got `""`). `compileDebugKotlin` was up-to-date and the new `SecurePreferences`/`AesGcmValueCodec`/lifeline test classes compiled and passed, so the failures are unstubbed resource resolution in test fixtures, not compile errors. This is the first recorded gate result on this tree: `docs/updates/2026_09_16_03_40_update.md` points at a STATUS "Gates" entry that was never written.
- 2026-09-16: **`TokenManager` migrated off the deprecated `androidx.security:security-crypto` (moved from `Next / TODO`).** The working tree replaces `EncryptedSharedPreferences` with an own `data/local/crypto/SecurePreferences` (private prefs file, AES-256-GCM values under an Android Keystore key) plus a one-time `LegacyEncryptedPreferences.drain()` import of the old store, which is now that library's only remaining caller. Uncommitted. Its new unit tests (`SecurePreferencesTest`, `AesGcmValueCodecTest`) pass inside the 2026-09-16 offline run above; the 9 failures there are unrelated copy fixtures. Migration of a real installed app remains unverified (needs a device).
- 2026-09-16: **`check_backend_health.ps1` post-deploy wiring (moved from `Next / TODO`; was pre-ticked).** Verified in the working tree, uncommitted: `.github/workflows/deploy.yml` gains a checkout step and a `Post-deploy health check` pwsh step running `chess99-android/scripts/check_backend_health.ps1`, both gated on `!inputs.check_only`; `scripts/deploy-notes.md` documents the runbook next to the new `scripts/vps-deploy-step1.sh`/`vps-deploy-steps2-7.sh` wrappers and `scripts/test-post-deploy-health.py`. Not exercised against a real deploy (deploys remain owner-only).
- 2026-09-16: **Swept the finished engineering items out of `Next / TODO` (each already recorded below with full evidence):** resume-refusal UI commits `f76fb18` / `0a2d50f` / `4fe6442` with their Playwright + emulator live checks; the 01:24 `CheckExpiredMatchesJob` fix `d3f8de8`; the dropped-participant schema/forfeit fix and its follow-ups; the `ELIMINATION` round-type fix; Android per-game-type Leave; the 70 s inactivity auto-pause and the Carbon 3 `diffIn*` audit; casual-bot auto-resume on open; takeback for everyone `0dda391`; the live Review toggle + per-move lifeline markers and the 15-module parity audit; the tier-1 backend deployment to production; the CLAUDE.md `/up` health-endpoint fix; the ambassador adult gate; the dependency-lint decision; and the in-game board polish.
- 2026-09-16 00:06: **Release AAB still blocked — confirmed by running the gate, and two further release blockers found (one fixed).** `:app:verifyReleaseConfiguration` was actually run (offline recipe; `WS_KEY_RELEASE` passed via env since `--gradle-user-home` skips the user-level properties) and fails on exactly one issue: the placeholder `app/google-services.json` — the owner-credential item above it. `GOOGLE_SERVER_CLIENT_ID` and `WS_KEY_RELEASE` pass. Separately, `keystore.properties` pointed `storeFile` at `C:/ArunApps/_keystores/` (stale since the move to `D:`), so signing would have failed on upload day; the key at `D:/ArunApps/_keystores/chess99-upload.jks` was verified read-only by `keytool` (SHA-1 `39:87:…:1B:C7`, matching the recorded upload fingerprint) and only that path line was corrected — gitignored, no secrets in git. The gate itself checked nothing about signing, so four signing checks were added to `app/build.gradle.kts` and mutation-checked. The stale 2026-07-17 bundle no longer exists (`app/build/outputs/bundle/` is absent), so there is nothing left to upload by mistake. **No AAB built; no commit, push or upload.** Evidence: docs/updates/2026_09_16_00_06_update.md.
- 2026-09-15 (current session): **Completed the feature-level audit of the 15 previously unaudited Android/web modules.** Added a source-evidence matrix and prioritized correctness/parity follow-ups to `docs/android-web-parity-gap-analysis.md`; browser/device acceptance remains separate. Uncommitted.
- 2026-09-15 (current session): **Closed Android/web Review and lifeline parity gaps.** Casual/learning multiplayer now has a live Android Review toggle with asynchronous top-five alternatives; move payloads and compact history preserve `review`/`best-move` markers; Android Game Review renders per-move badges and a lifeline summary. Frontend Jest 14 suites / 116 passed, `pnpm build` compiled, backend 400 passed / 158 skipped / 1 risky; Android verification remains blocked before compile by the offline-missing AGP 8.9.1 plugin marker. Uncommitted. Evidence: docs/updates/2026_09_15_15_30_update.md.
- 2026-09-15 14:08: **Casual Android bot games now resume automatically when reopened.** Android sends pause clocks with navigation pauses and requests resume during the open flow for casual computer games; the backend auto-accepts resume requests for bot/synthetic games, restores legacy null clocks from the time control, and keeps human-vs-human requests unchanged. `SyntheticResumeTest`: 3 passed / 26 assertions; full backend suite: 400 passed / 158 skipped / 1 risky. Android test execution was attempted but is blocked by the offline environment missing the cached AGP 8.9.1 plugin marker. Uncommitted. Evidence: docs/updates/2026_09_15_14_08_update.md.
- 2026-09-15 13:37: **Elimination brackets can now be generated.** Added `ChampionshipRoundType::ELIMINATION` plus the existing-database lookup migration, and corrected `EliminationBracketService` reads to use persisted `round_type_id` (first-round generation, follow-up pairing, bracket reads, winner/loss checks, validation). New feature tests cover first-round pairings, persistence, and completed-winner progression; full backend gate: 397 passed / 158 skipped / 1 risky / 0 failed. Local migration applied; production MySQL not migrated. Uncommitted. Evidence: docs/updates/2026_09_15_13_37_update.md.
- 2026-09-15 13:23: **Dropped-player follow-ups closed end to end.** The drop's forfeits now record the dropped player's loss too (`processPlayerForfeit`, plus a lone loss for a bye row) — a Swiss recalc was masking the gap, so the new tests use `elimination_only`. Five paid-participant counts (`GenerateNextRoundJob::hasEnoughParticipants`/`hasEliminationWinner`, `MatchSchedulerService::hasEliminationWinner`/`scheduleMatchesWithTimeWindows`, `AutoGenerateRoundsCommand::hasEliminationWinner`) now chain `->notDropped()`; `ChampionshipRoundProgressionService`'s match-derived variant deliberately untouched. The drop is surfaced: `/participants` adds `active_participants`/`dropped_participants`, web shows a Withdrawn badge, Still In/Withdrawn counters, two filters and an own-drop notice, Android strikes the name through with `Withdrawn — <reason>`. Gates: backend 394 passed / 0 failed, Jest 14 suites / 116 passed, eslint 0 errors, `pnpm build` compiled, Android 270 tests / 0 failures + `lintDebug` OK. Not browser- or device-checked (needs a live forfeit run); 06:35 migration still unrun on production MySQL. Uncommitted. Evidence: docs/updates/2026_09_15_13_23_update.md.
- 2026-09-15 12:58: **Web: the paused-game fallback poll no longer 404s.** In `PlayMultiplayer.js` it called `api/games/{id}/resume-status`, which has no route. It now calls the registered `api/websocket/games/{id}/resume-status`, whose response has the fields the poll reads. The effect also now clears its 2-minute cleanup timeout on re-run. `route:list` confirms the route. eslint 0 errors, Jest 13 suites / 110 passed, `pnpm build` compiled. Not browser-checked (the poll only runs while a paused game's WebSocket is down). Uncommitted. Evidence: docs/updates/2026_09_15_12_58_update.md.
- 2026-09-15 12:50: **Live-checked the 09:59 fresh-load sent panel on web (Playwright, local stack) — pass — and committed it** as `4fe6442` `fix(client): show the sent resume panel on a fresh load with our own request pending [skip ci]` (`PlayMultiplayer.js`, `resumeRequestRefusal.js` + test, 09:59 + 12:50 docs). With A's request pending (58 s TTL), a lobby-style open showed "Resume request sent from lobby!" + "Waiting for opponent to accept... 32s" (server 32.7 s) → 29s after 3 s, with Cancel, no "Auto-resume may have failed", 0 POSTs. At expiry it fell back to Manual Resume Request. Gates: Jest 3 suites / 32 passed, eslint 0 errors, `pnpm build` compiled. Not pushed or deployed. Evidence: docs/updates/2026_09_15_12_50_update.md.
- 2026-09-15 10:40: **A whole-project `pnpm test` now runs only the unit suites.** New `chess-frontend/jest.config.js` rebuilds the CRA Jest config and sets `testPathIgnorePatterns` to skip `tests/e2e`. It is needed because `pnpm test -- <args>` makes Jest drop CRA's `--config` and collect the Playwright specs (`TransformStream is not defined`). CRA won't accept that key in package.json. `pnpm test --watchAll=false` and `pnpm test -- --watchAll=false` both give 13 suites / 110 passed; with the file moved aside the `--` form exits 1 on every e2e spec. Uncommitted. Evidence: docs/updates/2026_09_15_10_40_update.md.
- 2026-09-15 09:59 (system clock): **Web: a fresh lobby-style load with your own resume request still pending now shows the sent panel with the server countdown** instead of "Auto-resume may have failed". The auto-send pre-check's `type:'sent'` branch uses the new `sentResumeStatusSeconds` helper (`resumeRequestRefusal.js`) to set the sent state and `startResumeCountdown`. Jest 3 suites / 32 passed, eslint 0 errors (77 pre-existing warnings), `pnpm build` compiled. Not live-checked in a browser, uncommitted. Evidence: docs/updates/2026_09_15_09_59_update.md.
- 2026-09-15 10:30: **Committed the web `sendResumeRequestRef` stale-closure fix** as `0a2d50f` `fix(client): send resume requests through a ref to the latest sendResumeRequest [skip ci]` on local `master` (2 files: `PlayMultiplayer.js` + docs/updates/2026_09_15_09_50_update.md). Re-ran gates first: Jest `resumeRequestRefusal` + `WebSocketGameService.contract` + `PlayMultiplayerHooks` 3 suites / 30 passed, eslint 0 errors (77 pre-existing warnings), `pnpm build` compiled. A whole-project `pnpm test` fails all 36 suites before running any test (it also collects the Playwright `tests/e2e` specs, which throw `TransformStream is not defined` under Jest); the targeted suites pass. Not pushed, not deployed.
- 2026-09-15 09:50: **Live-checked the 01:58 resume refusal UI on web (Playwright) and the Android emulator against a local stack — both pass; web needed one fix.** On web, `handleRequestResume` was memoized with the first-render `sendResumeRequest` (`user` null), so after any page load, resume clicks and the lobby auto-send never POSTed ("Missing user or game context", a bug from 2025-11). Fixed with a `sendResumeRequestRef` (uncommitted). After the fix, a retry after a fresh load with A's own request pending got 200 `is_same_user`, `expires_in_seconds:18` → "Resume request sent from lobby!" + "Waiting for opponent to accept... 18s → 15s" + Cancel. Android (local-target debug build, prod-target APK restored afterwards): a retry about 19 s after another client's request showed the disabled "Waiting for opponent… 7s" and the pending snackbar, and the button re-enabled at expiry. Jest 3 suites / 30 passed, eslint 0 errors, `pnpm build` compiled. No physical phone. Evidence: docs/updates/2026_09_15_09_50_update.md.
- 2026-09-15 08:37: **Ran the missing gates for the 01:58 resume-refusal UI and committed the web + Android client work** as `f76fb18` `fix(client): resume-request refusal UI and leave-pause parity [skip ci]` on local `master` (10 files: web `resumeRequestRefusal.js` + its test + `PlayMultiplayer.js`; Android `GameNavigationWarningDialog.kt`/`PlayMultiplayerScreen.kt`/`PlayMultiplayerViewModel.kt` + `PlayMultiplayerResumeRequestTest`/`PlayMultiplayerLeaveTest`; the 23:15 and 01:58 update notes). Web: `pnpm build` compiled successfully, `resumeRequestRefusal` 5/5 Jest, eslint 0 errors (77 pre-existing warnings). Android: `testDebugUnitTest lintDebug assembleDebug` (`--no-daemon`, in-process Kotlin, offline) BUILD SUCCESSFUL — 265 tests / 0 failures / 0 skipped, lint 0 errors / 0 warnings / 69 informational. The gates ran on the working tree (a superset of the committed content); the committed files are byte-identical to the tested ones. The 01:18 backend change was already in `45adbb1`; the 06:35 drop work and the `HomeViewModel` bot-name fix stay uncommitted, and the Android leave behaviour is unit-verified but not device-checked. Not pushed, not deployed.
- 2026-09-15 07:31: **Committed the `GameController::activeGames` ordering change with `ActiveGamesListTest`** as `ab40417` `fix(games): fall back to created_at in active games ordering [skip ci]` on local `master` (2 files). `orderByRaw('COALESCE(last_move_at, created_at) DESC')` with the `syntheticPlayer` eager-load, so a just-started no-moves game (NULL `last_move_at`) no longer sorts below days-old games. Ran `php artisan test --filter=ActiveGamesListTest` on the working tree: 1 passed (8 assertions). Not pushed, not deployed.
- 2026-09-15 07:21: **Committed the 01:24 `CheckExpiredMatchesJob` fix with its test** as `d3f8de8` `fix(championships): read expired-match activity from games.moves and count forfeits by result_type_id [skip ci]` on local `master` (3 files: `CheckExpiredMatchesJob.php` at its 01:24 state — `moves` JSON array + `result_type_id` — `tests/Feature/CheckExpiredMatchesJobTest.php` 6 tests, and the update note). Staged via a partial patch so the uncommitted 06:35 dropped/result-side rewrite of the same job and its 4 extra tests stay out of the commit. Verified the exact committed tree in a throwaway worktree (vendor junctioned): `php -l` clean, `--filter=CheckExpiredMatchesJobTest` 6 passed (21 assertions), full suite 374 passed / 158 skipped / 1 risky / 0 failures. Working tree files restored byte-identical (sha256). Not pushed, not deployed. Evidence: docs/updates/2026_09_15_01_24_update.md.
- 2026-09-15 06:35: **Decided the `championship_participants.dropped` schema and fixed the drop loop's `FORFEIT_PLAYER1`-for-everyone result type** (TODO item). Schema: nullable `dropped_at` + `dropped_reason` (+ index `championship_id, dropped_at`) rather than a boolean or a new `registration_status` value — the stamp is auditable, and dropping must stay orthogonal to the payment/registration state machine whose `cancelled`/`refunded` values free `active_unique_key` and would let a dropped player re-register. `ChampionshipParticipant` keeps `dropped` as a boolean alias (mutator/accessor, like `is_paid`), plus `notDropped()`/`dropped()` scopes and `markAsDropped('forfeit_limit')`; Swiss and elimination eligibility (including hybrid Swiss qualifiers, read from standings) now skip dropped players. The remaining-match forfeits record the side the dropped player actually occupies — with the old code `StandingsCalculatorService` handed the win to the dropped player and a loss to their opponent whenever they were player2 — and a bye row completes with no winner. 13 new tests (`ChampionshipParticipantDropTest` 9, `CheckExpiredMatchesJobTest` +4); full suite 388 passed / 158 skipped / 1 risky / 0 failures; three mutation checks fail as expected, files restored byte-identical. Not MySQL-verified (local dev is SQLite); `php artisan migrate --force` in this session applied the two pending migrations to the local dev `database/database.sqlite`. Not committed. Evidence: docs/updates/2026_09_15_06_35_update.md.
- 2026-09-15 01:58: **Checked the web and Android resume UI against the 01:18 same-user refusal (10–30 s): both were wrong, both fixed.** Web: the WS refusal ("still pending") missed the "already pending" branch, so the button stuck on "Requesting…" with no countdown, and the REST 409 showed a fake 10 s. It now shows the sent panel with the server's countdown (new `utils/resumeRequestRefusal.js`), and real failures re-enable the button. Android: a 200 `success:false` said "Resume request sent"; it now reads `success`, waits on `expires_in_seconds` with the button disabled ("Waiting for opponent… Ns"), and uses separate copy for opponent-pending and other refusals. Web Jest 3 suites / 30 passed (5 new), eslint 0 errors; Android `presentation.game.*` unit tests BUILD SUCCESSFUL, new `PlayMultiplayerResumeRequestTest` 4/4. Not browser/device-checked, not committed. Evidence: docs/updates/2026_09_15_01_58_update.md.
- 2026-09-15 01:40: **Committed the 00:48 MonitorGameInactivity work and the 01:18 Carbon 3 diff fixes** as `45adbb1` `fix(backend): inactivity auto-pause and Carbon 3 signed-diff inversions [skip ci]` on local `master` (15 files: 10 backend sources, 3 tests, 2 update notes). `CheckExpiredMatchesJob.php` was staged at its 01:18 state (only the diff flip); the 01:24 `moves()`/`result_type_id` rewrite, `CheckExpiredMatchesJobTest`, and the other session's `GameController` activeGames ordering + `ActiveGamesListTest` stay uncommitted. Verified the exact committed tree by swapping those four files out: `php -l` clean, `php artisan test` 368 passed / 158 skipped / 1 risky / 0 failures (375 minus the 7 excluded tests); files restored byte-identical (sha256). Not pushed, not deployed.
- 2026-09-15 01:24: **Fixed `CheckExpiredMatchesJob` calling nonexistent `Game::moves()`** (TODO item). The last move now comes from the `games.moves` JSON array (mover = entry `user_id`, or the side not on `turn` for legacy entries) and its time from `last_move_at`. Also fixed `getPlayerForfeitCount` filtering a nonexistent `result_type` column (now `result_type_id`): on MySQL it would have rolled back every forfeit even after the `moves()` fix, and SQLite silently counted 0 so nobody was ever dropped. New `CheckExpiredMatchesJobTest` (6; each fix's tests fail with the old code restored); full suite 375 passed / 158 skipped / 1 risky / 0 failures. Not committed, not MySQL/VPS-verified. Evidence: docs/updates/2026_09_15_01_24_update.md.
- 2026-09-15 01:18: **Audited all backend `diffIn*()` sites for the Carbon 3 signed-diff inversion.** Three were really inverted: (1) `GameRoomService::requestResume` stale cleanup — the 20-min and 1-min branches never fired, and "same user within 10 s" was true at any age; now measured `resume_requested_at -> now()`, so a same-user retry between 10 and 30 s is blocked with a countdown instead of silently replacing the request. (2) `CheckExpiredMatchesJob` forfeit comparison was flipped, and that path is still unreachable: `$game->moves()` doesn't exist. (3) Championship `needs_warning` was true for every upcoming match. Also `(int)`-cast Carbon 3 float diffs that reached `int` params, JSON countdowns and log-rotation day counts. New `ResumeRequestStaleCleanupTest` (4) plus one `needs_warning` test; both key tests fail with the old expressions restored. Full suite 369 passed / 158 skipped / 1 risky / 0 failures. Not committed. Evidence: docs/updates/2026_09_15_01_18_update.md.
- 2026-09-15 00:48: **Fixed the `games:monitor-inactivity` 70 s auto-pause** (TODO item). Decision: auto-pause IS wanted (the 18:31 owner-approved lifecycle: left-active game → ~70 s pause → 1 h abort). `pauseGame()` now accepts a null pauser (system pause; `GamePausedEvent` already took `?User`), the command pauses with the server's last known clocks, and bot games (`computer_player_id`/`synthetic_player_id` set) are excluded from both branches — no bot answers a resume request and the forfeit path crashed on their null player side every minute; they now deterministically wait for `games:cleanup-abandoned` to abort at 1 h with `*`, never a loss. Found and fixed a second latent bug: Carbon 3 signed diffs made `Game::getInactiveSeconds()`/`getPausedSeconds()` return negatives, so even without the TypeError the thresholds could never fire. New `MonitorGameInactivityTest` (7 tests) covers pause/exclusions/forfeit; full suite 364 passed / 158 skipped / 1 risky / 0 failures. Not committed, not device/VPS-verified. Evidence: docs/updates/2026_09_15_00_48_update.md.
- 2026-09-14 23:21: **Verified the 23:15 casual-Leave delivery end-to-end** (picked up the prior session's finished-but-unreported tree; no source changes needed). Confirmed in-tree: `leaveGame` fire-and-forgets `gameApi.pauseNavigation` for both casual arms and the dialog's casual rows use the pause/resume copy ("counts as a loss" remains only for rated). Re-ran the full gate with the same flags (`--no-daemon`, in-process Kotlin): BUILD SUCCESSFUL — 261 tests / 0 failures / 0 errors / 0 skipped (`PlayMultiplayerLeaveTest` 6/6), lint 0 errors / 0 warnings / 69 informational, assembleDebug OK (`app-debug.apk` 29.6 MB). Two pre-existing VS Code java processes left alone; no Gradle daemon left. Not device-checked, not committed.
- 2026-09-14 23:15: **Android casual Leave now fire-and-forgets `pause-navigation`** (owner task, supersedes the 19:50 casual-bot "send nothing" decision). Any casual leave — human or bot — launches `GameApi.pauseNavigation` on a separate `viewModelScope` coroutine and navigates immediately without waiting (rated still waits ≤5 s for the resign); both casual dialog rows now read "Your game will be paused. You can resume it from Home within an hour." `PlayMultiplayerLeaveTest` updated to 6 tests incl. a slow-pause-doesn't-delay-Leave case; `testDebugUnitTest lintDebug assembleDebug` (in-process, no-daemon) exit 0: 261 tests / 0 failures, lint 0 errors/0 warnings/69 informational. Known gap accepted with the task: reopening a paused bot game shows "Request Resume", which no bot answers. Not device-checked, not committed. Evidence: docs/updates/2026_09_14_23_15_update.md.
- 2026-09-14 19:50: **Android Leave game fixed per game type.** Rated resigns, which records the loss. Casual vs a human pauses. Casual vs a bot sends nothing and stays open, so Home "Continue playing" resumes it; a paused bot game would be stuck, since no bot answers `requestResume`. The dialog copy now matches each case. Also found: `MonitorGameInactivity` hits a TypeError passing `'inactivity'` as `pausedByUserId`, so inactivity auto-pause never fires (reproduced on PHP 8.2; not changed). Android `testDebugUnitTest lintDebug assembleDebug` exit 0: 260 tests / 0 failures, lint 0 errors/0 warnings. `ActiveGamesListTest` passes. Not device-checked, not committed. Evidence: docs/updates/2026_09_14_19_50_update.md. (Casual-bot behavior since changed by the 23:15 entry.)
- 2026-09-14 19:30: **Device-checked and committed the `games/unfinished` fix** as `4d7d6ed` `fix(android): read games/unfinished as a bare JSON array` on local `master`. Only the six 18:07 files are in it; another session's leave-game, activeGames and bot-name edits stay uncommitted. Built in a detached worktree holding exactly the commit content: `testDebugUnitTest lintDebug assembleDebug` exit 0, 254 tests / 0 failures, lint 0 errors/0 warnings. Emulator against the local backend: Home shows no resume banner and lists a navigation-paused game (466, created then discarded) with Resume/Discard; `games/unfinished` 200 as a bare array, 0 `JsonSyntaxException`. Not pushed. Evidence: docs/updates/2026_09_14_19_30_update.md.
- 2026-09-14 19:20: **Committed** STATUS.md and docs/updates/2026_09_14_11_57_status_archive.md on local `master` under `docs(status): record 18:07-19:10 work and add 11:57 archive [skip ci]`; only those two staged. Docs-only, so no build or tests; checked for secrets first (none). Not pushed. The 18:59 note/screenshots, deploy/workflow edits, the 18:07 Android fix and other untracked notes stay uncommitted.
- 2026-09-14 19:10: **Committed** docs/updates/2026_09_14_17_55_update.md and its 8 `devicecheck_signedin_*` screenshots on local `master` as `d533f7d` `docs(updates): add 17:55 Android device check note and screenshots [skip ci]`; only those 9 files were staged. The login screenshot shows the password masked. Docs-only, so no build or tests. Not pushed. The 18:59 note and its `devicecheck_persona_*` screenshots, this file, deploy/workflow edits and the 18:07 Android fix are still uncommitted.
- 2026-09-14 18:59: **Device-checked the persona-handoff leave: root case unreachable, reachable path PASS.** A root PlayComputer exists only for guests (Onboarding -> Try as guest). `synthetic-players` and `games/computer` are `auth:sanctum` (401 without a token), so guests get no persona row and no handoff. Signed in: Home -> Play computer -> Riya (casual) -> PlayMultiplayer 465 -> Leave -> Home; system Back -> launcher. The deep-link root case already passed at 17:55. Signed-in Home also showed no resume-error banner with the 18:07 build. Still true: casual Leave says "counts as a loss" and sends nothing. The process sweep also stopped an emulator 175628 of unknown owner. No code changes. Evidence: docs/updates/2026_09_14_18_59_update.md.
- 2026-09-14 18:31: **Decided: Leave game must NOT end a casual bot game server-side.** Every layer treats casual leave as pause → resumable → auto-abort with `*` (web bot games call `pause-navigation` on leave; rated leave is the only forfeit path; `games:cleanup-abandoned` aborts 7-day-active/1h-paused games with no result). The Android defects are the "counts as a loss" copy shown for casual games and Leave sending no server call (`GameApi.pauseNavigation` declared but unused); rated Leave also sends nothing. Follow-ups recorded, not implemented. Docs-only, no tests needed. Evidence: docs/updates/2026_09_14_18_31_update.md.
- 2026-09-14 18:07: **Fixed Android Home's "Couldn't check for games to resume" banner.** It was not a timeout. `games/unfinished` returns a bare JSON array, but `GameApi` typed it `Response<JsonObject>`, so Gson conversion threw on every signed-in Home load. Dashboard's unfinished list was silently empty for the same reason. Now `JsonElement` plus a shared `arrOrField` helper; the tests use the real shape, with a regression test and a converter test. Offline `testDebugUnitTest lintDebug assembleDebug` exit 0: 254 tests, 0 failures; lint 0 errors/0 warnings. Not device re-checked, not committed. Evidence: docs/updates/2026_09_14_18_07_update.md.
- 2026-09-14 18:20: **Committed** scripts/rotate-ci-deploy-key.sh, chess-frontend/scripts/smoke-takeback-live.cjs and docs/updates/2026_09_14_11_57_update.md on local `master` as `30e17b1` `chore(scripts): retain deploy-key and takeback tools [skip ci]`; only those three staged. Pre-commit `bash -n` and `node --check` exit 0 (syntax only), no secrets in either script. Not pushed; this file, the 11:57 archive and other edits stay uncommitted.
- 2026-09-14 18:05: **Committed** STATUS.md and docs/updates/2026_09_14_17_24_update.md on local `master` under `docs(status): record Android commits and device check [skip ci]`; the 17:18 note was already in `2896ed1`. Docs-only, no build or tests needed. Not pushed; the 17:55 note/screenshots, deploy/workflow edits and logs stay uncommitted.
- 2026-09-14 17:55: **Device-checked both open Android items: PASS** on a throwaway project-local emulator against the local backend, using the committed debug APK. Signed-out `chess99://game/463` -> login opens PlayMultiplayer as root; Leave game -> signed-in Home; system Back -> launcher, game not shown. Casual game shows Best (on: 3 arrows, Undo unchanged; off: arrows cleared); rated game 464 shows only Draw/Resign. No code changes; persona-handoff root path and production not covered. Everything started was stopped and the AVD deleted. Evidence + screenshots: docs/updates/2026_09_14_17_55_update.md.
- 2026-09-14 17:24: **Committed** the eight Android lint-cleanup files and docs/updates/2026_09_14_16_56_update.md on local `master` as `9a134c5` `chore(android): clear lint warnings [skip ci]`; nothing else staged. Pre-commit offline `:app:lint :app:testDebugUnitTest :app:assembleDebug` rerun: exit 0, lint 0 errors/0 warnings, 251 tests 0 failures (60/62 tasks up-to-date against the 16:56 inputs). Not pushed; this file and other edits stay uncommitted. Evidence: docs/updates/2026_09_14_17_24_update.md.
- 2026-09-14 17:18: **Committed** the eight Android leave-routing/Best-toggle source and test files on local `master` as `2896ed1` `fix(android): restore root leave routing and casual Best controls [skip ci]`, together with their evidence notes, the leave screenshots and this file. The 16:25-17:06 attempts had been blocked by read-only Git metadata; this session could write it. Before committing, a clean `HEAD` worktree containing only the eight files ran offline `:app:testDebugUnitTest :app:assembleDebug`: 251 tests, 0 failures, debug APK built, exit 0. The worktree was removed afterwards. The lint-cleanup files, deploy/workflow edits and logs stay uncommitted. Not pushed. Evidence: docs/updates/2026_09_14_17_18_update.md.
- 2026-09-14 17:00: Forced offline Android unit-test rerun passed: 251 tests, 0 failures/errors/skips; same project-local Gradle home and read-only dependency cache. Exit 0; single-use daemon exited. Evidence: docs/updates/2026_09_14_17_00_update.md.
- 2026-09-14 16:56: Cleared all 12 Android lint warnings (10 code fixes, 2 narrowly documented suppressions preserving commit-result and Google credential handling). Offline lint: 0 errors/0 warnings; 251 unit tests pass; debug build and whitespace check pass. Gradle processes exited. Evidence: docs/updates/2026_09_14_16_56_update.md.
- 2026-09-14 16:40: Android `:app:lint` rerun **passed** (BUILD SUCCESSFUL, exit 0): 0 errors, 12 warnings (6 UseKtx, 5 ModifierParameter, 1 CredentialManagerSignInWithGoogle), none in the uncommitted leave-routing/Best-toggle files. Ran offline: copied the complete Gradle 8.11.1 install from `~/.gradle` into the project-local wrapper home and read `~/.gradle/caches` as a read-only dependency cache (`GRADLE_RO_DEP_CACHE`). No network, no global writes, no daemon left. Evidence: docs/updates/2026_09_14_16_40_update.md.
- 2026-09-14 16:28: Android `:app:lint` attempted on the current working tree; blocked before analysis by Gradle 8.11.1 download network denial (exit 1). No lint pass or source changes; no daemon started. Evidence: docs/updates/2026_09_14_16_28_update.md.
- 2026-09-14 16:25: Android leave-routing and Best-toggle commit attempted, blocked by read-only `.git/index.lock`; nothing staged or committed. Android whitespace check passed; unit/build rerun blocked by Gradle distribution network denial. Existing code preserved. Evidence: docs/updates/2026_09_14_16_25_update.md.
- 2026-09-14 16:14: Signed-in PlayMultiplayer root-leave verification attempted, **blocked**: ADB sees no device; existing AVD is outside permitted write scope; Gradle unit rerun failed at distribution download (network denied). Previous fix preserved; exact root deep-link reproduction and remaining acceptance recorded in docs/updates/2026_09_14_16_14_update.md. No new device/test pass; ADB stopped.
- 2026-09-14 15:45: Android guest "Leave game" fixed: when Play Computer (or Play Multiplayer) is the back-stack root, leave now routes via `coldStartDestination` (guest -> Login, first run -> Onboarding, signed-in -> Home) instead of a no-op `popBackStack()`; ordinary stacks still pop. 251 unit tests pass (3 new in MainNavigationTest), debug build OK; emulator guest run confirmed toolbar Back and in-game "Leave game" both land on Login. Signed-in root leave not device-tested. Uncommitted. Note: docs/updates/2026_09_14_15_45_update.md
- 2026-09-14 13:11: Ran the mandatory pre-deploy quality gates for the owner runbook handoff: **first run 2/4 failed, both failures were defects in the gate script itself — fixed both and re-ran: ALL 4 GATES PASSED** (Gate 3's `--no-interaction` is rejected by PHPUnit 11.5.41 — suite itself is green: 356 passed/158 skipped/1 risky/0 failures, exit 0; Gate 2's `--max-warnings=0` contradicted the repo's own warn-level `.eslintrc.json` and `pnpm lint` — 303 pre-existing warnings on master, now counted not blocking, NOT auto-fixed since 86 are hooks-deps changes too risky pre-deploy). E2E skipped via the runbook's own `--skip-e2e` (no local redis/mysql/servers). Handoff verified read-only: `origin/master` `568e46c` (local 6 ahead, empty `[skip ci]` head `690f348`), no `chess-frontend/` changes in the push → `CHESS99_SKIP_FRONTEND=1`, latest deploy.yml run still failed 09-13 `34764068752`, secrets still 2025-10-02, `/up` 200. Owner copy-paste runbook (gates → push → vps-deploy-step1 → steps2-7 → health gate → post-checks) in the evidence. [Evidence](docs/updates/2026_09_14_13_11_update.md).
- 2026-09-14 13:11: Android Best move restored for casual games: Play Computer Best is unlimited in Casual, pool-limited in Learning, refused in Rated; PlayMultiplayer gains a Best toggle for non-rated games with rated guards on every arrow path. 248 unit tests pass; emulator guest run confirmed casual arrows (undos unchanged) and Learning 5->4. Multiplayer toggle not device-tested. Uncommitted. Note: docs/updates/2026_09_14_13_11_update.md
- 2026-09-14 12:39: Owner task "choose automatic deployment behavior and push master": **prepared as a decision brief, not executed — both the choice and the push are owner-only** (no-push/no-deploy/workflow-toggle rules). Re-verified read-only: after fetch `origin/master` `568e46c` (local 6 ahead, empty `[skip ci]` head `690f348` → push as-is fires nothing), remote `deploy.yml` active with bare `workflow_dispatch` (no `check_only` until `daf9405` lands), latest run still failed 09-13 `34764068752`, all three secrets still 2025-10-02 (VPS `authorized_keys` 0 `gha-deploy` entries → any deploy run fails at SSH), VPS `HEAD`/`origin/master` `568e46c`, cache dirty as expected, `/up` 200, uncommitted edits stay out of the push. Three copy-paste options in the brief: A push as-is (recommended), B `--allow-empty` non-skip head to fire a deploy (only after key rotation; never `reset --hard`, it would wipe the working tree), C rotate → push → check-only dispatch. [Decision brief](docs/updates/2026_09_14_12_39_update.md).
- 2026-09-14 12:37: Re-ran both post-deploy checks live from this session (VPS network access confirmed, unlike 12:08): **`/up` 200 (pass); VPS `bootstrap/cache` status still not empty (fail — expected pre-deploy, not a regression)**. The deploy still has not happened: after fetch `origin/master` `568e46c` (local 6 ahead, head `690f348` `[skip ci]`), latest deploy.yml run still the failed 09-13 `34764068752`, VPS `HEAD`/`origin/master` `568e46c`, cache still ` M packages.php`/` M services.php`/`?? routes-v7.php`. Push + deploy remain owner-only. [Evidence](docs/updates/2026_09_14_12_37_update.md).

Earlier entries: [status archive](docs/updates/2026_09_14_16_14_status_archive.md).

## In progress
- Approved experience slice: native compile/unit/lint and bounded independent phone-test review passed. Signed APK ready for owner exploratory testing; no physical phone connected. Physical arm64/TalkBack/authenticated two-client acceptance remains open. Prior history: [review record](docs/updates/2026_09_08_10_45_experience_review.md).
- Release-owner configuration and Play internal-test validation for Android 1.0. Community programme migration review and deployment remain owner actions; the tester/admin workflow is ready for local verification.

## Next / TODO
- [x] Commit the delivered-but-uncommitted work in reviewed, gated batches — **done 2026-09-17 10:24** as `2ea0d5f` (backend) / `34454a7` (web) / `eba4933` (Android) / `8fda0b4` (CI wiring) / docs, each with a `[skip ci]` head; gates re-run first (backend 409/0, Jest 15/128, Android 319/0); no push. Evidence: docs/updates/2026_09_17_10_24_update.md.
- [ ] Browser/device-check the dropped-player UI: force a drop in the local stack (two forfeits for one player in a test championship), then confirm the web Withdrawn badge + Still In/Withdrawn counters + own-drop notice and the Android participants row
- [ ] Device-check Android Leave on the three game types on an emulator (rated bot → game finished as a loss; casual human and casual bot → paused in Home with Resume) — the leave-game source/test files themselves were committed as `f76fb18` (2026-09-15), so only this acceptance remains
- [ ] **[needs owner: push, then ServerMigrationAgent at deploy]** The `bootstrap/cache` untracking is committed locally as `d48a4b4` and not pushed (2026-09-14, `docs/updates/2026_09_14_01_45_update.md`). **Pushing it starts `deploy.yml`**: the head commit has no `[skip ci]`. Disable the workflow, or put `[skip ci]` in the **head** commit, before pushing if no deploy is wanted. Amending `d48a4b4` alone does not skip it (`docs/updates/2026_09_14_09_45_update.md`) (`docs/updates/2026_09_14_09_29_update.md`). On the first manual deploy that includes it, run `git checkout -- chess-backend/bootstrap/cache/packages.php chess-backend/bootstrap/cache/services.php` on the VPS, then `git pull origin master || (cd chess-backend && rm -f bootstrap/cache/packages.php bootstrap/cache/services.php && php artisan package:discover)` (`scripts/deploy-notes.md` step 1). Keep the `||` branch, including the `rm`: without it, a failed pull leaves a manifest listing dev-only providers, prod errors, and `package:discover` alone fails too (reproduced 02:05). If the pull fails, fix the cause and re-run step 1 before step 2. Afterwards check `git status --short -- chess-backend/bootstrap/cache` is empty and `/up` returns 200. Attempted 2026-09-14 and blocked: it is a deploy action and the commit is unpushed (`docs/updates/2026_09_14_01_55_update.md`). SSH itself works with `ssh -i ~/.ssh/narun_vps_ed25519 narun@69.62.73.225`. Post-deploy check re-run 09:35: VPS still at `568e46c` and `bootstrap/cache` dirty as expected, `/up` 200. Re-run the check commands in `docs/updates/2026_09_14_09_35_update.md` once deployed. Re-checked 09:36: now 3 unpushed commits, head `c9db620`, still no `[skip ci]`. The copy-paste owner steps are in `docs/updates/2026_09_14_09_36_update.md`. Re-checked 10:10: `/up` 200 but VPS cache still dirty at `568e46c` — deploy not done, `d48a4b4` unpushed (`docs/updates/2026_09_14_10_10_update.md`). CI's `git reset --hard` does not need this step. 10:15: the empty `[skip ci]` head commit is now in place (`0e18197`, per the owner task), so a plain `git push origin master` no longer starts `deploy.yml` — no workflow disable needed (`docs/updates/2026_09_14_10_15_update.md`); the push itself still needs the owner (this worker's no-push rule). 10:20: still not pushed/deployed (`origin/master` and VPS HEAD both `568e46c`, cache dirty, `/up` 200); step 1 is now also packaged as the guarded wrapper `scripts/vps-deploy-step1.sh` — at the deploy, run `ssh -i ~/.ssh/narun_vps_ed25519 narun@69.62.73.225 'bash -s' < scripts/vps-deploy-step1.sh` from the repo root instead of copy-pasting the two commands (it adds the recovery fallback, premature-run/failed-merge guards and both post-checks; evidence `docs/updates/2026_09_14_10_20_update.md`). 10:50: steps 2-7 are also packaged now — `scripts/vps-deploy-steps2-7.sh` (fail-fast, `sudo -n`, pnpm preflight incl. nvm, www-data chown, post-deploy `/up`+web+3-services gate; `CHESS99_SKIP_FRONTEND=1` variant for pushes with no frontend changes, true of `568e46c..0e18197`); the full owner order — push → step-1 wrapper → steps-2-7 wrapper (`CHESS99_SKIP_FRONTEND=1`) → health script — is in `docs/updates/2026_09_14_10_50_update.md`. Re-checked 10:51: deploy still not done — `origin/master` still `568e46c` after fetch, VPS cache still dirty, `/up` 200 (`docs/updates/2026_09_14_10_51_update.md`); re-run the check after the owner deploy. Re-checked 11:13: unchanged — `origin/master` still `568e46c` (local master 6 ahead, head `690f348`), VPS `HEAD`/`origin/master` `568e46c`, cache still ` M packages.php`/` M services.php`/`?? routes-v7.php`, `/up` 200 (`/docs/updates/2026_09_14_11_13_update.md`). Re-checked 11:17 for the direct "push master" order: push-ready and still pending — `origin/master` `568e46c` after fetch (local 6 ahead, empty `[skip ci]` head `690f348`), deploy.yml active so the head marker is the guard; push remains owner-only (`docs/updates/2026_09_14_11_17_update.md`). Re-checked 11:25: deploy still not happened — `origin/master` still `568e46c`, VPS `HEAD`/`origin/master` `568e46c`, cache still ` M packages.php`/` M services.php`/`?? routes-v7.php`, `/up` 200; step 1 not run (deploy action + premature, pull brings nothing) (`docs/updates/2026_09_14_11_25_update.md`). Re-checked 11:40 for the direct post-deploy confirm: deploy still not happened — `origin/master` still `568e46c` after fetch (local 6 ahead, head `690f348`), latest deploy.yml run still the failed 09-13 one, VPS `HEAD`/`origin/master` `568e46c`, cache still ` M packages.php`/` M services.php`/`?? routes-v7.php`, `/up` 200 (`docs/updates/2026_09_14_11_40_update.md`). Re-checked 12:37 from a session with VPS network access: identical — `/up` 200, cache dirty, no deploy (`docs/updates/2026_09_14_12_37_update.md`). 12:39: the push-vs-deploy behavior choice itself is now written up as an owner decision brief with three copy-paste options (A push as-is / B deploy-on-push after rotation / C check-only dispatch) — pick one and push (`docs/updates/2026_09_14_12_39_update.md`). Re-checked 2026-09-16 (owner task "run the lookup migration on production during deploy"): still not deployed — both `2026_09_15_*` migrations are untracked, origin/master and VPS HEAD are still `568e46c`, and a read-only SSH check found no `2026_09_15_*` migration files or `migrate:status` rows on the VPS; step 3 `php artisan migrate --force` of `scripts/vps-deploy-steps2-7.sh` applies both automatically once the owner deploys.
- [ ] **[needs owner: SSH key + GitHub secrets]** Fix CI deploy auth. Run `scripts/rotate-ci-deploy-key.sh --dry-run`, then run it for real: it makes the key, authorises it for `narun@69.62.73.225` and sets all three secrets. The `deploy.yml` change is committed on local `master` as `daf9405` with `[skip ci]` (2026-09-14). Push it (`git push origin master`), confirm no push-triggered run started, then run `gh workflow run deploy.yml --ref master -f check_only=true`. Manual commands and the exact-match sudoers rule are in `scripts/deploy-notes.md` § CI Deploy Key. Preconditions were re-verified on 2026-09-14 (admin login OK, no key yet, dry run clean). The `ssh-copy-id` form is in `docs/updates/2026_09_14_00_55_update.md`. Re-checked 09:43: still no key, and `check_only` is not on `origin/master`. Push with a `[skip ci]` head first; the copy-paste order is in `docs/updates/2026_09_14_09_43_update.md`. Re-checked 10:33: preconditions all hold (dry-run clean, admin SSH OK, gh authed, secrets still 2025-10-02, last run still failed) and head `0e18197` now has `[skip ci]`, so the required push delivers `daf9405` without firing a deploy; updated owner order in `docs/updates/2026_09_14_10_33_update.md`. Re-checked 10:52: preconditions unchanged (dry-run clean, no key file); local master now 6 ahead with a fresh empty `[skip ci]` head `690f348`, so the push delivers `daf9405` without a deploy; latest copy-paste order in `docs/updates/2026_09_14_10_52_update.md`. Re-checked 11:36: preconditions still unchanged (dry-run rc=0 creating nothing, no key file, secrets still 2025-10-02, VPS `authorized_keys` 0 `gha-deploy` entries, remote `deploy.yml` still has no `check_only` input); rotate → push → dispatch order and copy-paste in `docs/updates/2026_09_14_11_36_update.md`
- [ ] **[needs owner decision, VPS root — only after the CI key works]** Narrow narun's `(ALL) NOPASSWD: ALL`: while it stands, anyone holding `SERVER_SSH_KEY` gets passwordless root. One command: `scripts/narrow-narun-sudoers.sh --dry-run`, then `scripts/narrow-narun-sudoers.sh` — owner-only, refuses to run until a `deploy.yml` run has gone green since 2026-09-01, then installs the 11-entry exact-match rule as `/etc/sudoers.d/chess99-deploy`, comments the grant at `/etc/sudoers:54` (the only passwordless narun line, located 2026-09-14), verifies every deploy sudo command still passes `sudo -n -l`, and rolls back on failure; afterwards re-run the check-only workflow. Verified 2026-09-14 by dry-run + 20 simulations (`docs/updates/2026_09_14_11_00_update.md`); still blocked on the key rotation item above
- [ ] Finish remaining recovery/visual acceptance on a device: physical font/TalkBack and authenticated two-client tests before release (nearby error vs empty state and post-login tab Back-stack are now unit-verified — `HomeViewModelTest` + the `navigateToMain popUpTo(Home)` fix, 2026-09-13)
- [ ] Complete a fresh Android device walkthrough incl. first-use tour timing/status on device (web tour gate landed in `d2e1840`; logout session clearing landed in `18d3302` and is now unit-verified incl. restart/offline tests — `coldStartDestination` + `HomeViewModelTest`, 2026-09-13)
- [ ] **[needs owner: second prod test account]** Run the two-client takeback leg live: set `CHESS99_SMOKE_B_EMAIL`/`CHESS99_SMOKE_B_PASSWORD` and run `node scripts/smoke-takeback-live.cjs pair` from `chess-frontend/` (the synthetic leg already passed on prod 2026-09-13; re-checked 2026-09-14 and still no account B, owner steps in `docs/updates/2026_09_14_00_25_update.md`)
- [ ] Deferred behind core chess UX: audit community capacity/earnings/identity/admin controls before sharing `/community-programs` publicly (the tester migration is already live: `Ran` in batch 46, tables empty, confirmed on the VPS 2026-09-14)
- [ ] **[needs a phone]** Side-load `chess99-android/chess99-1.0.0-phone-test-2026-09-13.apk` (built at `568e46c` 2026-09-13 through the full Android gate pass; replaces the 09-09 APK) and follow `chess99-android/PHONE_TEST.md`; run `:app:connectedDebugAndroidTest -PDEBUG_TARGET=prod` on arm64 (retried 2026-09-14: no device attached, suite compiles; runbook in `docs/updates/2026_09_14_01_25_update.md`; side-load retried 2026-09-14 01:35, still no phone, APK checksum/arm64 libs verified, owner steps in `docs/updates/2026_09_14_01_35_update.md`). September 8 emulator exercised x86_64 only; do not uninstall an existing app to bypass a signature conflict.
- [ ] **[needs owner action, Cloud Console]** Verify the existing `Chess99 Android App` OAuth client in GCP project `289055198367` has package `com.chess99.app` and SHA-1 `D5:DF:BD:6C:3A:08:BB:72:5E:75:AD:FB:90:A1:8B:7A:9C:BD:82:31` (the debug key today's APK is signed with); create a second Android client for the upload key `39:87:25:23:36:1A:E8:75:4F:84:B4:7B:8B:9E:40:69:4C:CD:1B:C7`, and a third for Play App Signing after enrolment. A GCP Android client holds exactly one package+SHA-1 pair, so each fingerprint needs its own
- [ ] **[needs owner credentials]** Replace placeholder `chess99-android/app/google-services.json` with a production Firebase file for `com.chess99.app`. No longer blocks Google Sign-In (decoupled 2026-08-23) — it now blocks only FCM push ("your opponent moved"), Crashlytics and Analytics, plus `:app:verifyReleaseConfiguration` for a Play build
- [ ] **[still blocked by the item above]** Build a fresh release AAB after `:app:verifyReleaseConfiguration` passes. Re-confirmed 2026-09-16 by running the gate: it fails on the placeholder `google-services.json` and nothing else, so the AAB cannot be built until the owner supplies the production Firebase file. Once it lands, re-run the gate then `:app:bundleRelease` with the offline flags in `docs/updates/2026_09_16_00_06_update.md` §5 (`preReleaseBuild` depends on the gate, so an incomplete config can't produce a bundle). The stale 2026-07-17 bundle is gone — `app/build/outputs/bundle/` no longer exists — so there is nothing left to upload by mistake. Upload-key signing is now ready and gate-enforced: the `keystore.properties` `storeFile` drive-letter drift (`C:` → `D:`) was fixed and four signing checks were added to `verifyReleaseConfiguration`. Re-verified again 2026-09-16 17:40 (`docs/updates/2026_09_16_17_40_update.md`): the gate still fails on that one issue with no signing issue; `:app:bundleRelease` was run and halts at the gate before compiling, so still no AAB and no `app/build/outputs/bundle/`; and a reverse mutation check (production-shaped Firebase file, nothing else changed) makes the gate pass exit 0 — **the owner's Firebase file is the only thing left**, and the placeholder was restored byte-identical afterwards
- [ ] **[remaining: Play App Signing only]** `assetlinks.json` server-side problem fixed by the 2026-09-13 production deploy — `https://chess99.com/.well-known/assetlinks.json` now answers 200 with `application/json` and `scripts/check_assetlinks.ps1` PASSes. Still open: append Play's signing SHA-256 after App Signing enrolment and redeploy
- [ ] **[needs a phone + internal track]** Run a physical-device internal-track pass: Google Sign-In, FCM, Crashlytics, reset/referral App Links, minor/adult chat report+block, offline puzzles and Stockfish
- [ ] **[needs Play Console + owner approval]** Complete Play Console Data Safety, Families/UGC declarations, content rating, screenshots/tablet assets and pre-launch report; get explicit owner approval before upload
- [x] Fix the 9 Android unit-test failures left by the UI-copy extraction (extraction done 2026-09-16 per docs/updates/2026_09_16_03_40_update.md: strings.xml 74→1353 resources, heuristic literals 537→199 with every survivor documented as deliberate) — offline `:app:testDebugUnitTest` on 2026-09-16: 289 tests, 9 failed, all `context.getString` returning `""` on relaxed mocks (`PlayComputerViewModelTest` ×4, `PlayMultiplayerResumeRequestTest` ×3, `PlayMultiplayerTakebackTest` ×2); stub resource resolution in those fixtures (e.g. resolve from strings.xml or a name→value map) and re-run the gate to green

MODELS: Codex current session (controller/web); gpt-5.6-sol/high (three disjoint native/backend implementation agents and fresh-context verification); earlier records retained in review archive
