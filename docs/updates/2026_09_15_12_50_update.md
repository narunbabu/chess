# 2026-09-15 12:50 — Live check of the 09:59 fresh-load sent panel (web) + commit

Local stack: `php artisan serve` (127.0.0.1:8000, SQLite), `reverb:start`,
web `pnpm start` (dev, localhost:3000). Fixture `chess-backend/livecheck_fixture.php`
(untracked): LiveCheck A (32) / B (33), fresh paused casual game **471**.
Script and screenshots: `logs/lc3/web_sent_panel_check.js`, `logs/lc3/*.png`.
(The fixture `setup` was run three times by mistake. Games 469/470 are
leftovers in the local SQLite only.)

## Scenario (headless Chromium, Playwright)

1. `age-request 471 0 58`: A's own request is pending, requested now and
   expiring in 58 s.
2. Open the game fresh the lobby way (`lastInvitationAction=resume_game`) and
   **don't click anything**.

## Result — PASS

- Pre-send check `GET api/websocket/games/471/resume-status` → 200
  `pending:true, type:'sent', requested_by_id:32, expires_at:07:20:21Z`.
  Console: `🎯 Skipping auto-send: our own request already pending {ownSeconds: 32}`.
- The overlay showed **"📤 Resume request sent from lobby!"** + **"Waiting for
  opponent to accept... 32s"** with Cancel Request and Back to Lobby
  (`web_1_sent_panel.png`). The request was 25.3 s old, so the expected
  remaining time was 32.7 s, which matches the server.
- 3 s later: **"... 29s"** (`web_2_countdown_plus3s.png`).
- "Auto-resume may have failed" was **not** shown while the request was live.
- **0 POSTs** to `resume-request`/`request-resume` for the whole run, so no
  duplicate request was sent.
- After server expiry (62 s): the panel went back to "Auto-resume may have
  failed" + Manual Resume Request (`web_3_after_expiry.png`). That is the
  existing countdown-expiry path, the same end state the 09:50 check saw.

## Side observation (pre-existing, not from this change)

During the paused state the page polls `GET api/games/{id}/resume-status`
(`PlayMultiplayer.js:4236`) about once a second, and every call returns 404
"Route not found". The working route is `api/websocket/games/{id}/resume-status`.
This is noise and wasted requests, not a functional failure here.

## Gates (re-run before commit)

- Jest `pnpm test --watchAll=false resumeRequestRefusal WebSocketGameService.contract PlayMultiplayerHooks`:
  3 suites / 32 passed.
- eslint on the 3 files: 0 errors, 77 warnings (pre-existing).
- `pnpm build`: Compiled successfully.

## Commit

`PlayMultiplayer.js`, `resumeRequestRefusal.js`, its test, and the 09:59 and
this update doc. `[skip ci]`, local `master`, not pushed or deployed. All
processes started for the check were stopped.
