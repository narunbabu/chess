# Earlier status entries retained from STATUS.md

- 2026-09-14 11:25: Owner task "at next manual deploy run deploy-notes step 1 and verify bootstrap/cache is clean": **blocked — the manual deploy has not happened, so step 1 was not run and the cache check fails as expected pre-deploy, not as a regression** (running step 1 is a deploy action, forbidden by this session's no-deploy rule, and premature — the pull would bring nothing; same ruling as 09:48/10:20/11:13). Read-only: after `git fetch origin`, `origin/master` still `568e46c` (local master 6 ahead, head `690f348` — owner push not done); latest deploy.yml run still the failed 09-13 `34764068752`; VPS `HEAD` `568e46c`, its `origin/master` ref `568e46c`; `git status --short -- chess-backend/bootstrap/cache` still ` M packages.php`, ` M services.php`, `?? routes-v7.php`; `https://api.chess99.com/up` **200**. Owner: push, then `scripts/vps-deploy-step1.sh` (its premature-run guard aborts exactly this state) — copy-paste in the evidence. [Evidence](docs/updates/2026_09_14_11_25_update.md).
- 2026-09-14 11:17: Owner task "push master with deploy.yml disabled or a `[skip ci]` head commit": **blocked at the push — the push is forbidden by this session's no-push rule** (same ruling as 09:29→10:52); nothing pushed or toggled. Of the task's two guards exactly one holds: `deploy.yml` is **active** on the remote (`gh workflow list`), not disabled, but head `690f348` carries `[skip ci]` (empty commit, verified `git diff --stat` empty), and GitHub checks only the head commit, so the push would not fire it. Verified read-only: after `git fetch origin`, `origin/master` still `568e46c` with local master 6 ahead / 0 behind (fast-forward), the 6 delivered commits are `daf9405`/`c9db620`/`d48a4b4`/`872ced4`/`0e18197`/`690f348`, latest deploy.yml run still the failed 09-13 `34764068752`, and the dirty tree stays uncommitted/out of the push. Owner: `git push origin master` — copy-paste in the evidence. [Evidence](docs/updates/2026_09_14_11_17_update.md).
- 2026-09-14 11:13: Owner task "After the first deploy, verify VPS bootstrap/cache is clean and /up returns 200": **still one of two passes — the first deploy has not happened, so the cache check fails as expected pre-deploy, not as a regression**. Read-only: after `git fetch origin`, `origin/master` is still `568e46c` (local master 6 ahead, head `690f348` — the owner push has not happened); VPS `HEAD` `568e46c`, its `origin/master` ref `568e46c`; `git status --short -- chess-backend/bootstrap/cache` still ` M packages.php`, ` M services.php`, `?? routes-v7.php`; `https://api.chess99.com/up` returns **200**. Starting the deploy is owner-only (push + VPS deploy steps are forbidden by this session's rules). [Evidence](docs/updates/2026_09_14_11_13_update.md).
- 2026-09-14 11:00: Owner task "narrow narun's NOPASSWD: ALL once the CI key works": **prepared and fully verified, not executed — the precondition is false (CI key still broken: secrets still 2025-10-02, no successful deploy.yml run since 2025-12-12, no rotated key file, `origin/master` still `568e46c` after fetch), and the `/etc/sudoers` edit is owner-only anyway**. Read-only on the VPS: the broad grant is exactly `/etc/sudoers:54` (`narun ALL=(ALL) NOPASSWD:ALL`), the only passwordless narun line (`sudoers.d` holds only `ubuntu`/`deployuser` grants). Gap found: the 00:45 8-command rule misses three `sudo -n` forms `vps-deploy-steps2-7.sh` runs (rsync, the combined two-path chown, `systemctl reload php8.3-fpm`) — narrowing with it would abort the manual deploy mid-flight. Delivered `scripts/narrow-narun-sudoers.sh` (owner-only): enforces the green-run precondition, installs the extended 11-entry rule as `/etc/sudoers.d/chess99-deploy` (visudo-validated, 0440, atomic), backs up + comments exactly line 54, verifies all 11 commands via `sudo -n -l` plus `sudo -n true` failing, rolls back on any failure. Verified: `bash -n`, 20 simulated cases (one real bug found and fixed: the idempotent re-run aborted), and a live `--dry-run` (VPS confirmed untouched: no rule file, no backup, grant still active). [Evidence](docs/updates/2026_09_14_11_00_update.md).
- 2026-09-14 10:52: Owner order "rotate CI key, empty `[skip ci]` commit, push master, then check-only `deploy.yml` run": **1 of 4 done — created the empty `[skip ci]` head `690f348` (local master now 6 ahead, 0 behind); the rotation script (owner-only header: creates `~/.ssh` key, edits VPS `authorized_keys`, sets GitHub secrets), the push and the workflow dispatch are all forbidden by this session's credentials/no-push/no-deploy rules** (same rulings as 10:33/10:50). Read-only: `bash -n` + `--dry-run` clean (rc=0, nothing created, no `chess99_gha_deploy` key), `origin/master` still `568e46c` after fetch, nothing staged so the dirty tree stayed out of the commit, and the new head's `[skip ci]` means the owner push won't fire `deploy.yml`. Copy-paste owner order in the evidence. [Evidence](docs/updates/2026_09_14_10_52_update.md).
- 2026-09-14 10:51: Owner task "After deploy: confirm VPS bootstrap/cache git status is empty and /up returns 200": **one of two passes — the deploy has not happened, so the cache check fails as expected pre-deploy, not as a regression**. Read-only: after `git fetch origin`, `origin/master` is still `568e46c` (local master 5 ahead, head `0e18197` — the owner push from the 10:50 task has not happened); VPS `HEAD` `568e46c` and its `origin/master` ref `568e46c`; VPS `git status --short -- chess-backend/bootstrap/cache` still ` M packages.php`, ` M services.php`, `?? routes-v7.php`; `https://api.chess99.com/up` returns **200**. Nothing pushed or run on the VPS. [Evidence](docs/updates/2026_09_14_10_51_update.md).
- 2026-09-14 10:50: Owner task "push master (starts deploy.yml) and run deploy-notes step 1 then steps 2-7 on the VPS": **blocked — the push and the VPS deploy are both forbidden by this session's no-push/no-deploy rules** (same ruling as 09:36→10:20); nothing pushed, nothing run on the VPS. Premise correction: head `0e18197` carries `[skip ci]`, so the push does **not** start `deploy.yml` (and every run since 2026-02-06 fails at SSH anyway — key rotation TODO still open). Re-verified read-only: `origin/master` still `568e46c` (local 5 ahead), VPS HEAD `568e46c`, `bootstrap/cache` still dirty as expected, no pending migrations (the 5 commits carry none; no `chess-frontend/` files either → step 6 skippable), `/up` and chess99.com 200, secrets still 2025-10-02. Delivered: `scripts/vps-deploy-steps2-7.sh`, a guarded fail-fast wrapper for steps 2-7 (`sudo -n`, pnpm preflight incl. nvm, www-data chown, post-deploy both-URLs-200 + 3-services gate, `CHESS99_SKIP_FRONTEND=1`), verified by `bash -n` + 8 simulated cases (all rc-correct); referenced from `deploy-notes.md` after step 7. Owner: push, then the two wrapper one-liners in the evidence. [Evidence](docs/updates/2026_09_14_10_50_update.md).
