# 2026-09-14 01:35 — Side-load the 2026-09-13 phone-test APK and follow PHONE_TEST.md: blocked, no phone

**Result: BLOCKED — no arm64 phone connected.** `adb install -r` was not run, no PHONE_TEST.md
checklist item was exercised, and nothing counts as device acceptance. No code changed.

## Device discovery (this session)

| Check | Result |
|-------|--------|
| `adb start-server`, then `adb devices -l` (SDK `platform-tools`, adb 1.0.41 / 36.0.0), re-checked after 5 s | empty list both times |
| `adb mdns services` (Wi-Fi debugging) | none discovered |
| `Get-PnpDevice -PresentOnly -Class USB,WPD,AndroidUsbDeviceClass` | host controllers, hubs, composite devices and the `Expansion` / `ExtremeSSD` storage volumes only; no Android/ADB/MTP interface |

No emulator was attached either, so there was no device to install to.

## APK checked offline (ready for the owner)

- File: `chess99-android/chess99-1.0.0-phone-test-2026-09-13.apk`, 29,217,676 bytes, dated 2026-09-13 23:06.
- SHA-256 `3184938edc20dcf8a5808859c43811fffbf63e848b5f03525454426a8273ee88`. This matches the
  checksum recorded in `2026_09_13_23_10_update.md`, `2026_09_13_23_50_update.md` and `2026_09_13_23_58_update.md`.
- Native libraries: `lib/arm64-v8a/`, `lib/armeabi-v7a/` and `lib/x86_64/` (3 entries each). That
  means an arm64 phone is supported.

## Owner steps

1. Plug in an arm64 Android 8.0+ phone with USB debugging on and accept the RSA prompt.
   `adb devices` must list its serial as `device`.
2. From `chess99-android/`: `adb -s <serial> install -r chess99-1.0.0-phone-test-2026-09-13.apk`.
   If it reports `INSTALL_FAILED_UPDATE_INCOMPATIBLE` (a signature conflict), **stop and report it**.
   Do not uninstall, because that erases local data.
3. Work through the checklist in `chess99-android/PHONE_TEST.md`. It covers onboarding and guest
   Play Computer, Casual as White/Black, Learning/Best move/Undo/resign/local review persistence,
   TalkBack legal-move and promotion controls at large font, the signed-in tabs, a two-tester
   Casual online game, and sign-out plus restart.
4. Optional, same device (disconnect any emulator first):
   `./gradlew.bat :app:connectedDebugAndroidTest -PDEBUG_TARGET=prod --no-daemon --console=plain --max-workers=2`
   (see `2026_09_14_01_25_update.md`).

## Cleanup

`adb kill-server` was run (rc=0). No Gradle, emulator or dev server was started.
