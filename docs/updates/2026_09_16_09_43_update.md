# 2026-09-16 09:43 — Android UI-copy unit-test fixtures fixed

## What changed

The three Android JVM fixtures affected by the 2026-09-16 UI-copy extraction
now provide the extracted resource text to their relaxed `Context` mocks:

- `PlayComputerViewModelTest`: engine-unavailable copy plus `You` and
  `Computer` share-summary labels.
- `PlayMultiplayerResumeRequestTest`: sent, own-pending, opponent-pending and
  failed resume-request copy.
- `PlayMultiplayerTakebackTest`: restored-connection and expired-takeback copy.

The engine-init test also now compares the resolved user-facing string rather
than the integer `R.string` resource ID.

## Verification

- Focused affected classes: `:app:testDebugUnitTest` — BUILD SUCCESSFUL.
- Full offline gate from `chess99-android/` with project-local Gradle home,
  read-only dependency cache and in-process Kotlin:
  `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug` — BUILD SUCCESSFUL.
- Unit reports: 32 suites, 298 tests, 0 failures, 0 errors, 0 skipped.
- Lint: 0 errors (45 existing warnings in the report).

No production source, deployment, commit or push was performed.
