# 2026-09-17 10:24 — Delivered-but-uncommitted work committed in five gated [skip ci] batches

Task: "Commit the P0 batch with a [skip ci] head alongside the other gated
uncommitted work" — the STATUS `Next / TODO` item that asked for the
delivered-but-uncommitted work to be committed "in reviewed, gated batches",
every commit with a `[skip ci]` head, no push.

## Batch decision

The P0 correctness batch (public replay 404 + viewer, Learn lesson
completion, Daily Challenge submit/review) cannot be committed in isolation:
its Android files carry the 09-16 string-extraction rewrite in the same
methods, the viewer calls the new `friendlyError(Context, @StringRes)`
signature from that batch, and `values/strings.xml` is a single shared file.
A P0-only commit would have been a never-gated Frankenstein or would not
compile. The batches were therefore cut by area, so that every commit state
is exactly a gated state (each area's gate ran on the full current content of
that area):

| Commit | Area | Files |
|---|---|---|
| `2ea0d5f` | backend: championship drops/elimination, publicShow 404 fix + `player_color`, bot-resume clocks, compact lifeline field, expired-match fixes, migrations, 4 new test classes, livecheck fixture | 21 |
| `34454a7` | web: resume-status poll route, best-move budget util, Withdrawn badge UI, jest.config.js e2e exclusion (incl. the previously staged 12:58 `PlayMultiplayer.js` fix, at its final gated state) | 8 |
| `eba4933` | android: P0 viewer/Learn/Daily fixes, app-wide string extraction (+ tooling and batch manifests), SecurePreferences crypto migration, lifeline markers, 12 new test classes | 158 |
| `8fda0b4` | CI/scripts: deploy.yml post-deploy health-check wiring, vps-deploy wrappers, health-check test, sudoers narrowing tool, gate-script fixes | 7 |
| (this commit) | docs: all 09-14..09-17 update notes + device-check screenshots, parity-gap analysis, STATUS | — |

Secret scan before each commit: no keys/tokens/passwords; the deploy scripts
reference only the key *path* `~/.ssh/narun_vps_ed25519` and the already-public
VPS IP; `app/build.gradle.kts` only validates `keystore.properties` entries.

## Gates re-run on the tree before committing (2026-09-17)

| Gate | Result |
|---|---|
| Backend `php artisan test` (full suite) | **409 passed / 158 skipped / 1 risky / 0 failed** (4665 assertions, 100.7 s) — first full-suite pass over the 09:35 `player_color` change (+2 tests vs 09-16's 407) |
| Web `pnpm test --watchAll=false` | **15 suites / 128 tests passed** |
| Android `:app:testDebugUnitTest --rerun` (offline recipe of `2026_09_17_02_00_update.md`: `--no-daemon --offline`, project-local Gradle home, RO dep cache, in-process Kotlin, `WS_KEY_RELEASE` via env) | **BUILD SUCCESSFUL, exit 0**; XML reports: **319 tests / 0 failures / 0 errors / 0 skipped** (36 class reports) |

Because the final HEAD tree is byte-identical to the gated working tree
(batches only add files to git, no content changed), the pushed-state gates
apply verbatim to the commit series.

## Deliberately left uncommitted / untracked

`chess99-android/build-check.log`, `chess99-android/merge_check.sh`,
`chess99-android/scripts/__pycache__/`, `logs/` — session junk, not work.

## Hygiene

No push, no deploy, no workflow changes beyond the already-gated `deploy.yml`
content. Every commit carries `[skip ci]`, so a later push cannot fire the
deploy workflow. Gradle ran `--no-daemon`; zero java processes left.
