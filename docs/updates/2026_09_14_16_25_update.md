# Android leave-routing and Best-toggle commit blocked

The requested local commit could not be created. Explicit staging of the eight
Android source/test files failed with:

```text
fatal: Unable to create 'D:/ArunApps/Chess-Web/.git/index.lock': Permission denied
```

This session exposes `.git` as read-only and does not allow approval escalation.
No permissions were changed or alternate Git metadata used. The index remains
empty and HEAD remains `690f348`. No push or deployment was attempted.

## Reviewed commit scope

Under `chess99-android/app/src/`:

- `main/java/com/chess99/presentation/MainActivity.kt`
- `main/java/com/chess99/presentation/navigation/NavGraph.kt`
- `main/java/com/chess99/presentation/game/PlayComputerScreen.kt`
- `main/java/com/chess99/presentation/game/PlayComputerViewModel.kt`
- `main/java/com/chess99/presentation/game/PlayMultiplayerScreen.kt`
- `main/java/com/chess99/presentation/game/PlayMultiplayerViewModel.kt`
- `test/java/com/chess99/presentation/game/PlayComputerViewModelTest.kt`
- `test/java/com/chess99/presentation/navigation/MainNavigationTest.kt`

Root leave uses current authentication/onboarding state; ordinary stacks pop.
Casual computer Best reveals do not spend the Learning help pool. Multiplayer
adds a Best toggle and rated guards. Existing implementation was left intact.
Relevant prior evidence: `2026_09_14_13_11_update.md` (also contains unrelated
deployment notes), `2026_09_14_15_45_update.md`, and
`2026_09_14_16_14_update.md`. Review STATUS.md's pre-existing edits when staging
documentation; unrelated workflow, deployment scripts and web smoke-test edits
must not be swept into this Android commit.

## Checks in this session

- `git diff --check -- chess99-android`: passed.
- From `chess99-android`, `./gradlew.bat --no-daemon --gradle-user-home D:/ArunApps/Chess-Web/chess99-android/.gradle :app:testDebugUnitTest :app:assembleDebug`:
  failed before tests/build at Gradle 8.11.1 distribution download with
  `java.net.SocketException: Permission denied: getsockopt`.
- `git diff --cached --name-only`: empty after staging failed.
- Earlier 251-test and debug-build passes are historical evidence, not fresh
  passes in this session. Signed-in root leave and multiplayer Best device
  acceptance remain pending.

No emulator, ADB server, dev server, watcher or Gradle daemon was started.
The wrapper exited before Gradle launched.

## Resume

Use a session with writable Git metadata and available Gradle dependencies,
rerun Android checks, stage the scoped files and supporting documentation, then
commit as `fix(android): restore root leave routing and casual Best controls [skip ci]`.
Keep the `[skip ci]` head convention for the existing owner deployment handoff.
This task authorizes a local commit only; do not push or deploy.
