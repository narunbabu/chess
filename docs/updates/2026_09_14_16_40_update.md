# Android lint rerun — passed (0 errors, 12 warnings)

Resumes the blocked 16:28 attempt (docs/updates/2026_09_14_16_28_update.md).

## How the download blocker was removed (no network, no global writes)

- The 16:28 project-local Gradle home held only a partial
  `gradle-8.11.1-bin.zip.part`. The user-level `~/.gradle` already had a complete
  Gradle 8.11.1 install, so it was **copied** (read-only source) into
  `chess99-android/.gradle/wrapper/dists/gradle-8.11.1-bin/bpt9gzteqjrbo1mjrsomdt32c/`,
  then `.part`/`.lck` were removed and a `.ok` marker was added. `.gradle/` is gitignored.
- Dependencies came from the user-level cache through Gradle's **read-only** shared
  cache (`GRADLE_RO_DEP_CACHE=C:\Users\ab\.gradle\caches`). It already held lint
  31.9.1, the version that matches AGP 8.9.1. Every writable cache stayed project-local.
- `--offline` was used, so no network was needed. `ANDROID_USER_HOME` pointed at
  `chess99-android/.gradle/android-user-home`, so nothing was written to `~/.android`.
  The Kotlin compiler ran in-process, so no Kotlin daemon was started.

Command (PowerShell, from `chess99-android`):

```powershell
$env:GRADLE_RO_DEP_CACHE='C:\Users\ab\.gradle\caches'
$env:ANDROID_USER_HOME='D:\ArunApps\Chess-Web\chess99-android\.gradle\android-user-home'
.\gradlew.bat --no-daemon --offline --gradle-user-home 'D:\ArunApps\Chess-Web\chess99-android\.gradle' '-Pkotlin.compiler.execution.strategy=in-process' :app:lint
```

## Result

`BUILD SUCCESSFUL in 7m 32s`, exit 0 (33 actionable tasks: 9 executed, 24 up-to-date).
`abortOnError` is at its default (true), and the report shows **0 errors, 12 warnings**,
plus 69 informational entries (mostly the deliberately demoted
`GradleDependency`/`AndroidGradlePluginVersion` checks).
Raw output: `logs/android-lint-2026_09_14_16_45.log` (UTF-16).
Reports: `chess99-android/app/build/reports/lint-results-debug.{html,xml,txt}`.

| Check | Count | Location |
|---|---|---|
| UseKtx (`SharedPreferences.edit` KTX) | 6 | `data/local/LobbyPreferences.kt` x5, `presentation/history/LocalGameReviewStore.kt` |
| ModifierParameter (Modifier not first optional param) | 5 | `common/ChessBoardView.kt`, `history/GameHistoryScreen.kt`, `common/GameTimerDisplay.kt`, `history/MoveAnalysisUi.kt`, `common/UpgradePromptCard.kt` |
| CredentialManagerSignInWithGoogle | 1 | `presentation/auth/GoogleSignInHelper.kt:60` |

None of the 12 warnings are in the files changed by the uncommitted Android
leave-routing and Best-toggle work (`MainActivity`, `PlayComputer*`, `PlayMultiplayer*`,
`NavGraph`, and their tests). Those changes introduced no lint findings.

## Scope

No source, lint config, credentials or global settings changed. No commit, push or deploy.
Before the final report, no process referencing Chess-Web remained. The build was a
single-use `--no-daemon` run; the other `java.exe` processes on the machine belong to
other projects or VS Code and were left alone. Warnings were recorded, not fixed.
