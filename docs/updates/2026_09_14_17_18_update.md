# Android leave-routing and Best-toggle commit

This session had writable Git metadata (a probe file in `.git` was created and
removed), so the 16:25-17:06 staging blocker no longer applied.

## Scope verified before committing

The eight files from `2026_09_14_16_25_update.md`, under `chess99-android/app/src/`:

- `main/java/com/chess99/presentation/MainActivity.kt`
- `main/java/com/chess99/presentation/navigation/NavGraph.kt`
- `main/java/com/chess99/presentation/game/PlayComputerScreen.kt`
- `main/java/com/chess99/presentation/game/PlayComputerViewModel.kt`
- `main/java/com/chess99/presentation/game/PlayMultiplayerScreen.kt`
- `main/java/com/chess99/presentation/game/PlayMultiplayerViewModel.kt`
- `test/java/com/chess99/presentation/game/PlayComputerViewModelTest.kt`
- `test/java/com/chess99/presentation/navigation/MainNavigationTest.kt`

Diffs reviewed: root leave routing (`leaveRouteFor`, `popBackStackOrExitTo`,
`rootExitDestination`), casual/Learning/Rated Best rules in Play Computer,
PlayMultiplayer Best toggle with rated guards, and the matching tests. They
touch none of the eight 16:56 lint-cleanup files.

## Checks in this session

The 17:00 run covered the whole working tree, including the uncommitted lint
cleanup. To prove the commit builds on its own, a temporary detached worktree at
`HEAD` (`690f348`) received only these eight files (plus the ignored
`local.properties` and `google-services.json`) and ran:

```powershell
$env:GRADLE_RO_DEP_CACHE = 'C:\Users\ab\.gradle\caches'
$env:ANDROID_USER_HOME = 'D:\ArunApps\Chess-Web\chess99-android\.gradle\android-user-home'
.\gradlew.bat --no-daemon --offline --gradle-user-home 'D:\ArunApps\Chess-Web\chess99-android\.gradle' '-Pkotlin.compiler.execution.strategy=in-process' :app:testDebugUnitTest :app:assembleDebug
```

- **BUILD SUCCESSFUL in 5m 19s**, exit 0; 52 actionable tasks, all executed.
- JUnit XML (25 suites): **251 tests, 0 failures, 0 errors, 0 skipped**;
  MainNavigationTest 8/8, PlayComputerViewModelTest 50/50.
- `app-debug.apk` assembled.
- `git diff --check` on the eight files passed.
- Log: `logs/android-commit-verify-2026_09_14.log`; script:
  `logs/android-commit-verify-2026_09_14.ps1` (logs are not committed).

The worktree was removed and pruned afterwards. No Java process referencing
Chess-Web remained. The other `java.exe` processes belonged to VS Code, Android
Studio and another project, and were left alone. No emulator, ADB or server was
started.

## Commit

`fix(android): restore root leave routing and casual Best controls [skip ci]`
on local `master`, containing the eight files, this note, STATUS.md, the
supporting evidence (`13_11`, `15_45`, `16_14` + its status archive, `16_25`,
`16_28`, `16_40`, `17_00`, `17_03`, `17_05`, `17_06`) and the six
`2026_09_14_leave_*.png` screenshots. The evidence docs and STATUS.md were
checked for credentials; none found.

Left uncommitted on purpose: the eight lint-cleanup files and their
`16_56` note (a separate commit), `.github/workflows/deploy.yml`, `scripts/*`,
`chess-frontend/scripts/smoke-takeback-live.cjs`, the other deploy notes and
`logs/`. The `[skip ci]` head keeps the owner's push from starting
`deploy.yml`. Not pushed, not deployed.

Still open: signed-in PlayMultiplayer root leave and the multiplayer Best toggle
have not been checked on a device.
