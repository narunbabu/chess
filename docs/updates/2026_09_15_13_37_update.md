# Elimination bracket round type fix

## Change

- Added `ChampionshipRoundType::ELIMINATION` with the stable `elimination` code and lookup ID 7.
- Added an idempotent migration for the `championship_round_types` lookup row used by existing databases.
- Updated `EliminationBracketService` queries to filter the persisted `round_type_id` column rather than the model's virtual `round_type` attribute. First-round generation, bracket visualization, winner detection, and subsequent-round pairing now use the same stored type.
- Added feature coverage for first-round pairings, persisted matches, and pairing completed winners into the next round.

## Verification

- `php artisan migrate --pretend` — passed; emitted the expected `elimination` insert.
- `php artisan migrate` — passed locally; `2026_09_15_010000_add_elimination_round_type` ran successfully.
- `php artisan test tests/Feature/EliminationBracketServiceTest.php tests/Feature/ChampionshipParticipantDropTest.php` — 16 passed / 0 failed.
- `php artisan test` — 397 passed / 158 skipped / 1 risky / 0 failed (4612 assertions).
- `git diff --check` — passed.

Production MySQL was not accessed or migrated in this session.
