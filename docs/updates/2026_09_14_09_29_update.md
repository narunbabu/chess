# 2026-09-14 09:29 — Push `d48a4b4` to origin/master: not pushed (blocked)

## Task
Owner: push `d48a4b4` to `origin/master`.

## Outcome
**Not pushed.** Two independent reasons:

1. **Session rules.** This unattended worker session forbids `git push` and deploys. The push has to come from the owner or an owner-authorised session.
2. **A plain push would start a production deploy.** `.github/workflows/deploy.yml` triggers on `push: branches: [master]` and has no path filter. GitHub decides whether to skip a push run from the *head* commit's message. The head would be `d48a4b4`, which has no `[skip ci]` (`grep -ci "skip ci"` = 0). `daf9405` has one, but it is not the head. So `git push origin master` starts `Deploy to Production`. Today that run would probably fail at the SSH handshake, because `SERVER_SSH_KEY` has been broken since June. If the key is rotated first (the TODO item), the run would do a real production deploy.

## Verified state (read-only)
- `git fetch origin` OK. `git rev-list --left-right --count origin/master...master` = `0 2`. Local `master` is exactly 2 commits ahead (`daf9405`, `d48a4b4`) and not behind, so the push is a clean fast-forward from `568e46c`.
- `d48a4b4` touches `chess-backend/bootstrap/cache/{.gitignore,packages.php,services.php}` and `scripts/deploy-notes.md` (+6/−375). It has no migrations and no frontend changes.
- `ci.yml` runs on `pull_request` only, so it is not triggered by a push.
- The working tree still has uncommitted changes (`STATUS.md`, `PHONE_TEST.md`, `check_backend_health.ps1`, `deploy-notes.md`, plus untracked files). They do not affect a push of committed refs.

## Owner options
- **Push without triggering a deploy:** `git push -o ci.skip origin master` does **not** work on GitHub. Use one of these instead:
  - Disable the workflow first (`gh workflow disable deploy.yml`), push, then re-enable it (`gh workflow enable deploy.yml`). Or
  - Amend the head message to include `[skip ci]` before pushing. This rewrites the unpushed `d48a4b4`, which is safe because it is local-only.
- **Push and let it deploy:** only after the CI key is rotated. Then confirm the run and check `https://api.chess99.com/up` returns 200. The CI script's `git reset --hard` does not need the manual `bootstrap/cache` checkout step.
- After any push, confirm with `gh run list --workflow deploy.yml -L 3` which runs, if any, started.

## Tests
None run: no code changed. `php artisan test` was last run on `d48a4b4` at commit time (356 passed / 158 skipped / 1 risky, rc=0).
