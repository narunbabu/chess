# Release AAB — re-verified blocked; the Firebase file is now proven to be the only blocker

Task: *"Build a fresh release AAB after `:app:verifyReleaseConfiguration` passes."*

**Outcome: no AAB was built — the precondition is still unmet.** `app/google-services.json`
is still the placeholder, which only the owner can replace. What is new in this session
is that the blocker was narrowed from "the gate reports one issue" to a *proven*
statement: with a production-shaped Firebase file in place and nothing else changed,
the gate passes. Everything else on the release path — package name,
`GOOGLE_SERVER_CLIENT_ID`, `WS_KEY_RELEASE`, and all four signing checks including the
keystore resolving on disk — is green today.

No source file was changed by this session.

## 1. The placeholder is still in place

`chess99-android/app/google-services.json`, 666 bytes, mtime 2026-02-24, untracked
(gitignored), still carries `"project_id": "chess99-placeholder"`,
`"project_number": "000000000000"`, `"api_key": "placeholder-api-key-for-compilation"`.
sha256 `098d7f85…1dc6294`.

## 2. The gate was run again — one issue, unchanged

Offline recipe (project-local Gradle home, read-only shared dep cache, `--no-daemon`,
`--offline`, in-process Kotlin; `WS_KEY_RELEASE` passed through the environment because
`--gradle-user-home` does not read `~/.gradle/gradle.properties`):

```powershell
$env:WS_KEY_RELEASE='<from ~/.gradle/gradle.properties>'   # length 20
$env:GRADLE_RO_DEP_CACHE='C:\Users\ab\.gradle\caches'
$env:ANDROID_USER_HOME='D:\ArunApps\Chess-Web\chess99-android\.gradle\android-user-home'
.\gradlew.bat --no-daemon --offline `
  --gradle-user-home 'D:\ArunApps\Chess-Web\chess99-android\.gradle' `
  '-Pkotlin.compiler.execution.strategy=in-process' :app:verifyReleaseConfiguration
```

`BUILD FAILED`, exit 1, in 31 s:

```
Release configuration is incomplete:
- app/google-services.json is still the placeholder config. FCM, Crashlytics, and Analytics would be inert.
```

No signing issue is listed, so the four signing checks added on 2026-09-16 00:06 all
pass: `keystore.properties` present, `storeFile` non-blank, `storeFile` resolving on
disk (`D:/ArunApps/_keystores/chess99-upload.jks`, present, 2760 bytes), and
`storePassword`/`keyAlias`/`keyPassword` non-blank. The drive-letter fix has held.

## 3. `:app:bundleRelease` really is gated — verified, not assumed

The task's own claim is that `preReleaseBuild` depends on the gate. That was executed
rather than trusted:

```
> Task :app:preBuild UP-TO-DATE
> Task :app:verifyReleaseConfiguration FAILED
…
- app/google-services.json is still the placeholder config. …
BUILD FAILED in 28s   (exit 1)
```

The build stops at the gate, before any compilation or packaging.
`chess99-android/app/build/outputs/bundle/` still does not exist and no `.aab` exists
anywhere under `chess99-android/` — checked both before and after the run. There is no
bundle to upload by mistake, and none was produced here.

## 4. Reverse mutation check — the Firebase file is the *only* remaining blocker

A failing gate naming one issue does not by itself prove the gate would pass once that
issue is cleared; a later check could fail only after the earlier one stops short. So
the repo's mutation-check convention was run in reverse.

`app/google-services.json` was backed up, then rewritten in place with a
production-*shaped* file — real project number `289055198367`, project id
`chess99-mutation-check`, a non-placeholder api key, package `com.chess99.app`
preserved — with nothing else in the tree touched. The gate then:

```
> Task :app:verifyReleaseConfiguration
BUILD SUCCESSFUL in 25s   (exit 0)
```

**So the production Firebase file is sufficient as well as necessary**: the moment a
real one lands, the gate goes green and `:app:bundleRelease` proceeds.

The placeholder was restored immediately afterwards and verified byte-identical —
sha256 back to `098d7f8584440457da46d6ea0e0d43c9a43ae7f819c46e953ecf157c41dc6294`,
`chess99-placeholder` present again, backup file removed. The synthetic file was
never committed (the path is gitignored) and existed only between the two commands
above. It is not a usable Firebase config and was not built against — only the gate's
regex checks read it.

## 5. What the owner must do; everything after it is automatic

1. Drop the production Firebase Android config for `com.chess99.app` at
   `chess99-android/app/google-services.json` (owner credentials — the preceding
   STATUS "Next / TODO" item).
2. Re-run the gate command in §2 — expected green, per §4.
3. Same flags, `:app:bundleRelease` → `app/build/outputs/bundle/release/app-release.aab`,
   signed with the upload key already configured and gate-checked.

Unchanged and still open beyond this task: Play App Signing enrolment, appending Play's
signing SHA-256 to `assetlinks.json`, the Play Console declarations, and the
physical-device internal-track pass.

## Scope

No source changes; no commit, push, deploy or upload. `app/build.gradle.kts` remains
modified in the working tree from the 00:06 session, untouched here. No credentials
read out, created or rotated; the keystore was not opened at all this session (only its
existence checked, by the gate). Nothing outside `D:\ArunApps\Chess-Web` was modified.
All three Gradle runs were single-use `--no-daemon`; no java process was left running
(verified: zero `java.exe` afterwards), and no emulator or server was started.
