# 2026-09-14 01:45 — Stop tracking generated `bootstrap/cache` files

## Task
Stop committing Laravel's generated `chess-backend/bootstrap/cache/{packages,services}.php`.
Found on the VPS in `docs/updates/2026_09_14_00_10_update.md`: every
`composer install --no-dev` rewrites them (+22/−53 at `568e46c`), so a later
`git pull` that touches them fails.

## Change (staged, not committed)
- `git rm --cached chess-backend/bootstrap/cache/packages.php services.php`.
  The files stay on disk locally.
- New `chess-backend/bootstrap/cache/.gitignore` containing `*.php`. It also
  covers `config.php`, `routes-v7.php` and `events.php` from `artisan optimize`.
  The `.gitignore` itself is not ignored, so the directory stays in git.
- `scripts/deploy-notes.md` step 1 now has the one-time `git checkout --` of the
  two files before `git pull`.

`git diff --cached --stat`: `.gitignore` +1, `packages.php` −93, `services.php` −282.

## Verification
- `git check-ignore -v` matches `packages.php`, `services.php`, `routes-v7.php`
  and `config.php` against the new `.gitignore:1:*.php`. `git status` does not
  list the on-disk files.
- **Regeneration:** I moved both files out of the tree and ran
  `php artisan package:discover` (rc=0). Laravel recreated both, and `cmp` shows
  they are byte-identical to the removed copies. On the VPS, composer's
  `post-autoload-dump` runs this step, and so does a request when the files are
  missing.
- **VPS pull scenario**, reproduced in a throwaway sparse clone (bare origin
  seeded from local `master`, deleted afterwards):
  - "VPS" clone at `daf9405` with both files modified. After this change was
    pushed to the origin, `git pull --ff-only` failed with *"Your local changes
    to the following files would be overwritten by merge"*, rc=1.
  - After `git checkout --` of the two files, the pull worked (rc=0). The files
    were removed from the working tree, and a regenerated `packages.php` left
    `git status` clean.
- CI `deploy.yml` uses `git fetch` + `git reset --hard origin/master`, so it
  does not hit this failure. Only the manual `git pull` procedure needs the
  cleanup.
- Backend suite `php artisan test`: rc=0, **356 passed, 158 skipped, 1 risky**
  (4458 assertions, 75 s).

## Not done
- Nothing was committed, pushed or deployed. The VPS was not touched.
- The server-side `git checkout --` is still a deploy-time action for
  ServerMigrationAgent/owner. It is in `scripts/deploy-notes.md` step 1.
