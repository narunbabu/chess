# 2026-07-14 08:00 — Android Play Store release blockers fixed

## Context
Play-readiness audit found the release build blocked (R8 failure, lint error,
missing WS key, Razorpay policy risk, broken assetlinks). Decisions locked:
no in-app purchases in v1 (Play Billing in v1.1), keep Facebook login,
target audience 13+ (listing targets parents).

## Changes — chess99-android

- **R8 fix**: `-dontwarn org.slf4j.impl.StaticLoggerBinder` in
  `proguard-rules.pro` (pusher-java-client's slf4j 1.x lookup; NOP fallback).
- **Facebook login wired**: real App ID `1696238708465565` +
  `fb1696238708465565` scheme in `strings.xml` (fixes the `AppLinkUrlError`
  release-lint error). Client token still TODO (Meta console → Advanced →
  Security). Removed redundant `FacebookActivity` label (lint warning).
- **Razorpay removed for Play policy compliance** (digital goods must use
  Play Billing; link-outs are anti-steering violations):
  - Deleted `PricingScreen.kt`, `RazorpayCheckout.kt`; removed
    `com.razorpay:checkout` dependency + proguard rules.
  - `PaymentApi` → only `getSubscription`/`cancelSubscription`.
  - `PaymentViewModel` → status/cancel/restore only
    (`isProcessingPayment` → `isProcessing`).
  - `SubscriptionScreen` → status-only; neutral "purchases not available in
    this app" card, no upgrade CTAs.
  - `DailyChallengesScreen` locked-track card: informational, no "View Plans".
  - Drawer: "Pricing & Plans" → "My Plan" (Subscription). `Screen.Pricing`
    and the `/pricing` deep link removed (pricing stays in the browser).
- **CRITICAL fix — prod endpoints**: release `API_BASE_URL`/`WS_HOST` pointed
  at `chess99.com`, which serves the SPA for `/api/*` (returns HTML). Both now
  point at `api.chess99.com` (verified against the live web bundle + curl).
- **WS key**: `WS_KEY_RELEASE=anrdh24nppf3obfupvqw` (public prod Reverb key,
  same as dev) added to `C:\Users\ab\.gradle\gradle.properties`.

## Changes — chess-frontend

- Added `public/.well-known/assetlinks.json` (package `com.chess99.app`,
  upload-key SHA-256) for Android App Links. Needs frontend deploy + nginx
  dot-directory check; Play App Signing fingerprint to be appended after
  Play Console enrollment.

## Gate results

- `gradlew assembleRelease` ✅ (signed, 4.79 MB, R8+shrink on)
- `gradlew lintRelease` ✅ 0 errors (was 1), 59 warnings (was 61)
- `gradlew bundleRelease` — AAB for Play upload

## Follow-ups
See `docs/android-play-store-release-plan.md` (Phases 2–6): Facebook client
token + console platform entry, assetlinks deploy via SMA, store assets,
Play Console setup/declarations, post-enrollment fingerprint registration
(Firebase + assetlinks + FB key hash), device verification.
