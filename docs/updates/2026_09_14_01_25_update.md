# 2026-09-14 01:25 — `:app:connectedDebugAndroidTest -PDEBUG_TARGET=prod` on the phone: blocked, no device

**Result: BLOCKED — no phone connected.** Nothing was installed and no instrumented test ran.
No code changed. HEAD `daf9405` (Android sources identical to deployed `568e46c`).

## Device discovery (this session)

| Check | Result |
|-------|--------|
| `adb start-server` then `adb devices -l` (SDK `platform-tools`), re-checked after 8 s | empty list |
| `adb mdns services` (Wi-Fi debugging) | none discovered |
| `Get-PnpDevice -PresentOnly` filtered for Android/ADB/MTP/phone vendors, `AndroidUsbDeviceClass`, `WPD` | only the `Expansion` and `ExtremeSSD` storage volumes plus a Bluetooth phonebook profile; no Android USB interface |

Unlike 2026-09-13, the VidyaPath `emulator-5580` was not attached either. So Gradle had
nowhere to install except a physical phone, and it was safe to run.

## Command run

From `chess99-android/`:

```
./gradlew.bat :app:connectedDebugAndroidTest -PDEBUG_TARGET=prod --no-daemon --console=plain --max-workers=2
```

- rc=1, `BUILD FAILED in 1m 16s`, 76 actionable tasks: 4 executed, 72 up-to-date.
- `kspDebugAndroidTestKotlin`, `compileDebugAndroidTestKotlin` and
  `transformDebugAndroidTestClassesWithAsm` ran with no compiler errors or warnings in the
  log. The test APK packaging was up-to-date. So the instrumented suite
  (`StockfishEngineInstrumentedTest`, 9 tests) **compiles** against the prod-target debug build.
- Failure: `Execution failed for task ':app:connectedDebugAndroidTest'. >
  com.android.builder.testing.api.DeviceException: No connected devices!`

## What still needs the owner

1. Connect an arm64 Android 8.0+ phone over USB with USB debugging on, and accept the RSA prompt.
   Check that `adb devices` shows exactly that serial as `device`. Disconnect or stop any emulator
   first, because Gradle installs on **every** attached device.
2. From `chess99-android/`, run the command above. The task installs `com.chess99.app` (debug
   signing). If an existing install has a different signature, **stop and report**. Do not
   uninstall, because that erases local data.
3. Results: `app/build/reports/androidTests/connected/debug/index.html` and
   `app/build/outputs/androidTest-results/connected/debug/`.

## Cleanup

`adb kill-server` run. `--no-daemon` left no Gradle daemon. The temporary run log was deleted.
The `ADBCSvc`/`ADBCBg` (Acer) and VS Code Java processes were pre-existing and not touched.
