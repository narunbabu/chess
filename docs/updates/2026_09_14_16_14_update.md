# Signed-in PlayMultiplayer root leave: device verification blocked

## Outcome

The requested device check is **not completed**. The previous session's root-leave
implementation remains unchanged. No commit, push, deployment, account changes,
or live game actions were performed.

## Checks in this session

- Inspected the working tree, STATUS.md, prior 15:45 evidence, MainActivity,
  NavGraph, DeepLinkHandler, and MainNavigationTest.
- `powershell.exe -Command "cd chess99-android; ./gradlew.bat --no-daemon :app:testDebugUnitTest --tests com.chess99.presentation.navigation.MainNavigationTest"`
  failed before running tests: Gradle wrapper attempted a distribution download
  and Java reported `java.net.SocketException: Permission denied: getsockopt`.
  A Gradle distribution exists in the user's global cache, but this run did not
  use it. The old XML report is dated 2026-09-14 15:35:48; it is not a new pass.
- Android SDK ADB `start-server` succeeded; `adb devices -l` returned an empty
  device list. The server was stopped in a finally block.
- No emulator was running. Existing AVD data is under
  `C:/Users/ab/.android/avd/Medium_Phone.avd`, outside this task's allowed write
  scope. That AVD was not launched or modified. No project-local AVD was prepared.
- No app or test code was changed. The two existing signed-in screenshots from
  the previous attempt do not constitute a recorded root-leave pass.

## Exact remaining device acceptance

Use a writable test emulator/device and the current debug APK, with an authorized
test account and a game belonging to that account. Preserve existing app data.

1. While signed out, open `chess99://game/<authorized-test-game-id>` and sign in.
   The login callback consumes the pending route and uses inclusive `popUpTo(0)`;
   PlayMultiplayer is therefore the root. Confirm that the game screen loads.
2. Tap toolbar Back, then confirm **Leave game** if prompted. Verify that Home
   appears with signed-in navigation, rather than Login or an unchanged game.
3. Press system Back from Home. Verify that it does not reveal the departed game.
4. Repeat through the persona handoff from a root PlayComputer if that path is
   available in the test setup. Record its actual back-stack precondition.
5. Capture the game/root setup, leave confirmation, Home, and Back result. Re-run
   MainNavigationTest using the project Gradle wrapper in a build-capable session.

Opening a deep link while **already signed in** is not sufficient to verify the
root fallback: MainActivity navigates without clearing Home, so leave can succeed
by ordinary pop. In the code reviewed, PlayMultiplayer uses
`popBackStackOrExitTo`, and MainActivity supplies a callback that re-reads login
state via `coldStartDestination`. This supports the intended behavior but is not
device evidence.

## Cleanup

ADB was stopped. No emulator, dev server, watcher, or Gradle daemon was left
running by this session. The two Java processes observed were pre-existing VS Code
Java tooling (started 11:04 and 11:10), and were left alone.
