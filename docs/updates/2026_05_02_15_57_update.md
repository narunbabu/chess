# Mobile Play Controls Redesign Follow-up

## Summary
- Promoted mobile play actions into always-visible controls: pause/resume, chat, undo, CCT, companion, moves, help, and resign.
- Added a compact last-move strip for mobile so the move drawer can stay hidden without losing immediate move context.
- Updated mobile landscape handling to apply up to 1400px wide short viewports, matching larger mobile-landscape preview widths.
- Added fallback named companion bots and auto-select the closest stronger coach for casual play.
- Updated mobile Playwright coverage for persistent controls, CCT drawer access, hidden moves, last moves, and default companion selection.

## Verification
- `pnpm build` passed.
- `pnpm test:e2e -- tests/e2e/mobile-play-layout.spec.js --project=chromium` passed (10 browser/project runs).
