# Android/web Review and lifeline parity — 2026-09-15

## Outcome

Closed the remaining parity item from `STATUS.md`:

- Casual and learning Android multiplayer now exposes a live Review switch.
- Each enabled player move is reviewed asynchronously against Stockfish and the latest top-five alternatives/rank are shown without delaying move submission.
- Review and Best markers travel in the multiplayer move payload, survive the backend compact-history representation, and are parsed by Android for both live history and Game Review.
- Android Game Review now shows per-move lifeline badges and a total-by-type lifeline summary. Local computer-game review records Best markers as well.

## Verification

- Frontend: `pnpm test -- --watchAll=false --runInBand` — 14 suites, 116 tests passed.
- Frontend: `pnpm build` — compiled successfully.
- Backend: `php artisan test` — 400 passed, 158 skipped, 1 risky, 0 failed.
- PHP syntax: `php -l` passed for the changed controllers; `git diff --check` passed.
- Android: the direct cached Gradle 8.11.1 binary reached plugin resolution, but the offline environment lacks the cached Android Gradle Plugin 8.9.1 marker. No Android source compilation or device test result is claimed from this session.

No commit, push, deploy, or device-side acceptance was performed.
