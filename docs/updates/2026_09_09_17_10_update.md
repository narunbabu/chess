# 2026-09-09 17:10 — Delivery closed out: Android gates + phone APK (resume of a timed-out dispatch)

## Context

A previous dispatch delivered ten commits (`2359d3a..a18c036`) and passed all
web/backend gates, but was killed by its timeout before the Android gates, the
STATUS.md commit and this record. Today's session resumed exactly there; nothing
already done was redone (HEAD stayed `a18c036`, working tree had only STATUS.md
modified).

## The ten delivered commits (already landed, not repeated today)

| SHA | Subject |
|-----|---------|
| `0dda391` | Takeback event-name parity plus bot auto-accept and request expiry |
| `1898541` | Promotion dialog and accessible move entry |
| `efd5479` | Sounds |
| `9812378` | Learning Best-move budget and offline game review |
| `18d3302` | Logout session clearing |
| `da8765c` | Bottom navigation |
| `0891010` | Daily streak |
| `d2e1840` | Web navigation and game-end experience |
| `7e83b4d` | Docs |
| `a18c036` | Opencode config |

## Gate results

### Already passed in the previous dispatch (recorded, not re-run)

- `chess-frontend pnpm build` — **PASS**
- `chess-frontend pnpm lint` — **PASS** (0 errors, 303 warnings)
- `chess-frontend pnpm typecheck` — **PASS**
- Frontend unit tests — **PASS** (12 suites, 103 tests)
- `chess-backend php artisan test` — **PASS** (356 passed, 158 skipped, 1 risky, 4458 assertions)
- `php artisan migrate --pretend` — **PASS** (exit 0; only pre-existing pending migrations)

### Android (run today, 2026-09-09, PowerShell, `chess99-android/`)

1. **`gradlew.bat test -PDEBUG_TARGET=prod` → FAILED before any test executed.**
   The release-variant unit tests require `preReleaseBuild`, which depends on the
   deliberate `verifyReleaseConfiguration` gate, and that gate fails on the
   known owner-credentials blocker (`app/google-services.json` is still the
   placeholder; replacing it is forbidden to agents and tracked in STATUS.md).
   Verbatim:

   ```
   > Task :app:verifyReleaseConfiguration FAILED

   FAILURE: Build failed with an exception.
   * What went wrong:
   Execution failed for task ':app:verifyReleaseConfiguration'.
   > Release configuration is incomplete:
     - app/google-services.json is still the placeholder config. FCM, Crashlytics, and Analytics would be inert.
   ...
   BUILD FAILED in 35s
   ```

   This is a pre-existing release-config gate, not a test or code regression —
   no test ran, nothing was weakened or skipped.

2. **`gradlew.bat testDebugUnitTest -PDEBUG_TARGET=prod --console=plain --max-workers=2` → PASS**
   (exit 0, 15m 35s). Counts parsed from the JUnit XML in
   `app/build/test-results/testDebugUnitTest/` (written 2026-09-09 16:38):
   **24 suites, 238 tests, 0 failures, 0 errors, 0 skipped** — identical to the
   2026-09-08 baseline. This is the repo's documented test gate
   (`docs/updates/2026_09_08_android_phone_test.md`); it runs the full debug
   unit-test suite and does not touch the release-only config gate.

3. **`gradlew.bat lint -PDEBUG_TARGET=prod --console=plain --max-workers=2` → PASS**
   (exit 0, 1m 21s; resolves to `:app:lintDebug`). Because Gradle initially
   satisfied `lintReportDebug` from an input-identical up-to-date check, the
   report was then regenerated with a forced full rerun
   (`lintDebug --rerun-tasks`, exit 0, 19m 47s, 33/33 tasks executed, report
   timestamp 2026-09-09 17:01). Counts from `lint-results-debug.xml`:
   **0 errors, 12 warnings, 69 informational** — same composition as the
   baseline: 6× `UseKtx`, 5× `ModifierParameter`, 1×
   `CredentialManagerSignInWithGoogle` (warnings); 66× `GradleDependency` +
   3× `AndroidGradlePluginVersion` (informational, deliberately demoted — see
   `docs/updates/2026_08_21_android_dependency_upgrade_decision.md`).

4. **`gradlew.bat assembleDebug -PDEBUG_TARGET=prod --console=plain --max-workers=2` → PASS**
   (exit 0, 15s). Output:
   - Gradle output path: `D:\ArunApps\Chess-Web\chess99-android\app\build\outputs\apk\debug\app-debug.apk` — **27.74 MB**
   - Dated repo-root copy (per the phone-test convention; `*.apk` is gitignored):
     `D:\ArunApps\Chess-Web\chess99-android\chess99-1.0.0-phone-test-2026-09-09.apk` — **27.74 MB**
   - Debug-signed, all three ABIs, prod backend target; supersedes
     `chess99-1.0.0-phone-test-2026-09-08.apk` (same code — the ten commits
     committed the already-verified working tree; the byte sizes match).

## STATUS.md

Committed in this session (only dirty path; `.karta/` brief left untracked).
`Done recently` now leads with a 2026-09-09 entry summarizing this delivery and
gates; the `[needs a phone]` TODO points at the 2026-09-09 APK.

## Not done / open

- Plain `gradlew test` cannot pass until the owner lands a production
  `app/google-services.json` (STATUS.md `[needs owner credentials]` item).
- Nothing pushed, merged or deployed; no keystore or `google-services.json`
  touched; no tests weakened or deleted.
