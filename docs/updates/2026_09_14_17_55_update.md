# 2026-09-14 17:55 — Android device check: signed-in root leave -> Home, multiplayer Best toggle

## Outcome

Both open device checks **pass** on an emulator against the local backend, using the
committed debug APK (`:app:assembleDebug` offline: BUILD SUCCESSFUL, 43/43 tasks
up-to-date against `9a134c5`). No source, test or config changes. No commit, push,
deploy, or production traffic: the debug build targets `http://10.0.2.2:8000`.

| Check | Result |
|---|---|
| Signed-out `chess99://game/463` -> Login -> sign in -> PlayMultiplayer as root | PASS: game opens directly after login |
| Root toolbar Back -> "Leave the game?" -> **Leave game** | PASS: signed-in **Home** (Play/Learn/Compete/You tabs, Menu) |
| System Back from that Home | PASS: goes to the launcher; the departed game is not shown |
| Casual game (463): Best button shown | PASS: Draw / Best / Pause / Undo 9 / Resign |
| Best on | PASS: button highlighted, 3 engine arrows (e2-e4, d2-d4, g1-f3); Undo stays 9 |
| Best off | PASS: arrows cleared |
| Rated game (464): Best absent | PASS: header "Rated • 10\|0", controls are only Draw and Resign (no Best, Pause, Undo) |

Screenshots (this folder): `2026_09_14_devicecheck_signedin_root_1c_creds_filled.png`,
`_root_2_after_login.png`, `_best_1_on.png`, `_best_2_off.png`,
`_root_3_leave_dialog.png`, `_root_4_after_leave.png`,
`_root_5_after_system_back.png`, `_rated_1_game464.png`.

Not covered: the persona-handoff root path (PlayComputer -> PlayMultiplayer) and
production backend/WebSocket behaviour. Unit tests were not re-run; no code changed
since the 17:24 run (251 tests, 0 failures).

## Setup (all project-local, all removed afterwards)

- Backend: `php artisan serve --host=127.0.0.1 --port=8000` and
  `php artisan reverb:start` from `chess-backend` (local SQLite, gitignored), detached,
  logs in `logs/devicecheck-*.log`. The test account exists locally (user 29). Games were
  created through the local API as that user: `POST /api/v1/games/computer`, casual
  463 (synthetic player 45) and rated 464 (synthetic player 46). Local DB only; both
  games remain `ongoing` there.
- Emulator: throwaway AVD in `chess99-android/build/avd-devicecheck` (android-35
  google_apis x86_64 image, read-only use), with `ANDROID_AVD_HOME`/`ANDROID_USER_HOME`
  project-local. A private adb server ran on port 5038, so the existing 5037 server and
  the user AVDs were left alone.
- Boot: the first three attempts died silently (launcher + SwiftShader with 3 GB/4
  cores, auto GPU, then SwiftShader with 2 GB). Free host commit memory was about
  4-7.5 GB. The fourth boot worked by running `qemu-system-x86_64-headless.exe`
  directly with `-gpu host -feature -Vulkan -memory 2048 -cores 2` (boot in 86 s).
- One run was voided by the tester, not the app: an ESC keyevent sent to hide the
  keyboard acted as Back on the root Login screen. The crash buffer was empty and no
  login request was sent. It was re-run without ESC.

## Observations, not investigated

- After leaving, Home showed "Couldn't check for games to resume". Re-checked
  directly afterwards: `GET /games/unfinished` and `/games/active` both returned 200. It
  was probably a timeout on the slow emulator (System UI also ANR'd once at boot), but
  this is unconfirmed.
- The WebSocket dot stayed orange (not connected to local Reverb). The app fell back
  to polling `websocket/room-state` about every 3.5 s.
- The leave dialog says leaving "counts as a loss", but casual bot game 463 was still
  `ongoing` in the local DB afterwards. This may be deliberate (resolved later by
  abandonment/timeout); not checked.

## Cleanup

App force-stopped, emulator killed (`emu kill`), private adb server killed, php serve
and Reverb process trees terminated, and the throwaway AVD folder deleted. Afterwards
nothing listens on 8000/8080/5038/5584. The remaining java/adb(5037) processes predate
this session.
