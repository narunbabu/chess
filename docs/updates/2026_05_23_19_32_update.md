# Casual Undo Usage Tracking Update

## Summary

- Tripled casual undo allowances:
  - Computer easy/medium/hard/expert tiers now allow 15/9/6/3 undos.
  - Multiplayer casual games now allow 9 undos on both frontend and backend enforcement.
- Added successful Undo usage tracking beside existing Best usage tracking.
- Added the end-card sentence format:
  - `Usage: Best X times and Undo Y times`
- Persisted `undo_button_uses` inside `review_report.summary` and `review_summary` JSON so the database retains the data for later performance analysis without a schema migration.

## Verification

- `pnpm test --watchAll=false`: passed, 78/78 tests across 9 suites.
- `pnpm build`: passed, production build compiled successfully.
- `pnpm lint`: passed with existing warnings, 0 errors.
- `pnpm typecheck`: passed, script maps to production build and compiled successfully.
- `php artisan test`: passed, 262 passed, 158 skipped, 1 existing risky warning.
- `php artisan migrate --pretend`: passed, dry run completed.
- `pnpm exec playwright test tests/e2e/mobile-play-layout.spec.js --project='Mobile Chrome' --reporter=line`: passed, 3/3.
- Full `pnpm test:e2e` did not complete locally: current Playwright config expands to 575 tests across 5 browser projects and exceeded 30 minutes without returning a result.
