# 2026-09-14 18:59 — Android device check: persona-handoff root path (PlayComputer -> PlayMultiplayer) leave

## Outcome

**The literal root case is unreachable in the current app. The reachable handoff passes.**
Checked on a throwaway emulator against the local backend, with the debug APK built at
18:05. That build includes the uncommitted 18:07 `games/unfinished` fix. No source,
test or config changes. No commit, push, deploy or production traffic: every request
went to local `php artisan serve`.

### Why a *root* PlayMultiplayer via persona handoff cannot happen today

- PlayComputer is the back-stack root only after Onboarding -> **Try as guest**
  (`NavGraph.kt` `popUpTo(Onboarding) inclusive`). Home, Dashboard and Login's
  "Play as Guest" all push it on top of an existing entry.
- The persona row needs `GET /api/v1/synthetic-players`. The start needs
  `POST /api/v1/games/computer`. Both sit inside `auth:sanctum` (`routes/api_v1.php`).
  Without a token, both return **401** (curl, local).
- A guest therefore gets no persona chips and cannot start a server game. The handoff
  only runs signed in, and signed in PlayComputer always has Home below it. The
  handoff's `popUpTo(PlayComputer) inclusive` leaves Home as PlayMultiplayer's
  previous entry, so Leave takes the ordinary pop, not `rootExitDestination`.

The `rootExitDestination` branch for PlayMultiplayer is still exercised by the
deep-link root path, device-checked PASS at 17:55.

| Check | Result |
|---|---|
| Fresh install -> Onboarding -> Try as guest -> root PlayComputer | PASS: "Game Setup" with **no persona row**. The app called local `synthetic-players`, and the same call returns 401 without a token. |
| Guest root PlayComputer (setup) -> toolbar Back | PASS: Login (onboarding seen, not signed in). No dialog in setup. |
| Sign in -> Home | PASS: Home with "Continue playing". **No "Couldn't check for games to resume" banner.** `games/unfinished` was called. |
| Home -> Play computer -> persona **Riya First Moves**, Casual -> Start Game | PASS: handoff to PlayMultiplayer. Local game **465** created (casual, synthetic player 45). Header "Playing Riya First Moves / Casual • 10\|0", controls Draw / Best / Pause / Undo 9 / Resign. |
| Toolbar Back -> "Leave the game?" -> **Leave game** | PASS: signed-in **Home** (Play/Learn/Compete/You). Not the setup form, so PlayComputer was popped. |
| System Back from that Home | PASS: launcher (`NexusLauncherActivity` focused). The departed game is not shown. |

Back-stack precondition, as recorded: Home -> PlayComputer -> (handoff) Home -> PlayMultiplayer(465).
Leave pops to Home, and Back from Home exits.

### Observations, not fixed

- The leave dialog again says "Your game is still going. If you leave now, it counts as
  a loss." for a **casual** game. After Leave the only requests were Home's own loads
  (`games/unfinished`, `user`, `games/active`, `lobby/players`), with no pause or resign.
  Game 465 stays `ongoing`, `paused_at` null. This matches the 18:31 decision and its open
  TODO (casual Leave should call `pauseNavigation`; fix the copy).
- Home's "Continue playing" showed two 58/59-day-old "vs Opponent" games. It did not
  show bot games 463/464/465, which are all `ongoing`. Not investigated: this may be the
  endpoint's filtering, not the client. The TODO "lists paused games" half stays open;
  no paused game was created.
- The WebSocket was not connected (Reverb not started). The app polled
  `websocket/room-state` every ~3.5 s, as at 17:55.

### Tester errors (voided, re-run)

- The first Start Game tapped no persona: my script picked the heading "Game Setup" as
  the persona name. That started a local Stockfish game ("Computer (Lv.2)", no server
  row, max game id stayed 464). Leaving it went to Home (ordinary pop). The run was
  repeated by tapping the persona by name.

Screenshots (this folder, `2026_09_14_devicecheck_persona_*`): `0_launch`,
`1_guest_root_playcomputer`, `2_guest_root_after_back`, `3_login_filled` (password
masked), `4_home_after_login`, `5_signedin_setup_personas`, `6_persona_selected`,
`7_after_handoff`, `8_leave_dialog`, `9_after_leave`, `10_after_system_back`.

## Setup

- Backend: `C:\php\php.exe artisan serve --host=127.0.0.1 --port=8000`, detached,
  logs in `logs/devicecheck-persona-serve.*.log`. Local SQLite, test user 29.
- Emulator: throwaway AVD `persona_check` (android-35 google_apis x86_64, 2 GB,
  2 cores) in `chess99-android/build/avd-persona`, private adb server on 5038.
- Boot problems, for the next run:
  1. Running `qemu-system-x86_64-headless.exe` directly exits at once with **no output**
     unless `emulator\lib64`, `lib64\gles_swiftshader` and `lib64\vulkan` are on `PATH`.
     The launcher adds them; a bare `Start-Process` does not.
  2. Through `emulator.exe`, qemu died after "Activated packet streamer for bluetooth
     emulation". `netsimd.exe` crashed at 18:45:31 (0xc0000409 in ucrtbase, Application
     event log).
  3. The boot that worked was direct qemu with those `PATH` entries and
     `-no-window -gpu host -feature -Vulkan -memory 2048 -cores 2 -no-metrics`.
     `boot_completed` came about 30 s after adb saw it.

## Cleanup

App force-stopped, `emu kill`, private adb server killed, php serve tree terminated,
and `chess99-android/build/avd-persona` deleted (AVD plus the helper `ui.sh`/`db.php`).
Afterwards nothing listens on 8000/5038/5554 and no qemu/emulator/netsim/php process remains.

**Over-broad stop, flagged for the owner:** the name-based process sweep also force-stopped
`emulator.exe` 175628 and two `crashpad_handler` processes. It tried qemu 42640, which
refused with Access denied and exited shortly after. None of these existed at 18:46,
and my only later launch was qemu 63180. No AVD under `%USERPROFILE%\.android\avd`
was touched after 18:40. Their owner could not be determined. If another session was
running an emulator around 18:50–19:00, it was stopped by this run.
