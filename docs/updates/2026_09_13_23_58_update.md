# 2026-09-13 23:58 — Physical-device Android walkthrough: blocked, no phone attached

## Task
Run the physical-device acceptance walkthrough from `STATUS.md` Next / TODO:
side-load `chess99-android/chess99-1.0.0-phone-test-2026-09-13.apk`, follow
`chess99-android/PHONE_TEST.md` (onboarding/first-use tour, Casual White/Black,
Learning/Undo/review, TalkBack + large font, sign-in tabs and Back-stack,
two-client online game, sign-out + restart), and run
`:app:connectedDebugAndroidTest -PDEBUG_TARGET=prod` on arm64.

## What was checked
- `adb devices -l` (23:48) listed exactly one device:
  `emulator-5580 … model:sdk_gphone64_x86_64`. **No physical phone is connected.**
- `Get-PnpDevice -PresentOnly` shows no Android/ADB/MTP USB device.
- `emulator-5580` is not a Chess99 device. It is AVD `VidyaPath_Release_QA`,
  and at the time it was running another project's live instrumentation
  (`com.vidyapath.app.a11y.TalkBackLiveFlowTest`). This session did not install
  on it or drive it, and did not restart the shared adb server (pid 133700),
  because doing so would break that run.
- Handoff APK integrity: `sha256sum chess99-1.0.0-phone-test-2026-09-13.apk` =
  `3184938edc20dcf8a5808859c43811fffbf63e848b5f03525454426a8273ee88`, which matches
  the value recorded in `2026_09_13_23_10_update.md`. It packages `arm64-v8a`,
  `armeabi-v7a` and `x86_64` native libs, so it runs on an arm64 phone as it is.

## Results
None. No walkthrough step, TalkBack/font check, two-client test or
`connectedDebugAndroidTest` run happened, and no results are claimed.

`.tmp-walkthrough/` (untracked, 21:44–22:50 today) holds artifacts from an
earlier x86_64 emulator session (`installed.apk`, `login.png`, TalkBack helper
scripts). No findings from that session were written up, so nothing from it is
counted as acceptance evidence here.

## Owner action to unblock
1. Connect an arm64 Android 8.0+ phone with USB debugging on, and accept the RSA prompt.
2. `adb -s <serial> install -r chess99-android/chess99-1.0.0-phone-test-2026-09-13.apk`.
   If the install reports a signature conflict, stop. Do not uninstall to get around it.
3. Follow `chess99-android/PHONE_TEST.md`, then from `chess99-android/`:
   `./gradlew.bat :app:connectedDebugAndroidTest -PDEBUG_TARGET=prod --no-daemon`
   (choose the device with `ANDROID_SERIAL=<serial>` when an emulator is also attached).
4. The two-client leg also needs the second prod test account (see the takeback TODO).

No code changed. Nothing was installed, committed, pushed or deployed.
