# 2026-09-14 00:10 — VPS service state and migrate:status re-checked

## Task
Re-check server-side service state and `php artisan migrate:status` over SSH.

## What was run (read-only)
SSH `narun@69.62.73.225`, key `~/.ssh/narun_vps_ed25519`, `BatchMode=yes`.
VPS clock: 2026-09-13 18:37 UTC, up 45 days, load 0.07. Disk `/` 51% used (48G free).

### Code
- `/opt/Chess-Web` is on `master` at `568e46c`, the deployed commit.
- The working tree is **not clean**. Tracked files
  `chess-backend/bootstrap/cache/packages.php` and `services.php` are modified
  (+22/−53). `routes-v7.php` is untracked. All three were written at
  2026-09-13 14:56–14:57 UTC, during the deploy's `composer install --no-dev`
  and route caching. These are generated caches that are also committed in git.
  A future `git pull` that touches either file will refuse to merge.

### Services (`systemctl`)
| Unit | State | Since (UTC) | Restart policy |
|---|---|---|---|
| chess-reverb | active/enabled | 2026-09-13 14:58:40 | on-failure, NRestarts=0 |
| chess-queue | active/running | 2026-09-13 18:20:42 | always, NRestarts=60 |
| php8.3-fpm | active/enabled | 2026-09-11 06:14:24 | — |
| nginx | active/enabled | 2026-09-11 06:14:24 | — |
| mysql | active/enabled | 2026-09-11 06:14:40 | — |
| redis-server | active/enabled | 2026-09-11 06:14:24 | — |

- `chess-queue` NRestarts=60 is expected. The worker runs with
  `--max-time=3600`, so it exits every hour and systemd restarts it. About 60
  hours have passed since 09-11 06:14, which matches.
- There are two `queue:work` processes. Their cgroups show one belongs to
  `chess-queue.service` (PID 2315192, cwd `/opt/Chess-Web/chess-backend`). The
  other (PID 2315186) belongs to `thogata-queue.service`, a different app. So
  Chess99 has exactly one worker.
- Reverb runs as `www-data` with `artisan reverb:start --host=0.0.0.0 --port=8080`.
- Listening ports: 80, 443, 8080 (Reverb) and 3306 and 6379 (both on localhost only).
- `systemctl --failed` lists only `fwupd.service`, the firmware updater. It is
  not part of Chess99.
- `journalctl -u chess-reverb` returned nothing, because user `narun` is not in `adm` or
  `systemd-journal`. Reverb logs were **not** inspected.
- No supervisor socket; workers are managed by systemd, not supervisor.

### Queue
Checked via `php artisan tinker --execute`: `failed_jobs=0`, `last_failed=none`,
`jobs_queued=0`.

### Migrations
- `php artisan migrate:status --no-ansi` → rc=0. The last rows:
  ```
  2026_05_20_000001_create_user_move_review_stats_table ............. [45] Ran
  2026_07_06_100000_add_chat_safety_controls ........................ [46] Ran
  2026_07_06_110000_create_guardian_child_relationships_table ....... [46] Ran
  2026_07_10_100000_add_age_safety_and_guardian_consent_fields ...... [46] Ran
  2026_08_29_100000_create_tester_program_tables .................... [46] Ran
  ```
- `php artisan migrate:status --pending` → rc=0, `INFO  No pending migrations.`

### Public health (from this laptop)
- `https://chess99.com` → 200
- `https://api.chess99.com/up` → 200
- `https://api.chess99.com/api/v1/health` → 200, `"status":"healthy"`, and
  websocket, push, and Google and Apple sign-in are all reported `true`

## Result
**Healthy.** All Chess99 services are active, and the migrations match the
2026-09-14 00:05 check: batch 46 is the latest and nothing is pending. No jobs
have failed and the queue is empty.

One issue found: the server checkout has uncommitted changes to generated
`bootstrap/cache` files, and that could block the next `git pull` deploy.

Nothing was migrated, written, restarted or deployed on the server. No code changed.
