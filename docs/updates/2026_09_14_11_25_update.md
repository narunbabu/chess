# 2026-09-14 11:25 — "At next manual deploy run deploy-notes step 1 and verify bootstrap/cache is clean"

## Ruling

**Still blocked on the owner: the manual deploy has not happened, so step 1 was not run and the
`bootstrap/cache` check fails as expected pre-deploy, not as a regression.** Running
deploy-notes step 1 (`git checkout -- chess-backend/bootstrap/cache/{packages,services}.php` +
`git pull origin master` + recovery fallback on the VPS) is a deploy action, forbidden by this
session's no-deploy rule — the same ruling as 09:48, 10:20 and 11:13. It would also be premature:
with `origin/master` unmoved, the pull brings zero commits and the checkout discards composer's
rewrites for nothing. The owner wrapper `scripts/vps-deploy-step1.sh` (delivered 10:20) contains
a premature-run guard for exactly this case and exits nonzero without touching the tree.

## Verified read-only (2026-09-14 11:25)

Local, after `git fetch origin`:
- `origin/master` = `568e46c` — the owner push of the 6 local commits (head `690f348`, `[skip ci]`)
  has **not** happened; local master 6 ahead / 0 behind (fast-forward).
- Latest `deploy.yml` run still the failed 2026-09-13 `34764068752` (`gh run list`, head `568e46c`) —
  no CI deploy either.

VPS over SSH (`ssh -i ~/.ssh/narun_vps_ed25519 narun@69.62.73.225`, read-only):
- `HEAD` = `568e46c`, `origin/master` ref = `568e46c` — no pull has run there.
- `git status --short -- chess-backend/bootstrap/cache`:
  ```
   M chess-backend/bootstrap/cache/packages.php
   M chess-backend/bootstrap/cache/services.php
  ?? chess-backend/bootstrap/cache/routes-v7.php
  ```
  Not clean — the documented expected pre-deploy state (`d48a4b4` untracking not yet deployed).

External:
- `https://api.chess99.com/up` → **200**.

Nothing was pushed, committed, or changed on the VPS.

## Owner steps (unchanged)

1. `git push origin master` (head `690f348` carries `[skip ci]`, so it will not fire `deploy.yml`).
2. Step 1 of the manual deploy, one command from the laptop:
   ```
   ssh -i ~/.ssh/narun_vps_ed25519 narun@69.62.73.225 'bash -s' < scripts/vps-deploy-step1.sh
   ```
   (exits 0 only when the cache status is empty and `/up` is 200), then steps 2-7 via
   `scripts/vps-deploy-steps2-7.sh` (`CHESS99_SKIP_FRONTEND=1` — no frontend files in the 6 commits).
3. After it, the read-only check to expect clean:
   ```
   ssh -i ~/.ssh/narun_vps_ed25519 narun@69.62.73.225 'cd /opt/Chess-Web && git status --short -- chess-backend/bootstrap/cache'
   curl -s -o /dev/null -w "%{http_code}" https://api.chess99.com/up
   ```
