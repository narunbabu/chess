# 2026-09-14 10:20 — deploy-notes step 1 "at the next deploy": still no deploy; step 1 packaged as a guarded one-command wrapper

Task: "Run deploy-notes step 1 (VPS bootstrap/cache cleanup) at the next deploy."

## Outcome

**Blocked — no deploy has happened, so step 1 was not run on the VPS.** Same
ruling as the 01:55, 02:05, 09:36, 09:40 and 09:48 attempts: step 1 is a deploy
action (`git pull origin master` on production), forbidden by this worker's
no-deploy rule, and there is nothing to pull anyway. What was done instead:
step 1 is now packaged as `scripts/vps-deploy-step1.sh`, a guarded,
verified wrapper the owner runs as the first command of the manual deploy.

## Verified state (read-only, 10:16–10:20)

- After `git fetch`: `origin/master` still `568e46c`; local `master` 5 ahead,
  head `0e18197` (the empty `[skip ci]` commit from 10:15). The owner has not
  pushed.
- SSH read-only (`ssh -i ~/.ssh/narun_vps_ed25519 narun@69.62.73.225`):
  VPS `HEAD` still `568e46c`; `git status --short -- chess-backend/bootstrap/cache`
  still shows ` M packages.php`, ` M services.php`, `?? routes-v7.php` —
  the expected pre-deploy state, unchanged since 09-14 00:10.
- `curl https://api.chess99.com/up` → 200.

Why running step 1 *now* would also be mechanically wrong: with
`origin/master == VPS HEAD`, the checkout would restore the tracked
dev-provider manifest and the pull would bring nothing ("Already up to
date", so the `||` recovery never fires) — prod would be left erroring
"class not found". This premature-run case is exactly what the new wrapper
guards against.

## scripts/vps-deploy-step1.sh (new, uncommitted)

Runs on the VPS via `bash -s` over SSH; `CHESS99_REPO_ROOT`/`CHESS99_UP_URL`
env overrides exist for testing. Behavior:

1. `git checkout --` the two manifests, tolerating "pathspec did not match"
   (the post-`d48a4b4` state where they are untracked).
2. `git pull origin master`; on failure: abort the merge if one is in
   progress (so "re-run step 1" is actually possible), run the notes'
   recovery fallback (`rm -f` both manifests + `php artisan package:discover`,
   rm-first is required), then exit 1.
3. If the manifests are still git-tracked after the pull (step 1 run before
   the push), rebuild the prod manifest immediately so the site keeps
   working, and fail with "cache NOT clean".
4. Rebuild the manifest once after a successful pull too, so prod never
   serves the dev-only provider listing in the gap before step 2.
5. Verify `git status --short -- chess-backend/bootstrap/cache` is empty and
   `/up` is 200; exit 0 only when both hold ("continue from step 2").

## Verification (local simulation, no VPS contact)

`bash -n` clean. Harness in the OS temp dir (shimmed `php`/`curl`, throwaway
git repos modelling the VPS states; deleted afterwards):

- Intended run (remote master has the untracking commit, manifests locally
  modified): checkout → fast-forward pull → untracked → rebuild → cache
  **clean**, `/up` 200, rc=0.
- Premature run (remote == local): warning, prod manifest rebuilt (site
  safe), cache reported **NOT clean** (` M`, ` M`, `??`), rc=1.
- Pull conflict (diverged local commit): merge aborted, recovery fallback
  ran, rc=1, `MERGE_HEAD` gone — a re-run fails cleanly instead of getting
  stuck mid-merge.
- Repeat run after the untracking landed: "pathspec did not match"
  tolerated, pull up-to-date, cache clean, rc=0 (idempotent).

`scripts/deploy-notes.md` step 1 now mentions the wrapper one-liner.
Nothing was pushed, committed, or changed on the VPS; no application tests
were run (no code that ships to the app changed).

## Owner steps at the next deploy

1. `git push origin master` (head `0e18197` has `[skip ci]`, so `deploy.yml`
   will not start).
2. From the repo root, as step 1 of the manual deploy:
   `ssh -i ~/.ssh/narun_vps_ed25519 narun@69.62.73.225 'bash -s' < scripts/vps-deploy-step1.sh`
   — the script streams from the laptop over SSH, so it does not need to be
   pushed or present on the VPS first; it performs the pull itself.
   (The plain copy-paste commands in `scripts/deploy-notes.md` step 1 remain
   equivalent minus the guards.)
3. Continue from step 2 (`composer install --no-dev`) in
   `scripts/deploy-notes.md`.
