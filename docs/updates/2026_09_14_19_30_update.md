# 2026-09-14 19:30 — Android Home device check after the `games/unfinished` fix; six files committed

Follow-up to docs/updates/2026_09_14_18_07_update.md and the "lists paused games" half left
open in docs/updates/2026_09_14_18_59_update.md.

## Outcome

**PASS.** Home shows no "Couldn't check for games to resume" banner, and "Continue playing"
lists a navigation-paused game. The six 18:07 files are committed on local `master` as
`4d7d6ed` `fix(android): read games/unfinished as a bare JSON array`. Not pushed.

## What was built and committed

The working tree also held another session's uncommitted edits made at 19:10–19:12:
- leave-game changes in `GameNavigationWarningDialog.kt`, `PlayMultiplayerScreen.kt`,
  `PlayMultiplayerViewModel.kt` and `PlayMultiplayerLeaveTest.kt`
- the backend `activeGames` ordering and `syntheticPlayer` change, plus `ActiveGamesListTest.php`
- a bot-name fallback hunk inside `HomeViewModel.kt` and `HomeViewModelTest.kt`

None of that is in the commit. To test exactly what was committed:

- Created a detached worktree at `aa4458d` (under `chess99-android/build/`, since removed).
  Copied in the six files, then deleted only the bot-name hunks (the `synthetic_player`
  fallback and its test). The worktree also needed the ignored `local.properties`,
  `app/google-services.json` (placeholder) and `third_party/stockfish-11`.
  `keystore.properties` was not copied.
- Ran `gradlew --no-daemon --offline :app:testDebugUnitTest :app:lintDebug :app:assembleDebug`
  there. Exit 0 in 4m 34s.
  - Unit tests: 254, with 0 failures, errors or skips (`HomeViewModelTest` 7, `JsonSafeTest` 12).
  - Lint: 0 errors, 0 warnings, 27 Information.
- Staged the worktree blobs straight into master's index with `update-index --cacheinfo`,
  so the other session's working copy was never touched. The staged patch was
  byte-identical to the worktree diff. Only those six paths are in the commit. The other
  session's changes are still uncommitted in the working tree.

## Device check

- Backend: local `php artisan serve` on 127.0.0.1:8000 (SQLite, test user 29), running the
  working-tree backend, **including the other session's uncommitted `activeGames` ordering fix**.
- Emulator: throwaway AVD `home_check` (android-35 google_apis x86_64) under
  `chess99-android/build/`, direct headless qemu, private adb server on 5039. The APK is
  the worktree build above.
- Test data: through the API, created casual bot game **466** (synthetic player 45) and
  called `POST games/466/pause-navigation`. `games/unfinished` then answered a **bare
  array** `[466 navigation]`, and `games/active` listed 466 first. Discarded afterwards
  with `DELETE games/466/unfinished` (200).

| Check | Result |
|---|---|
| Sign in -> Home | PASS: Home rendered |
| Resume-error banner | PASS: absent from the screenshot and the UI dump |
| "Continue playing" | PASS: card 1 **Paused**, "Playing as White · No moves yet", Resume + Discard; card 2 Active |
| Network | Second Home round `<-- 200 OK games/unfinished` and `<-- 200 OK games/active` |
| logcat | 0 `JsonSyntaxException` / "Expected a JsonObject" |

Screenshots: `2026_09_14_devicecheck_home_1_login_filled.png` (password masked) and
`2026_09_14_devicecheck_home_2_after_login.png`.

### Caveats

- Home dedups by id, and a paused game is also in `games/active`. So the Paused card is
  drawn from the active list. The `games/unfinished` half is proven by the 200 response
  and the missing banner: a conversion failure sets `resumeLoadFailed`. It is not proven
  by the card itself.
- Card names read "vs Opponent" because the bot-name fallback is not in this commit.
- logcat has `E HomeViewModel: Failed to load active/unfinished games for Home` at
  19:26:27. The stack trace is `JobCancellationException`: `refresh()` from init was
  cancelled by the `ON_RESUME` refresh 250 ms later. It is harmless, since the generation
  guard drops that round, but `runCatching` logs cancellations as errors. That is
  misleading in logs. Not changed.
- The first on-device sign-in failed because `adb shell input text` split the password at
  `@`. It was a tester error: the retry typed `@` as a key event.

## Cleanup

Emulator killed (`emu kill`), private adb server killed, `artisan serve` and its
`php -S` child stopped. The AVD, scratch folder, token file and worktree were deleted.
No process started by this run was left running. `qemu-system-x86_64` 74572
(AVD `LaunchLaneQA_20260914`, started 19:22 by another session) was left alone.
