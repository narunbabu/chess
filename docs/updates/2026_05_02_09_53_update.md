# Pending Location Approval Queue

## Summary
- Added a backend approval queue for user-entered profile locations.
- User-entered "Other" location text stays on the user profile while the request is pending.
- Platform admins can approve pending locations into the canonical country/state/district/mandal/village tables or reject them.
- Approval backfills matching user profiles and clears `location_other`.
- Fixed the guided tour card so it centers in mobile view.
- Preserved the organization request type fix so frontend values match backend validation.

## Verification
- `php -l` on touched PHP files passed.
- `php artisan migrate --pretend` passed.
- `php artisan test --filter=PendingLocationApprovalTest` passed.
