# 2026-09-14 09:48 — deploy-notes step 1 (bootstrap/cache checkout) at next manual deploy: blocked, no deploy yet

Owner task: "On next manual VPS deploy run deploy-notes step 1 bootstrap/cache checkout".

## Result: blocked

There has been no manual deploy since 2026-09-13. Step 1
(`scripts/deploy-notes.md` lines 47–48: `git checkout --` of
`bootstrap/cache/{packages,services}.php`, then `git pull origin master`)
discards files and pulls into production. That is a deploy action, and the
worker rules forbid it. It is also pointless before `master` is pushed: the
pull would find nothing new. Nothing on the VPS or GitHub was changed. Only
read-only checks were run.

## Evidence (read-only, 09:48)

- `git fetch origin`: local `master` is 3 ahead / 0 behind `origin/master`
  (`daf9405`, `d48a4b4`, `c9db620`). `origin/master` is still `568e46c`.
- `gh run list --workflow deploy.yml -L 2`: the last run is still
  2026-09-13 14:55 (`34764068752`, `568e46c`, failure, 14s).
- VPS (`ssh -i ~/.ssh/narun_vps_ed25519 narun@69.62.73.225`, rc=0):
  - `git rev-parse --short HEAD` → `568e46c`
  - `git status --porcelain -- chess-backend/bootstrap/cache` →
    ```
     M chess-backend/bootstrap/cache/packages.php
     M chess-backend/bootstrap/cache/services.php
    ?? chess-backend/bootstrap/cache/routes-v7.php
    ```
    Unchanged from 09:35/09:36/09:40, so step 1 is still needed.
- `curl`: `https://api.chess99.com/up` 200, `/api/v1/health` 200,
  `https://chess99.com` 200.

## Owner steps (unchanged)

1. Push `master`. Head `c9db620` has no `[skip ci]`, so the push starts
   `deploy.yml` (which currently fails on SSH auth).
2. On the VPS: `cd /opt/Chess-Web`, run step 1 exactly as written, then steps 2–7.
3. Verify:
   ```bash
   git status --short -- chess-backend/bootstrap/cache   # expect no output (routes-v7.php is now ignored)
   curl -s -o /dev/null -w "%{http_code}\n" https://api.chess99.com/up   # expect 200
   ```
