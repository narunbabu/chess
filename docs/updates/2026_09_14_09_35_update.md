# 2026-09-14 09:35 — Post-deploy check of VPS `bootstrap/cache` and `/up`: deploy has not happened

## Task
Owner: after the deploy that includes `d48a4b4`, confirm `git status` of `chess-backend/bootstrap/cache` is clean on the VPS and `/up` returns 200.

## Outcome
**Cannot confirm a clean tree: that deploy has not happened.** `/up` is 200. All checks were read-only. Nothing was pulled, checked out or restarted.

## Verified state
- `git fetch origin`: `origin/master` = `568e46c`. Local `master` is 3 ahead and 0 behind (`daf9405`, `d48a4b4`, `c9db620`), so `d48a4b4` is still unpushed.
- `gh run list --workflow deploy.yml -L 5`: the newest run is 2026-09-13T14:55Z for `568e46c` (failure, 14s). No run since.
- VPS over SSH as `narun@69.62.73.225` (read-only `git rev-parse` / `git status` / `ls`):
  - `HEAD` = `568e46c`, `origin/master` (VPS remote-tracking) = `568e46c`, `## master...origin/master`
  - `git status --porcelain -- chess-backend/bootstrap/cache`:
    ```
     M chess-backend/bootstrap/cache/packages.php
     M chess-backend/bootstrap/cache/services.php
    ?? chess-backend/bootstrap/cache/routes-v7.php
    ```
    **Not clean.** These are the expected pre-deploy diffs: the `--no-dev` manifests, plus an untracked route cache. `config.php` is also on disk. All files are owned by `www-data` and dated 2026-09-13 14:56–14:57.
- Locally, `git check-ignore -v chess-backend/bootstrap/cache/routes-v7.php` matches `bootstrap/cache/.gitignore:1:*.php`. After `d48a4b4` is deployed, `routes-v7.php` and `config.php` will be ignored as well, so an empty `git status` is the expected post-deploy result.
- HTTP (curl, 09:34 IST): `https://chess99.com` 200, `https://api.chess99.com/up` 200 (`text/html`, nginx), `https://api.chess99.com/api/v1/health` 200 `{"status":"healthy",...}`.

## Correction to earlier notes
SSH to the VPS **works** with `~/.ssh/narun_vps_ed25519` (`-o IdentitiesOnly=yes -o BatchMode=yes`). `id_hostvps` and `id_ed25519` are refused (`Permission denied (publickey)`). The "local SSH key is refused" blocker in earlier notes came from a bare `ssh narun@69.62.73.225`, which does not offer this key. Manual deploy steps can use `ssh -i ~/.ssh/narun_vps_ed25519 narun@69.62.73.225`.

## To finish this check after the deploy
```bash
ssh -i ~/.ssh/narun_vps_ed25519 -o IdentitiesOnly=yes narun@69.62.73.225 \
  'cd /opt/Chess-Web && git rev-parse --short HEAD && git status --short -- chess-backend/bootstrap/cache'
curl -s -o /dev/null -w '%{http_code}\n' https://api.chess99.com/up
```
Pass: `HEAD` is at or after `d48a4b4`, the status output is empty, and the code is `200`.

## Tests
No code changed, so no test suites were run. Only the read-only probes above.
