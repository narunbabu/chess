# 2026-09-17 02:00 — Android unit-test gate re-run offline (AGP deps available)

## Task

"Run the Android unit-test gate when AGP dependencies are available."

## Availability check

The shared read-only Gradle dependency cache `C:\Users\ab\.gradle\caches` is
present and populated, so AGP and every plugin/dependency resolve with
`--offline`. No network resolution was attempted and none was needed.

## What was run

Same offline recipe as `docs/updates/2026_09_16_17_40_update.md` §2 (project-local
Gradle home, read-only shared dep cache, `--no-daemon`, `--offline`, in-process
Kotlin; `WS_KEY_RELEASE` passed through the environment because
`--gradle-user-home` does not read `~/.gradle/gradle.properties`):

```powershell
$env:WS_KEY_RELEASE='<from ~/.gradle/gradle.properties>'   # length 20
$env:GRADLE_RO_DEP_CACHE='C:\Users\ab\.gradle\caches'
$env:ANDROID_USER_HOME='D:\ArunApps\Chess-Web\chess99-android\.gradle\android-user-home'
.\gradlew.bat --no-daemon --offline `
  --gradle-user-home 'D:\ArunApps\Chess-Web\chess99-android\.gradle' `
  '-Pkotlin.compiler.execution.strategy=in-process' :app:testDebugUnitTest --rerun
```

The first invocation (without `--rerun`) reported `:app:testDebugUnitTest
UP-TO-DATE` — a cached pass from the 2026-09-16 17:30 run, not an execution.
`--rerun` was added so the gate actually executed the suite.

## Result

`BUILD SUCCESSFUL in 1m 10s`, exit 0, `33 actionable tasks: 1 executed, 32 up-to-date`.

Parsed from `app/build/test-results/testDebugUnitTest/*.xml` (36 class reports,
newest written 2026-09-17 01:58):

| Metric | Value |
|---|---|
| tests | **315** |
| failures | **0** |
| errors | **0** |
| skipped | **0** |

This matches the 315/0 recorded on 2026-09-16 17:30, now re-confirmed by a real
execution on the current working tree (Android sources are unchanged since that
session — the tree is still uncommitted).

## Scope and hygiene

- Only `:app:testDebugUnitTest` was run; `lintDebug`, `assembleDebug`,
  `connectedDebugAndroidTest` and the release gate were out of scope for this task.
- No source changes, no commit, no push, no deploy.
- `--no-daemon` throughout; `Get-Process java` afterwards returns nothing, so no
  Gradle/Kotlin daemon was left running.
