# Prior Done recently entries

Archived from STATUS.md during artifact disposition; historical claims were not reverified in this task.

- 2026-09-14 10:33: Owner task "rotate the CI deploy key with scripts/rotate-ci-deploy-key.sh": **blocked — the script is owner-only by design** (its header says agents must not run it; it creates a `~/.ssh` private key, edits the VPS `authorized_keys` and sets GitHub secrets — all forbidden by this session's credentials/server/account rules; same ruling as 00:15/00:55/09:43). Verified read-only: `bash -n` + `--dry-run` clean (rc=0, nothing created), still no `~/.ssh/chess99_gha_deploy`, admin SSH login OK, `gh` authed as narunbabu with `repo`+`workflow`, all three secrets still dated 2025-10-02, latest `deploy.yml` run still the failed 09-13 `34764068752`. Rotation remains a real owner blocker; copy-paste owner order (now simpler — head `0e18197` has `[skip ci]`, so the needed push won't deploy) in the evidence. [Evidence](docs/updates/2026_09_14_10_33_update.md).
- 2026-09-14 10:20: Owner task "run deploy-notes step 1 (VPS bootstrap/cache cleanup) at the next deploy": **blocked — no deploy has happened, nothing was run on the VPS** (step 1 is a deploy action; same ruling as 01:55/02:05/09:36/09:40/09:48). Re-verified read-only after fetch: `origin/master` still `568e46c` (local 5 ahead, head `0e18197`), VPS HEAD `568e46c`, `bootstrap/cache` still dirty (` M packages.php`, ` M services.php`, `?? routes-v7.php`), `/up` 200. Running step 1 now would also restore the dev-provider manifest for nothing (pull brings no commits). Delivered instead: `scripts/vps-deploy-step1.sh`, a guarded one-command wrapper for step 1 (recovery fallback, premature-run and failed-merge guards, post-checks; exit 0 only when the cache is clean and `/up` 200), verified against simulated VPS git states (intended/premature/conflict/repeat, all rc-correct); referenced from `deploy-notes.md` step 1. Owner: push master, then run the wrapper as step 1 of the manual deploy. [Evidence](docs/updates/2026_09_14_10_20_update.md).
- 2026-09-14 10:15: Owner task "push master (3 commits), empty `[skip ci]` commit first if no deploy is wanted": **created the empty `[skip ci]` head commit `0e18197`; the push itself is blocked by this session's no-push rule**. After fetch, `origin/master` is still `568e46c` and local master was 4 ahead (the task's "3" predated `872ced4`), now 5 ahead with the empty head; no deploy is wanted from the push because `deploy.yml`'s SSH key is still broken and the deploy is planned manually. Verified `0e18197` changes no files. No push, workflow toggle or VPS action. Owner: `git push origin master`. [Evidence](docs/updates/2026_09_14_10_15_update.md).
- 2026-09-14 10:10: Re-ran the post-deploy `bootstrap/cache` + `/up` check (the 10:04 connection failures were transient; SSH and HTTPS both work now). **`/up` 200, but the VPS `bootstrap/cache` status is not empty** (` M packages.php`, ` M services.php`, `?? routes-v7.php`): the deploy including `d48a4b4` has not happened — VPS `HEAD` still `568e46c`, and after fetch `origin/master` is still `568e46c` with local master 4 ahead / 0 behind. This is the expected pre-deploy state, not a regression. Read-only; owner deploy TODO stays open. [Evidence](docs/updates/2026_09_14_10_10_update.md).
- 2026-09-14: Owner task "run deploy-notes step 1 on the server, then continue from step 2": **blocked by this run's explicit no-deploy and project-folder-only rules**. Reviewed `scripts/deploy-notes.md`: step 1 restores the two cache manifests and pulls with the recovery fallback; step 2 starts production dependency installation. No SSH, server changes, push or commit attempted. Deployment gates and live state were not verified; application tests were not run for this status-only update. Existing owner deploy TODO remains open.
- 2026-09-14: Owner task `git push origin master`: **blocked by this session's no-push/no-deploy/no-account-change rules**. Local checks confirm `master` at `872ced4`, 4 ahead / 0 behind cached `origin/master` (not fetched), and local `deploy.yml` triggers on pushes to master. No push, workflow toggle or commit attempted; remote workflow state and deployment gates remain unverified. Owner push TODO stays open; only this status entry changed, and application tests were not run.
- 2026-09-14 10:04: Re-ran the post-deploy checks from `docs/updates/2026_09_14_09_35_update.md`: **blocked by connection failures** (SSH port 22 permission denied; HTTPS curl error 7, code 000). Deployed HEAD, cache cleanliness and `/up` are unverified this session; owner post-deploy TODO stays open. [Evidence](docs/updates/2026_09_14_10_04_update.md).
- 2026-09-14: Owner task push d48a4b4 with CI suppressed and manual deploy step 1 using narun_vps_ed25519: **blocked by this session's explicit no-push/no-deploy/no-account-change rules**. Local read-only checks: master HEAD 872ced4, 4 ahead / 0 behind cached origin/master (not fetched); d48a4b4 is in local history. Reviewed deploy-notes step 1 and local push-triggered deploy.yml. No push, commit/amend, workflow toggle, SSH or VPS action attempted; remote state and deployment gates not verified this session. Existing owner TODO remains open.
- 2026-09-14: **Deleted** the 10 untracked `.tmp-*` files in the repo root. They were leftovers from the 09-13 runs: reverb/backend/frontend logs, their PID file and `.tmp-start-servers.ps1` from 19:51, and the 18:15 Android gate log. First checked that none of the three logged PIDs were still running and that ports 3000, 8000 and 8080 had no listeners. The Android log recorded `:app:testDebugUnitTest FAILED` at 18:15, before the later 09-13 unit-test fixes. It was not re-run. `git status` now lists no `.tmp-*`. No code changed.
- 2026-09-14: **Committed** `chess99-android/scripts/check_backend_health.ps1` as `872ced4` on local `master`. The script is now a pass/fail gate (`/up` 200, `/api/v1/health` 200 + `healthy`, `/api/v1/synthetic-players` 401, chess99.com 200; exit 0/1). Run against prod before committing: 4/4 OK, rc=0. A copy with one wrong expected status gave rc=1. Only that file was committed. Not pushed: local is now 4 ahead / 0 behind, and head `872ced4` has no `[skip ci]`, so a plain push still starts `deploy.yml`.
- 2026-09-14 09:48: Owner task "on next manual VPS deploy run deploy-notes step 1 `bootstrap/cache` checkout": **blocked, no deploy has happened, nothing run on the VPS**. Step 1 is a deploy action (worker rules forbid it), and before `master` is pushed the pull would bring nothing. Read-only re-check: local 3 ahead / 0 behind, `origin/master` and VPS HEAD both `568e46c`, last `deploy.yml` run still 09-13. VPS `bootstrap/cache` still shows ` M packages.php`, ` M services.php`, `?? routes-v7.php`. `/up`, `/api/v1/health`, chess99.com all 200. [Evidence](docs/updates/2026_09_14_09_48_update.md).
- 2026-09-14 09:45: Owner task "disable `deploy.yml`, push `master`, re-enable (or amend `[skip ci]` into `d48a4b4`)": **blocked, nothing pushed, toggled or amended**. The worker rules forbid `git push` and GitHub account changes. Correction: GitHub checks only the **head** commit for `[skip ci]`. Amending `d48a4b4` leaves head `c9db620` without it, so the deploy would still fire. Reword `c9db620` or add an empty `[skip ci]` commit instead. Read-only checks: 3 ahead / 0 behind, `deploy.yml` active and push-triggered, `ci.yml` PR-only, `/up` 200, `/api/v1/health` 200. [Evidence + both owner options](docs/updates/2026_09_14_09_45_update.md).
- 2026-09-14 09:43: Owner task "rotate CI deploy key and run `deploy.yml` check_only": **blocked, no change made**. The rotation script is owner-only: it creates a key, edits the VPS `authorized_keys` and sets GitHub secrets. `check_only` is also missing from `origin/master`'s `deploy.yml`, because it exists only in the unpushed `daf9405`. Read-only checks: dry run clean, no `~/.ssh/chess99_gha_deploy` yet, the secrets were last set 2025-10-02, and the last deploy run (09-13) failed. Health checks: `/up`, `/api/v1/health` and chess99.com all 200. [Evidence + owner steps](docs/updates/2026_09_14_09_43_update.md).
- 2026-09-14 09:40: Owner task "at next manual deploy run step 1, then check `bootstrap/cache` and `/up`": **blocked, no deploy has happened**. Running step 1 is a deploy action, and the worker rules forbid it. Read-only checks: `origin/master` and the VPS are both still at `568e46c`, local is 3 ahead, and the last `deploy.yml` run was 09-13. VPS `bootstrap/cache` still shows ` M packages.php`, ` M services.php` and `?? routes-v7.php`, so it is not empty yet. `/up` 200, `/api/v1/health` 200. [Evidence](docs/updates/2026_09_14_09_40_update.md).
- 2026-09-14: Owner task "push master (`d48a4b4`, `daf9405`)": **blocked again, not pushed**. Worker rules forbid `git push`. Re-verified after `git fetch`: local `master` is 3 ahead / 0 behind (`daf9405`, `d48a4b4`, `c9db620`). Head `c9db620` has no `[skip ci]`, so a plain push fires `deploy.yml`. Owner push options: `git push -o ci.skip` does not apply to GitHub; amend/add an empty `[skip ci]` commit, or push and accept the deploy. `/up` 200, `/api/v1/health` 200.
- 2026-09-14 09:36: Owner task "push `master`, then deploy-notes step 1": **blocked, nothing pushed or deployed**. The worker rules forbid `git push` and deploys. Re-verified read-only: local `master` is 3 ahead / 0 behind (`daf9405`, `d48a4b4`, `c9db620`). Head `c9db620` lacks `[skip ci]`, so a push starts `deploy.yml`. VPS is still at `568e46c` with `bootstrap/cache` dirty, so step 1 is still needed. `/up` and `/api/v1/health` return 200. [Evidence](docs/updates/2026_09_14_09_36_update.md).
- 2026-09-14 09:35: Post-deploy check of VPS `bootstrap/cache` and `/up`: **deploy not done yet**, so a clean tree cannot be confirmed. `origin/master` and the VPS `HEAD` are both `568e46c`, and no `deploy.yml` run has happened since 09-13. VPS `git status -- chess-backend/bootstrap/cache` shows ` M packages.php`, ` M services.php` and `?? routes-v7.php`, the expected pre-deploy state; the new `*.php` ignore covers `routes-v7.php`. `/up` 200, `/api/v1/health` 200 healthy. SSH **works** with `~/.ssh/narun_vps_ed25519`; only the other keys are refused. [Evidence](docs/updates/2026_09_14_09_35_update.md).
- 2026-09-14: **Committed** the leftover doc edits on local `master`, on top of `d48a4b4`: the `scripts/deploy-notes.md` step-1 `||` fallback and sudoers-state paragraph (matched against the 00:45 and 02:05 evidence), the `PHONE_TEST.md` 09-13 APK refresh (matches `568e46c`), this file, the 22 `docs/updates` notes from 09-13 and 09-14 (placeholders only, no secrets) and the Karta brief E052. Nothing was discarded. Left uncommitted because they are not docs: `check_backend_health.ps1` (run today: 4/4 OK, rc=0), `scripts/rotate-ci-deploy-key.sh`, `chess-frontend/scripts/smoke-takeback-live.cjs` and the `.tmp-*` logs. The new head also lacks `[skip ci]`, so a push still starts `deploy.yml`. Not pushed.
- 2026-09-14 09:29: Owner task "push `d48a4b4` to origin/master": **not pushed, blocked**. The worker session rules forbid `git push`. Also, the head commit `d48a4b4` has no `[skip ci]`, so a plain push fires `deploy.yml` (`on: push: [master]`, no path filter), which is a prod deploy once the CI key works. Verified: fast-forward, 2 ahead / 0 behind `origin/master` after fetch. Skip-deploy push options are in the [evidence](docs/updates/2026_09_14_09_29_update.md).
- 2026-09-14 02:05: Retried the VPS `bootstrap/cache` checkout: **still blocked**. It is a deploy action, `d48a4b4` is unpushed, and the SSH key is still refused (rc=255). The 01:55 fallback in step 1 turned out to be broken: `package:discover` also fails against the bad manifest (reproduced, rc=1). Step 1's `||` branch now deletes both files before discovering. Verified locally: rc=0, byte-identical regeneration, backups restored. [Evidence](docs/updates/2026_09_14_02_05_update.md).
- 2026-09-14: VPS `bootstrap/cache` checkout before the first manual pull: **not run, blocked**. It is a deploy action, `d48a4b4` is still unpushed, and this machine's SSH key is refused by the VPS. Found and fixed a hazard in deploy-notes step 1. The restored `packages.php` lists dev-only providers, so a pull that fails after the checkout leaves prod erroring "class not found". Reproduced locally (`artisan` rc=1, then restored: identical files, rc=0). Step 1 is now `git pull origin master || (cd chess-backend && php artisan package:discover)` and is safe to repeat. [Evidence](docs/updates/2026_09_14_01_55_update.md).
- 2026-09-14: **Committed** the `bootstrap/cache` untracking locally as `d48a4b4`: `.gitignore` +1, `packages.php` −93, `services.php` −282, plus the deploy-notes step-1 `git checkout --`. The notes' separate sudoers-state paragraph was left uncommitted. The files stay on disk and are ignored. Re-ran `php artisan test` before committing: 356 passed / 158 skipped / 1 risky, rc=0. Not pushed or deployed.
- 2026-09-14: **Stopped tracking** the generated `chess-backend/bootstrap/cache/{packages,services}.php`. They were removed with `git rm --cached` and a new `bootstrap/cache/.gitignore` of `*.php` was added (staged, not committed). `package:discover` recreates both byte-identically. A simulated VPS pull fails with the files modified, then succeeds after `git checkout --`, which is now step 1 in `scripts/deploy-notes.md`. `php artisan test`: 356 passed / 158 skipped, rc=0. [Evidence](docs/updates/2026_09_14_01_45_update.md).
- 2026-09-14: Tried to side-load the 09-13 phone-test APK and follow `PHONE_TEST.md`. **Blocked: no phone connected.** `adb devices` (checked twice), `adb mdns services` and `Get-PnpDevice` found no Android device, so `adb install -r` was not run and no checklist item was tested. Offline check: the APK's SHA-256 `3184938e…` matches the 09-13 build records, and it includes `arm64-v8a` libraries. [Evidence](docs/updates/2026_09_14_01_35_update.md).
- 2026-09-14: Ran `:app:connectedDebugAndroidTest -PDEBUG_TARGET=prod` for the phone and it is **blocked: no device connected**. `adb devices` (checked twice), `adb mdns services` and `Get-PnpDevice` all came up empty, and no emulator was attached this time. Gradle compiled the 9-test `StockfishEngineInstrumentedTest` suite cleanly, then failed with `DeviceException: No connected devices!` (rc=1). Nothing was installed and no test ran. [Evidence](docs/updates/2026_09_14_01_25_update.md).
- 2026-09-14: **Written up and deleted** the untracked `.tmp-walkthrough/` from the 09-13 21:44–22:50 emulator TalkBack session (28 MB). Its `installed.apk` is byte-identical to the kept 09-09 phone-test APK, which predates `568e46c`. The only evidence is a Register screenshot with TalkBack focus on the title and a Login uiautomator dump. The emulator relaunches never logged boot-complete. No pass/fail results survived, so **nothing counts as acceptance**, and the device/TalkBack TODOs stay open. The adb/TalkBack driving technique is kept in the note. [Evidence](docs/updates/2026_09_14_01_15_update.md).
- 2026-09-14: `chess99-android/scripts/check_backend_health.ps1` **verified under PowerShell 7.6.6**. pwsh 7 is not installed here, so a portable zip was used from a temp folder and then deleted. Prod run: 200/200/**401**/200, PASS, rc=0. The 401 is read from the exception. Against a local stub, pwsh 7 and PS 5.1 agree: 404, 500, an unexpected 200, a JSON body without "healthy" and a refused connection all FAIL, and `-File` exits 1. No code change needed. [Evidence](docs/updates/2026_09_14_01_05_update.md).
- 2026-09-14: CI deploy key rotation (keygen, `ssh-copy-id` to `narun@69.62.73.225`, set `SERVER_SSH_KEY`/`SERVER_USER`/`SERVER_HOST`) is **still blocked on the owner and was not done**. It changes keys and secrets, which this worker may not touch. Read-only re-check: secrets are still dated 2025-10-02 and the latest deploy run `34764068752` still fails. The `chess99_gha_deploy` key does not exist yet. The admin key logs in and `sudo -n` works. `ssh-copy-id` is available. The script passes `bash -n` and its dry run (rc=0) created nothing. [Evidence](docs/updates/2026_09_14_00_55_update.md).
- 2026-09-14: Sudoers rule for `narun` is **not needed, and nothing was changed on the VPS**. Checked over read-only SSH: `sudo -n true` already succeeds because narun has `(ALL) NOPASSWD: ALL`, and all 8 deploy `sudo` commands pass `sudo -n -l`. The exact-match rule in `scripts/deploy-notes.md` matches the VPS binary paths and passes `visudo -c` there, kept for narrowing later. [Evidence](docs/updates/2026_09_14_00_45_update.md).
- 2026-09-14: Committed the `deploy.yml` `check_only`/sudo change and `scripts/deploy-notes.md` on local `master` as **`daf9405`** (YAML parses; `[skip ci]` in the message). **Not pushed and `gh workflow run deploy.yml -f check_only=true` not run.** The session rules forbid push and deploy. Also, `on: push: [master]` has no path filter, so a push without `[skip ci]` would start a full prod deploy. The check-only run will keep failing at SSH until the key is rotated. Owner commands are in the evidence file. [Evidence](docs/updates/2026_09_14_00_35_update.md).
- 2026-09-14: Re-checked the VPS over SSH (read-only, HEAD `568e46c`): **healthy**. `chess-reverb` (port 8080), `chess-queue`, php8.3-fpm, nginx, mysql and redis are all active. The queue's 60 restarts are the hourly `--max-time` recycle, and the second `queue:work` belongs to `thogata-queue`. `failed_jobs=0`. `migrate:status` rc=0, batch 46 is the latest and nothing is pending. All three public health URLs return 200. **Found:** the server checkout has modified tracked `bootstrap/cache/{packages,services}.php`, which could block the next `git pull`. Reverb journal not readable by `narun`. [Evidence](docs/updates/2026_09_14_00_10_update.md).
- 2026-09-14: The two-client takeback smoke is **still blocked: no second prod account exists**. `.env.local` holds only account A and no `CHESS99_SMOKE_B_*` is set anywhere. **No account was created**, because registering on live prod and saving a password is owner-only. Ran `node scripts/smoke-takeback-live.cjs pair`: SKIP, exit 2, with no network calls. The owner steps (about 5 minutes) are in the evidence file. [Evidence](docs/updates/2026_09_14_00_25_update.md).
- 2026-09-14: CI deploy key is **still not rotated** (owner-only: key + GitHub secrets). Re-checked read-only: secrets unchanged since 2025-10-02 and the latest deploy run `34764068752` still fails. Added `scripts/rotate-ci-deploy-key.sh`, a one-command owner rotation that is idempotent, has `--dry-run` and checks login and sudo. `bash -n` passes. Dry run: rc=0, and it created nothing. [Evidence](docs/updates/2026_09_14_00_15_update.md).
- 2026-09-14: Checked the VPS over SSH (read-only, HEAD `568e46c`). `php artisan migrate:status` shows `2026_08_29_100000_create_tester_program_tables` as **Ran** in batch 46, alongside the three 2026-07 migrations, and `--pending` reports none. `Schema::hasTable` confirms `tester_applications` and `tester_submissions` both exist, with 0 rows. Nothing was changed or deployed. [Evidence](docs/updates/2026_09_14_00_05_update.md).
- 2026-09-13: Tried `:app:connectedDebugAndroidTest -PDEBUG_TARGET=prod` on arm64 and it is **blocked: no arm64 target**. There is no phone on USB, Wi-Fi or PnP. The only SDK images are x86_64 and the host is AMD64, so an arm64 emulator is not possible either. Gradle was **not** run, because it would have installed onto the only attached device, VidyaPath's x86_64 `emulator-5580`. The owner runbook is in the evidence file. [Evidence](docs/updates/2026_09_13_23_52_update.md).
- 2026-09-13: Tried the phone-test APK side-load again and it is **still blocked: no arm64 phone found** on USB (`adb devices`, `Get-PnpDevice`) or Wi-Fi (`adb mdns services`). The only device is the x86_64 VidyaPath emulator `emulator-5580`, which was left alone. APK SHA-256 still matches. Nothing was installed or tested, and the `[needs a phone]` TODO stays open. [Evidence](docs/updates/2026_09_13_23_50_update.md).
- 2026-09-13: Tried the physical-device walkthrough but **could not run it: no phone was connected**. `adb devices` showed only `emulator-5580`, another project's (VidyaPath) emulator mid-way through a TalkBack test, which was left alone. The handoff APK's SHA-256 matches the recorded value and it packages arm64-v8a. Nothing was tested and the device TODOs stay open. [Evidence](docs/updates/2026_09_13_23_58_update.md).
- 2026-09-13: Fixed a stale expectation in `chess99-android/scripts/check_backend_health.ps1`. `/api/v1/synthetic-players` is inside `auth:sanctum`, so the script now expects 401 there (a 200 would mean the route lost its auth). It also checks `/api/v1/health` reports `healthy` and exits 1 on failure. Against prod under PS 5.1: PASS, exit 0. A negative test exits 1. PS7 was not run because it isn't installed. [Evidence](docs/updates/2026_09_13_23_55_update.md).
- 2026-09-13: Found why CI `Deploy to Production` has failed since 2026-02-06 (last green run 2025-12-12). The VPS rejects `SERVER_SSH_KEY`: `unable to authenticate … [none publickey]`. The key parses, and the secrets are unchanged since 2025-10-02, so the server side changed. **The secret was not rotated**, because keys and GitHub secrets are owner-only. Added a `check_only` dispatch input to `deploy.yml` that tests SSH, write access and sudo without deploying. Wrote the owner rotation guide in `scripts/deploy-notes.md` § CI Deploy Key. YAML parses; `bash -n` passes. [Evidence](docs/updates/2026_09_13_23_42_update.md).
- 2026-09-13: Post-deploy checks green (chess99.com, `/up`, `/api/v1/health` 200 healthy, assetlinks `application/json`). The live takeback smoke against production passed **13/13** on the synthetic leg: server auto-accept, `game.undo.accepted` on the real socket in 48ms, board reset persisted, test game resigned. The two-client leg is written in `chess-frontend/scripts/smoke-takeback-live.cjs` but has not run, because a second prod account is needed. [Evidence](docs/updates/2026_09_13_23_40_update.md).
- 2026-09-13: Checked the push/merge + VPS deploy task against live state and found it already done, so nothing was re-pushed or redeployed. After `git fetch`, local HEAD, `origin/master` and `origin/chore/unfreeze-working-tree-2026-09-02` are all `568e46c`. The four migrations the earlier deploy applied are exactly the four files dated after 2026-07 (the fourth being `2026_08_29_100000_create_tester_program_tables`). Live URLs checked from outside: chess99.com 200, api.chess99.com/up 200, `/api/v1/health` 200 JSON, assetlinks.json 200 `application/json`. Server-side `migrate:status` and service state were not re-checked (no SSH in this session).
- 2026-09-13: Rebuilt the prod-target phone-test APK at `568e46c` (the deployed commit, with the nav/logout and takeback fixes): `chess99-android/chess99-1.0.0-phone-test-2026-09-13.apk`, 29.2 MB, debug-signed (SHA-1 `D5:DF:…:82:31`), SHA-256 `3184938e…73ee88`. Fresh gates: `testDebugUnitTest` 246/0 failures, `lintDebug` 0 errors/12 warnings. No device attached, so nothing installed. [Evidence](docs/updates/2026_09_13_23_10_update.md).
- 2026-09-13: Pushed and deployed to production per owner decision. Pushed `chore/unfreeze-working-tree-2026-09-02` → origin and fast-forwarded `origin/master` `87cdc0a..568e46c` (35 commits, includes the five `feat/tier1` backend commits and the previously unpushed `7cc6ff4`). VPS deploy per `scripts/deploy-notes.md` (SSH `narun@69.62.73.225`, key `~/.ssh/narun_vps_ed25519`): `git reset --hard origin/master` at `568e46c`, `composer install --no-dev --optimize-autoloader`, `migrate --force` applied the four additive pending migrations (`2026_07_06_100000` chat safety, `2026_07_06_110000` guardian-child, `2026_07_10_100000` age/guardian-consent — all verified additive, drops only in `down()`), config/cache/route/view clears + config/route re-cache, frontend `pnpm build` + `rsync -a --delete build/ /var/www/chess99.com/`, storage/bootstrap-cache chown www-data, `chess-reverb` restart, `php8.3-fpm` + `nginx` reload. Post-deploy: chess99.com and api.chess99.com/up both HTTP 200 (from VPS and externally), `/api/v1/health` healthy JSON, all three services active, nginx error log clean, `check_assetlinks.ps1` **PASS** (assetlinks.json now 200 `application/json` — fixes the server-side copy problem). `check_backend_health.ps1`: `/up` and chess99.com OK; its `synthetic-players` 401 expectation is stale (route is inside `auth:sanctum` in both old and new code — pre-existing, not a regression). CI `Deploy to Production` workflow run 34764068752 failed at SSH handshake — pre-existing broken/missing `SERVER_SSH_KEY` secret since June; deploy was performed manually over SSH instead.
- 2026-09-13: Certified the `0dda391` realtime fix deploy-ready and prepared the owner runbook (deploy itself is an owner action; no push/deploy performed by the agent). Fresh gates: backend full `php artisan test` 356 passed/158 skipped/1 risky/4,458 assertions (matches baseline); focused UndoTakebackTest + BroadcastEventNameContractTest 13 passed/315 assertions; `migrate --pretend` exit 0 — only the pre-existing additive `2026_08_29_100000_create_tester_program_tables` would apply via `migrate --force`; frontend `pnpm build` exit 0 and unit 12 suites/103 tests passed. `0dda391` carries no migrations; web/backend trees are byte-identical to `a18c036`, whose full gate pass incl. 10 Chromium E2E was green 2026-09-09. Owner steps: push/merge `chore/unfreeze-working-tree-2026-09-02` (33 commits ahead of `master`, includes the five `feat/tier1` backend commits), then VPS deploy per CLAUDE.md (`composer install --no-dev`, `migrate --force`, config/cache/route/view clears, `systemctl restart chess-reverb`, `reload php8.3-fpm`, frontend build copied to `/var/www/chess99.com/`, `reload nginx`), then `curl https://chess99.com` + `https://api.chess99.com/up`. Web E2E not re-run this session (redundant — identical tree already E2E-green).
- 2026-09-13: Committed the pending Android navigation/logout verification work after re-running its gates green: extracted `coldStartDestination` (restart-after-logout never resumes authenticated Home — unit-tested for returning-user/first-run/logged-in), fixed bottom-tab switches to `popUpTo(Screen.Home)` because after the post-login `popUpTo(0)` the graph start destination is off-stack so every tab switch accumulated and Back walked tab history instead of returning to Play, added `HomeViewModelTest` (6 tests: logout clears session + pending deep links before navigating and still navigates when the server revoke fails offline, in-flight refresh never repaints Home after logout, nearby error-vs-empty/retry recovery), and stubbed `getChatMessages` in `PlayMultiplayerTakebackTest` to stop an order-dependent ClassCastException leak. Fresh gates: `:app:testDebugUnitTest --rerun` 246 tests/0 failures/0 skipped (MainNavigationTest 5, HomeViewModelTest 6, PlayMultiplayerTakeback 5); `:app:lint` 0 errors/12 warnings (debug), matching baseline. Physical-device acceptance (walkthrough, TalkBack, authenticated two-client) remains open. Also brings in the previously uncommitted 2026-09-13 takeback status entry.
- 2026-09-13: Closed the takeback TODO — verified all three layers of the landed `0dda391` fix against root cause E003: (a) wire names `game.undo.request/accepted/declined` and `draw.offer.sent` now match on backend, web and Android, pinned by three contract tests; (b) synthetic opponents get a server-side responder — `GameRoomService::requestUndo` auto-accepts under a row lock with a position-version recheck (shared `applyUndo`); (c) unanswered requests expire (server cache 35s vs client 30s, `expires_at` in the event, late/duplicate accepts rejected, both clients auto-recover). Gates re-run green: backend UndoTakebackTest + BroadcastEventNameContractTest 13 passed/315 assertions; web full unit 103 passed (incl. 25 takeback/WS-contract focused); Android full `testDebugUnitTest` 246 passed/0 failures (incl. PlayMultiplayerTakeback 5, GameWebSocketService 4, RealtimeEventContract 1). No code changes required; uncommitted Android navigation/logout work (another TODO item) left untouched.
- 2026-09-09: Landed the ten-commit experience delivery (`2359d3a..a18c036`: takeback event parity + bot auto-accept/expiry, promotion dialog + accessible move entry, sounds, Learning Best budget + offline review, logout session clearing, bottom navigation, daily streak, web navigation + game-end, docs, opencode config). All gates green: frontend build, lint (0 errors/303 warnings), typecheck, 103 unit tests; backend 356 passed/158 skipped/1 risky (4,458 assertions), `migrate --pretend` clean (pre-existing pending only); Android `testDebugUnitTest` 238 tests/0 failures/0 errors, lint 0 errors/12 warnings/69 informational (plain `test` still stops at the known placeholder-`google-services.json` `verifyReleaseConfiguration` gate — release variants only). Fresh prod-target APK `chess99-android/chess99-1.0.0-phone-test-2026-09-09.apk` (27.74 MB, supersedes 09-08). Not pushed/deployed. [Evidence](docs/updates/2026_09_09_17_10_update.md).
- 2026-09-08: Fresh prod-target Android test APK built/signed (`chess99-1.0.0-phone-test-2026-09-08.apk`): 238 JVM + 9 Android15 native-engine tests passed; build/lint passed (0 errors/12 warnings). Emulator install/cold launch, guest Black game, accessible Nc6 move, computer reply, resign and local Review verified. Fixed chosen colour, local-rated fallback, corrupt review loading, sounds and multiplayer 48dp/accessibility. Bounded fresh review found no new P0/P1; NOT full parity/Play certification. [Evidence](docs/updates/2026_09_08_android_phone_test.md).
- 2026-09-08: Owner approved implementation; delta-spec recorded. Local web guest-first entry, four-destination navigation, Learn hub, explicit colour selection, first-tour clock/engine gate + refresh recovery, accessible legal moves/promotions, canonical takeback events and expiry recovery implemented. Build/lint passed; 84 existing unit + 19 focused tests and 10 Chromium E2E passed at this checkpoint. Not deployed; final integration/independent review still in progress.
- 2026-09-08: Reviewed live web guest entry/setup/tour plus current web/Android source; wrote [experience improvement plan](docs/chess99-web-android-experience-plan-2026-09-08.md). P1 findings: clock runs under first-use tour, undo/draw wire-name mismatch, Home Logout only navigates, missing Android local Best/Review, and board accessibility gap. Android device/runtime checks remain pending; no code or deployment changes.
- 2026-09-02: Local web single-player game-end Review patch and guest fallback added; prior record reports 6/6 mocked E2E and frontend gates green. Existing uncommitted work, not reverified or deployed in this review.
- 2026-09-02: Unfroze six weeks of changes in commit 3fa4c61; corrected Stockfish nested-repository ignores and removed tracked local settings from index. Nothing pushed.
- 2026-09-02: Corrected the earlier takeback assessment: client/backend event names do not match; HTTP 200 was not proof of delivery. September 8 source still has the mismatch alongside uncommitted backend synthetic/expiry work.
- 2026-08-29: Local community tester applications/report links/admin queue and promoter-cap work added; additive migration not run in production. Deferred behind core chess UX; not a verified live programme.
- 2026-08-24: Android Elo filtering, mode/time setup, Learning label and Undo eligibility added; native synthetic move metadata fixed and emulator-tested against production. Prior record: 204 JVM tests; August 24 debug APK supersedes earlier phone-test APKs.
- 2026-08-23: Google Sign-In decoupled from Firebase configuration; production-target debug APK built. OAuth fingerprint checks, production Firebase, App Links and release/internal-track verification remain owner/release tasks.
