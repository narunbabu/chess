# 2026-09-14 01:05 — check_backend_health.ps1 verified under PowerShell 7

Task: run `chess99-android/scripts/check_backend_health.ps1` under PowerShell 7
to verify its error-status path (4xx/5xx read from the exception).

No code change was needed. The script is still uncommitted in the working tree.

## Environment

PowerShell 7 is **not installed** on this laptop (only Windows PowerShell
5.1.26100.9444). Nothing was installed system-wide. The portable
`PowerShell-7.6.6-win-x64.zip` from GitHub releases was unpacked into a
throwaway `.tmp-pwsh/` in the repo and deleted afterwards.

## 1. Live prod run

| Shell | Output | rc |
|---|---|---|
| pwsh 7.6.6 Core | `/up` 200, `/api/v1/health` 200, `/api/v1/synthetic-players` **401**, `chess99.com` 200, `PASS` | 0 |
| Windows PowerShell 5.1 | same | 0 |

Under pwsh 7, `Invoke-WebRequest` throws on the 401. The script reads the
status from `$_.Exception.Response.StatusCode`, which is an
`HttpResponseMessage` enum, cast with `[int]`, and reports OK.

## 2. Local stub: every branch

A local Python `http.server` stub was started detached on 127.0.0.1:18765
and stopped afterwards. The real script body was run with only `$targets`
swapped:

| Target | Expect | pwsh 7.6.6 | PS 5.1 |
|---|---|---|---|
| `/unauth` → 401 | 401 | OK HTTP 401 | OK HTTP 401 |
| `/health-ok` → 200 `{"status":"healthy"}` | 200 + healthy | OK | OK |
| `/notfound` → 404 | 200 | FAIL HTTP 404, expected 200 | same |
| `/boom` → 500 | 200 | FAIL HTTP 500, expected 200 | same |
| `/ok` → 200 | 401 | FAIL HTTP 200, expected 401 | same |
| `/health-bad` → 200 `{"status":"degraded"}` | 200 + healthy | FAIL not "healthy" | same |
| port 18799, nothing listening | 200 | FAIL request failed ("actively refused") | FAIL request failed ("Unable to connect") |

Exit code with `-File` on a failing run: **pwsh 7 rc=1, PS 5.1 rc=1**. On the
passing prod run both returned rc=0, so the script works as a deploy gate
under both shells.
