# Android lint cleanup

Cleared the 12 Android lint warnings without changing lint configuration or dependencies.

- Converted five LobbyPreferences writes to AndroidX KTX `edit`, preserving asynchronous `apply` and grouped rating-range updates.
- Moved `modifier` to the first optional parameter in ChessBoardView, PlayerInfoBar, UpgradePromptCard, ReplayContent, and AnalysisLoadingIndicator. Existing callers use named arguments.
- Added a method-scoped `UseKtx` suppression to LocalGameReviewStore.save: KTX `edit(commit = true)` returns Unit and would discard the Boolean disk-write result exposed by this method. Synchronous persistence and the memory fallback are unchanged.
- Added a declaration-scoped `CredentialManagerSignInWithGoogle` suppression for the existing lint false positive. GoogleSignInHelper already rejects non-Google credentials, checks both Google token type constants, and parses with GoogleIdTokenCredential.createFrom. Authentication behavior is unchanged.

## Verification

Ran from chess99-android with project-local writable Gradle/Android homes, the existing read-only dependency cache, and the Kotlin compiler in-process:

```powershell
$env:GRADLE_RO_DEP_CACHE = 'C:\Users\ab\.gradle\caches'
$env:ANDROID_USER_HOME = 'D:\ArunApps\Chess-Web\chess99-android\.gradle\android-user-home'
.\gradlew.bat --no-daemon --offline --gradle-user-home 'D:\ArunApps\Chess-Web\chess99-android\.gradle' '-Pkotlin.compiler.execution.strategy=in-process' :app:lint :app:testDebugUnitTest :app:assembleDebug
```

- BUILD SUCCESSFUL in 9m 37s, exit 0; 62 tasks (23 executed, 39 up-to-date).
- Lint XML: **0 errors, 0 warnings, 57 informational notices**. No severity changes or baseline additions. The previous offline report had 69 informational entries; this count is not a dependency-upgrade claim.
- JUnit XML totals: **251 tests, 0 failures, 0 errors, 0 skipped**.
- Debug APK assembly passed. Existing Kotlin compiler deprecation warnings remain outside this lint task.
- `git diff --check -- chess99-android` passed.
- Build log: `logs/android-lint-cleanup-2026_09_14.log`; reports in `chess99-android/app/build/reports/` and `app/build/test-results/testDebugUnitTest/`.

An initial inline PowerShell invocation failed to quote environment values and was cancelled before analysis; the successful run used a project-local script. Its daemon PID 61448 and the successful run's daemon PID 85280 were both absent on final process checks. No emulator, ADB, server, or watcher was started. Process-command-line enumeration was denied by the sandbox; known daemon PIDs were checked directly instead.

No device or live Google sign-in test was run. Existing unrelated working-tree changes were preserved. No commit, push, deployment, or publishing.
