# 2026-09-14 02:05 — VPS `bootstrap/cache` checkout before the first manual pull (retry)

## Task
On the next manual deploy, run `git checkout --` of
`chess-backend/bootstrap/cache/{packages,services}.php` on the VPS before
`git pull`.

## Outcome: still blocked on the owner. Not run on the VPS.
- It is a production deploy action, and this worker may not deploy or push.
- `git log origin/master..master` still lists `d48a4b4` and `daf9405`, so no
  deploy can include the untracking yet.
- `ssh -o BatchMode=yes narun@69.62.73.225 true` → `Permission denied
  (publickey)`, rc=255. The VPS working tree was **not** inspected.
- CI does not need this step: `.github/workflows/deploy.yml` uses
  `git fetch origin` + `git reset --hard origin/master`.

## Defect found in the 01:55 fallback, and fixed
The 01:55 step 1 read
`git pull origin master || (cd chess-backend && php artisan package:discover)`.
That fallback cannot work. `package:discover` boots the app, and booting loads
the broken manifest, so it fails in the same way as every web request.

Local reproduction in `chess-backend` (both files backed up first):
1. Wrote `d48a4b4^:chess-backend/bootstrap/cache/packages.php` with
   `PailServiceProvider` renamed to a class that does not exist, to stand in
   for a `--no-dev` install. Deleted `services.php`.
2. `php artisan --version` → rc=1.
3. `php artisan package:discover` (the 01:55 fallback) → rc=1,
   `Class "Laravel\Pail\PailMissingServiceProvider" not found` in
   `ProviderRepository.php line 205`.
4. Deleted both files, then `php artisan package:discover` → rc=0.
   `php artisan --version` → rc=0. The regenerated `packages.php` is
   byte-identical (`cmp`) to the backup, and so is the `services.php` rebuilt
   on first boot.
5. Restored the backups: `cmp` identical for both, `php artisan --version` →
   Laravel 12.31.1 rc=0, `git status --short -- chess-backend/bootstrap/cache`
   empty. Backup directory deleted.

`scripts/deploy-notes.md` step 1 now reads:

```bash
git checkout -- chess-backend/bootstrap/cache/packages.php chess-backend/bootstrap/cache/services.php
git pull origin master || (cd chess-backend && rm -f bootstrap/cache/packages.php bootstrap/cache/services.php && php artisan package:discover)
```

If the pull failed, stop and fix the cause before step 2, then run step 1
again. It is safe to repeat.

## Owner steps (unchanged apart from the fallback)
1. Push `master` (`d48a4b4`, `daf9405`).
2. At the manual deploy, run step 1 exactly as above.
3. Afterwards `git status --short -- chess-backend/bootstrap/cache` must be
   empty and `curl -sI https://api.chess99.com/up` must return 200.

## Checks run
Only the reproduction above. No test suite was run: the only changes are
documentation, and the cache files were restored byte-identical. The last full
`php artisan test` run was at 01:45 (356 passed / 158 skipped, rc=0).
