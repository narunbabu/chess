# 2026-09-13 23:50 — Phone-test APK side-load: still blocked, no arm64 phone attached

(The file is stamped with this machine's clock at the time of the run. That
clock reads earlier than the `23_58` note from the previous attempt.)

## Task
Side-load `chess99-android/chess99-1.0.0-phone-test-2026-09-13.apk` on a physical
arm64 phone and follow `chess99-android/PHONE_TEST.md`.

## What was checked
- `adb devices -l` (adb 36.0.0) listed one device only:
  `emulator-5580 … model:sdk_gphone64_x86_64`. That is the VidyaPath emulator,
  not a phone and not arm64. It was not touched: nothing installed and nothing driven.
- `adb mdns services` showed only that emulator (`adb-EMULATOR36X2X12X0`), so
  there is no wireless-debugging phone on the network either.
- `Get-PnpDevice -PresentOnly`, filtered on ADB/Android/MTP/vendor names,
  returned nothing, so no phone is on USB at all, not even an unauthorised one.
- APK integrity: SHA-256 is still
  `3184938edc20dcf8a5808859c43811fffbf63e848b5f03525454426a8273ee88` (matches
  `2026_09_13_23_10_update.md`).

## Results
None. No install, no PHONE_TEST.md checklist step and no
`connectedDebugAndroidTest` run happened. Nothing is claimed. The adb server
was already running before this session, so no process was started or left behind.

## Owner action to unblock
Same as `2026_09_13_23_58_update.md`: connect an arm64 Android 8.0+ phone with
USB debugging on and accept the RSA prompt. (Or pair it with
`adb pair`/`adb connect` over Wi-Fi.) Then:
`adb -s <serial> install -r chess99-android/chess99-1.0.0-phone-test-2026-09-13.apk`
(stop if there is a signature conflict and do not uninstall), follow
`PHONE_TEST.md`, and run
`ANDROID_SERIAL=<serial> ./gradlew.bat :app:connectedDebugAndroidTest -PDEBUG_TARGET=prod --no-daemon`
from `chess99-android/`.
