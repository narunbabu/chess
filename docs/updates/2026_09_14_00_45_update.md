# 2026-09-14 00:45 — narun sudoers: `sudo -n` already works, no rule added

## Task
Add the exact-command NOPASSWD sudoers rule for `narun` if `sudo -n` fails.

## Checked (read-only SSH as `narun@69.62.73.225`, admin key `narun_vps_ed25519`)
- `id`: uid 1003, groups `narun sudo www-data users`.
- `sudo -n true`: **succeeds** (`SUDO_N_OK`).
- `sudo -n -l` shows `(ALL : ALL) ALL` and `(ALL) NOPASSWD: ALL`.
- `sudo -n -l <cmd>` is allowed for each of the 8 commands the deploy runs:
  `chown -R www-data:www-data …/storage`, `chown -R www-data:www-data …/bootstrap/cache`,
  `crontab -u www-data -l`, `crontab -u www-data -`, `nginx -t`,
  `systemctl restart php8.3-fpm`, `systemctl restart chess-reverb`, `systemctl reload nginx`.
- Binary paths match the documented rule: `/usr/bin/chown`, `/usr/bin/crontab`,
  `/usr/sbin/nginx`, `/usr/bin/systemctl` (`/bin` → `usr/bin`). sudo 1.9.15p5.
- The exact-match rule in `scripts/deploy-notes.md` § CI Deploy Key passes
  `visudo -c -f -` on the VPS (`stdin: parsed OK`, rc=0). Read from stdin, so no file was written.

## Result
The condition is false, so **no sudoers change was made**. The deploy's `sudo` steps
will not be skipped for lack of a password once the CI key is rotated. The CI
check-only run is still expected to fail at the SSH handshake until then.

## Worth an owner decision (not done)
`NOPASSWD: ALL` means whoever holds the CI deploy key (`SERVER_SSH_KEY`) gets
passwordless root on the VPS. To narrow it, install the validated exact-match rule
as `/etc/sudoers.d/chess99-deploy` and remove narun's `NOPASSWD: ALL` grant. Do both
in one root session and keep a second root shell open. Then re-run the check-only
workflow. The nginx config copy (`sudo cp` to a timestamped backup) is outside the
exact rule and would become a manual step, as the notes already say.
