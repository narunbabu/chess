# Android lint blocked before analysis

Requested: run Android lint on the current working tree.

Ran from `chess99-android` through PowerShell:

```powershell
./gradlew.bat --no-daemon --gradle-user-home D:/ArunApps/Chess-Web/chess99-android/.gradle :app:lint
```

The wrapper exited 1 while downloading Gradle 8.11.1 from
`https://services.gradle.org/distributions/gradle-8.11.1-bin.zip`:
`java.net.SocketException: Permission denied: getsockopt`.
Raw output: `logs/android-lint-2026_09_14_16_28.log`.

Lint never started, so no current-tree lint findings or pass can be reported.
The project-local wrapper cache contains an incomplete distribution download.
The first invocation also had a PowerShell exit-code forwarding error; a second
invocation corrected that and confirmed exit 1 with the same download failure.

No Android source, lint settings, credentials, or global configuration changed.
Existing working-tree changes were preserved. No commit, push or deployment.
The wrapper exited before launching Gradle; no daemon, server, emulator or
watcher was started or left running by this task.

Resume with permitted dependency network access or a provisioned writable
project-local Gradle distribution/dependency cache, then rerun the command above.
