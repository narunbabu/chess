# Android phone-test APK rebuilt at 568e46c — 13 September 2026

## Scope

Rebuild the prod-target phone-test APK from `568e46c` (the commit deployed to
production today) for the next device session. The build contains the Android
navigation/logout fixes, the takeback/draw event-name fixes and
`HomeViewModelTest`. No code changes. Nothing pushed, deployed or installed.

## Source

- HEAD `568e46c` on `chore/unfreeze-working-tree-2026-09-02`. `chess99-android/`
  had no uncommitted changes (`git status`/`git diff 568e46c` on that path were empty).

## Gates

Command, run from `chess99-android/`:
`gradlew.bat --no-daemon :app:testDebugUnitTest :app:assembleDebug :app:lintDebug -PDEBUG_TARGET=prod --console=plain --max-workers=2`

- Result: `BUILD SUCCESSFUL in 9m`, exit 0. `compileDebugKotlin`, `packageDebug`,
  `testDebugUnitTest` and `lintAnalyzeDebug` all ran (none were UP-TO-DATE).
- Unit tests: 246 tests, 0 failures, 0 errors, 0 skipped (counted from
  `app/build/test-results/testDebugUnitTest/*.xml`).
- Lint (debug): 0 errors, 12 warnings, 69 informational. Same as the baseline.
- Kotlin deprecation warnings (`PusherManager` `HttpAuthorizer`, and others) are
  the same ones seen before.

## Artifact

- `chess99-android/chess99-1.0.0-phone-test-2026-09-13.apk` (a copy of
  `app/build/outputs/apk/debug/app-debug.apk`; `*.apk` is gitignored)
- Size 29,217,676 bytes. SHA-256 `3184938edc20dcf8a5808859c43811fffbf63e848b5f03525454426a8273ee88`
- `apksigner verify`: Verifies, v2 scheme true, 1 signer, certificate SHA-1
  `D5:DF:BD:6C:3A:08:BB:72:5E:75:AD:FB:90:A1:8B:7A:9C:BD:82:31` (debug key, the
  same key as the 09-08 and 09-09 APKs, so `adb install -r` over them works)
- `aapt2 dump badging`: `com.chess99.app` 1.0.0 (1), target SDK 36, native code
  arm64-v8a / armeabi-v7a / x86_64
- The generated debug `BuildConfig` base URL is `https://api.chess99.com/api/v1/` (prod target).
- It replaces `chess99-1.0.0-phone-test-2026-09-09.apk` (built at `a18c036`,
  before the navigation/logout and takeback commits).

## Not done

- No device session: `adb devices` listed no devices. Install, the
  `PHONE_TEST.md` walkthrough and `:app:connectedDebugAndroidTest -PDEBUG_TARGET=prod`
  on arm64 are still open.
- No emulator run this session.
